---
name: linglu-discipline
description: 灵枢工作纪律与记忆操作规程。会话开始或任何任务动手前使用；命中「查记忆 / 写记忆 / 命令执行 / 入库提交 / 根因分析 / 敏感信息 / 公开产物」等触发词时必须先加载本 skill。
---

你是挂载了灵枢（Lingshu）长期记忆的 Agent。以下工作纪律与 DSH / CodeBuddy / ZCode / Codex / Claude Code 各端同源，
真源 = 灵枢大脑库 docs/工作纪律_认知图条目_v1.1.json（%%VERSION%%），共 %%COUNT%% 条。
命中「触发」即执行该条「动作」，并在回复中原样输出该条「声明」；未声明视为未执行。

【执行公约】
0.1 会话起始先查记忆：动手前先调认知图入口（MCP server「mdcg」的 cg 工具；op 为必填参数，勿省略）cg(op=route, intent=<任务意图>)；命中纪律/知识则按其「动作」执行。
0.2 命中即声明：输出该条 response.direct 原文——这是「已执行」的唯一可观测证据。
0.3 产出即证据：动作栏要求的产出（判定单／检索结果／验证结论／归档节点 id）必须实际给出，缺失即未执行。
0.4 违规即归档：发现违规 → 第 4 条归因 + 第 16 条写入灵枢记忆；隐瞒违规本身是更严重的违规。
0.5 例外不外溢：某条的「不适用」只豁免该条，不豁免其它条。

%%PREAMBLE%%【%%COUNT%% 条工作纪律 · 编号｜触发 → 动作 → 命中后必须输出的声明】
%%ROUTING_TABLE%%

【使用须知】
- 「触发」命中即执行对应「动作」，并在回复中输出该条「声明」原文。
- 逐条的完整「不适用」边界见真源 JSON 对应节点的 negative.reject；此处仅列关键项。
- 本段为自动生成（真源指纹 SHA256 前16位：%%SOURCE_SHA%%），请勿手改；改真源后重跑 scripts/render_discipline.py。

# 并发必然性理论：收敛带宽刚性定理 v0.2

> **版本**：v0.2 ｜ 2026-09-15 ｜ 状态：条件论地基重构稿（蜂巢并发验证未执行，见 §7）
> **体系定位**：三份核心文档之**理论层**——《并发必然性理论》（本文）→《认知代理与收敛结构》（架构层）→《子代理配置标准》（工程层）。三份体系由使用者裁定收敛（2026-09-15）；其余理论文档（单线程与注意力 v1.0、GridWorld 规格、概念分层对齐表、蜂群同错检测协议）为补充资料。
> **v0.2 变更**：依据使用者裁定，**地基从神经科学事实（v0.1 的 F1/F2）迁移到条件论与信息差**（真源=《白箱智能是什么？》系列四篇，下文以 ①②③④ 加节号引用）。定理 T 的形式化与工程面保留并重述；推论 T2 按「任务分解前提删除」修正；神经科学内容降为附录 A，不承担论证重量。
> **架构层承接**：公设的完整展开、认知代理定义、收敛结构见《认知代理与收敛结构_条件论重构_v0.1.md》（下称「架构层文档」）；本文不重复其内容，只立理论判据。

---

## 0 核心主张（一页版）

**定理 T（收敛带宽刚性，条件论重述）**：认知系统的扩展只能发生在展开侧。单收敛位的调用带宽存在刚性上界（④快照三问之三：未被调用的信息等于这次不存在），而展开侧（条件投影）近乎无限可扩；当任务的信息流宽度超过单收敛位的调用带宽时，把整任务压入单一收敛位会使信息差按 R>C 推论扩大（④二），收敛粒度下沉是把**容量瓶颈转换为协调成本**的结构响应。

- **推论 T1（串行=收敛粒度选择）**：串行不是执行的本性，是收敛粒度的选择——把整个任务当作单个收敛事件（E-单线程）。
- **推论 T2（投影收敛开放，修正 v0.1 的「并发默认」）**：任何认知任务默认允许多条件投影独立收敛（公设 P2——「可分」不依赖「可分解」）；**串行是需要给出理由的例外**。但允许 ≠ 必优：投影间盲区同构时多代理退化为重复计算（架构层文档 CA-T2'），串行正当场景见 T3。
- **推论 T3（串行正当性条件）**：仅当子任务链存在严格数据依赖（后者需要前者的产出才能开始）时，该链内串行是唯一形态；完整正当场景四分类（基座/约束/成本/耦合串行）见架构层文档 §5.1（承接补充资料《单线程与注意力集中 v1.0》§3.2）。
- **推论 T4（并发不豁免收敛）**：并发扩的是展开侧；每路产出仍需收敛位裁决，裁决责任不随 worker 数量分散（工程投影：蜂巢「agent 本体只做编排」）。

**一句话**：收敛带宽刚性决定了扩展方向只能朝展开侧；但「展开侧」的单位是**条件投影**，不是「任务切块」——v0.1 的「可分解任务」前提在条件论下被删除。

---

## 1 证据基座（条件论三公设）

| # | 公设 | 内容 | 真源锚点 |
|---|---|---|---|
| P1 | 任务原子性 | 认知任务本身原子；非原子展开发生在实现层，不构成任务分解 | 架构层文档公设 P1（建模公设） |
| P2 | 条件投影可分性 | 同一任务可在多个相互区分的条件空间上建立独立投影（D=D(C)：不同投影携带不同信息可得性） | ①一、③二；架构层公设 P2 |
| P3 | 收敛带宽有限性 | 单次认知的收敛带宽受调用带宽约束；多投影提高有效观测容量，不消除信息差 | ④三之「调用带宽」、④二 R>C；架构层公设 P3 |

**关键设计**：定理 T 只依赖 P2+P3（P1 用于封「子代理=任务切块」误读）。本文不需要：神经科学事实、任何意识理论、注意力机制假设——后者全部降为附录 A 启发。

---

## 2 单线程的白箱精确化（E-义）

「单线程」三义（S-主观报告 / E-收敛形态 / C-工程约束）的完整展开与防滑移边界见补充资料《单线程与注意力集中 v1.0》§1；本文只在 **E-义**上刻画：

- **E-单线程** = 收敛粒度设为「整任务」的理想型——全部展开隐藏在收敛位视角外，收敛只在根节点发生一次；
- **多收敛** = 收敛粒度下沉到投影层——各条件投影各自收敛，产出到达收敛位时已是裁决单位（带四态资格）。

收敛粒度（G_c）与收敛带宽（B_c）是两个独立参数：粒度改变收敛事件的**位置**，带宽约束收敛事件的**容量**；互推须显式给出资源账本。

**2.4 工程含义（条件式，替代 v0.1 的全称表述）**：当任务信息流宽度超过单收敛位调用带宽时，整任务串行黑盒=把宽展开面压入窄调用口——此时 agent 不默认承担整任务串行义务（CA-T3 判据见 §4）。前提不满足时，串行由该前提正当化，是权衡结果而非倒置。

---

## 3 理论 → 灵枢实现对照表

（v0.1 的「人脑↔灵枢」两列同构表随地基迁移降级至附录 A；本表只保留纯工程面对应，不依赖任何神经科学内容。）

| 理论概念 | 灵枢实现 | 对应关系 |
|---|---|---|
| 条件投影多路展开 | 四路并行召回（lexical/bucket/entity/graph RRF 融合） | 结构对应 |
| 资格由条件证据裁决，不由相似度裁决 | judge 资格裁决 / 条件路由收敛 | 结构对应 |
| 展开侧扩容（\|E\| 增大） | 蜂巢 worker 池（spec/result 文件协议） | 结构对应 |
| 收敛位唯一、责任不分散（T4） | agent 编排位 / 认知图写入广播 | 结构对应 |
| 预测误差→条件更新 | predict 误差驱动学习闭环 | 机制对应 |

---

## 4 定理 T 的形式化

设系统由两部分组成：
- **展开集 E**：可复制、可扩容的收敛单位集合（条件投影代理；蜂巢 worker）；
- **收敛位 C**：对多路产出做更高阶收敛（裁决/融合）的单元，其单次可调用信息量受调用带宽约束 B_c（④快照三问之三）。

**命题 1（收敛瓶颈）**：若所有收敛动作都在根节点串行发生，总墙钟时间 ≥ 全部子任务数 × 单次收敛耗时；且当信息流宽度 W > B_c 时，单收敛位无法取用全部展开面，信息差按 R>C 扩大（④二）——瓶颈不仅在时间，更在信息。

**命题 2（粒度下沉）**：若各条件投影相互独立，收敛动作可下沉至各投影并行发生，根节点只做一次更高阶收敛。总墙钟时间从 Σ（各路收敛时间）降为 max（各投影收敛时间）+ 一次融合成本。

**定理 T**：当任务的信息流宽度超过单收敛位调用带宽（W > B_c）时，把收敛粒度从根下沉到投影层，在信息面上是结构响应而非偏好；且展开侧扩容（|E| 增大）可继续压缩 max 项，而收敛侧扩容不存在——调用带宽是单点观测的结构属性（④快照三问：任何单点观测都是窄的），不可靠复制消除。

**证明要点**：命题 1 由 P3（调用带宽刚性）给出信息面约束、由串行聚合给出时间面约束；命题 2 由投影独立性（P2 的建立条件）给出；合取即得。∎

**适用域**：W ≤ B_c 或存在 T3 串行契约时，本文不作下沉判断——粒度选择是逐任务的成本-收益权衡（架构层文档 CA4；补充资料单线程 v1.0 §3.3）。

---

## 5 工程落地（纪律第 17 条的理论位置）

| 纪律动作 | 理论对应 |
|---|---|
| 任务→拆 spec | 收敛粒度下沉（命题 2）；**200k 上下文预算=拆解信号**（W > B_c 的工程判据，见《子代理配置标准 v0.5》§6） |
| spec 独立性检查 | 条件投影实质区分判定（架构层 CA-T1：四维任一不同且影响信息可得性） |
| `hive_spawn` 并发提交 | 展开侧扩容（\|E\| 增大） |
| agent 收 result 汇总裁决 | 收敛位唯一、责任不分散（T4） |
| 宿主自带 subagent 不是等价通道 | 展开动作必须经统一文件协议留痕（spec/status/result），否则收敛位失去裁决材料 |

---

## 6 边界声明（防外溢）

- **CA1 体系**：本文边界编号沿用架构层文档 CA1-CA6（架构/功能层陈述、建模公设不越界、认知结构并行、非排他、带宽=调用带宽表述、元条款），不另立体系。
- **B4 并发不豁免收敛**（=T4）：多路产出使收敛位的裁决责任更重，裁决材料必须结构化（文件协议、留痕）。
- **B6 数字诚实**：本文不使用任何数值锚点（4±1 等已随神经科学地基降级）；「W > B_c」是关系判据，W 与 B_c 均不承诺数值测量——工程代理量见《子代理配置标准》§6。
- **诚实边界**：本文定理是架构层结构判据，不构成「并发总优于串行」的规范断言（CA4）。

---

## 7 验证状态（诚实标注）

| 项 | 状态 |
|---|---|
| v0.1 计划的「蜂巢 4 视角并发反思验证」（逻辑攻击/科学锚点/边界攻击/自洽性） | **未执行**（v0.1 §7 为待回填空节） |
| v0.2 地基替换后的重新验证 | **待执行**：按架构层文档 §7 实验纲领（七测度）设计；测度 2 承接 GridWorld 规格、测度 4 承接蜂群同错检测协议 |
| 相关审查留痕 | 《单线程与注意力集中 v1.0》五单元审查（反思×2+验证×3，三 REJECT→重写）结论以其 §6 留痕表为准（job id 可回放）；审查中间件产物（单元原始产出文件）已按《子代理配置标准》§5.4 产出物生命周期在系统整合后清理 |

---

## 8 关联文档与节点

**三份核心体系**：
- 架构层：《认知代理与收敛结构_条件论重构_v0.1.md》（公设展开/代理定义/收敛结构/实验纲领）
- 工程层：《子代理配置标准_v0.5.md》（六位角色拓扑/统一执行默认/确定性执行/spec 配置/上下文预算/工具面/理论对应节）

**补充资料**（参考源，不再承担正式理论职责）：
- 《单线程与注意力集中_无争议理论文档_v1.0.md》（S/E/C 三义、三类串行、串行正当场景、争议封口方法）
- 《GridWorld最小闭环规格_v0.1.md》（实验设施：闭环机制验证）
- 《概念分层对齐表_v0.1.md》（五层置信概念：融合位裁决纪律的概念映射）
- 《蜂群同错检测实验协议_v0.1.md》（实验协议：共识≠真相验证）

**真源**：白箱智能四篇（①②③④）；认知图节点 `mem_1789454083769`（树状递归/条件裁决/误差驱动/调用带宽）、`mem_1789374897301`（hive 落地与文件协议）、`mem_1789454597670`（收敛带宽定理首提）。

---

## 附录 A 神经科学启发（v0.1 证据基座的降级存档）

> **状态**：依使用者裁定（2026-09-15）降为启发引用，不承担本文任何论证重量；保留于此仅为溯源。

- **F1 大规模并行**：人脑约 860 亿神经元持续并行活动；感觉通道各自独立并行处理（Herculano-Houzel & Azevedo 等，2009）。
- **F2 收敛带宽有限**：工作记忆项目数约 4±1（Cowan, 2001）；多对象视觉追踪上限约 4-5（Pylyshyn & Storm, 1988）。注意：工作记忆组块容量与多对象追踪容量不是同一构念（单线程 v1.0 B7）。
- **F3 工程同构**（保留为工程事实，非神经科学）：一个会话绑定一个收敛主体；执行位（worker）可任意扩容（灵枢蜂巢 `HIVE_WORKERS` 可调）。
- **解释性对照**（仅启发）：全局工作空间理论、预测加工框架——v0.1 §3 同构表的「人脑」列随地基迁移移除，对照关系见该表存档（git 历史 v0.1）。

# 缺陷挖掘报告 · 自主迭代 v8（安全专项）

- **日期**：2026-09-25
- **范围**（生效条件）：第 8 轮 = 安全专项，四区复核（legacy / auth / inject / fs）确认 **22 条目**（四区 rejected 数组均为空，整项证伪 0；另 2 处证据形态修正见第三节末）→ 去重后独立缺陷 **20**（exec.py:103 与 op=ref 各有两区双报）→ 本轮修复 **5 个补丁覆盖 6 个独立缺陷**（high 4 / medium 2，其中 ref 缺陷两区分别评级 medium/high，取高者处置）→ 遗留 **14**（medium 10 / low 4）
- **legacy 重开依据**（重要）：本轮 8 项 legacy 均非重复报——其中 **v2 N5/N11（verify_runner iter_id 穿越）与 v2 N10（快照 HEAD 残留）经 git 取证证实批次 37 提交（03ae053）信息声称已修但未落盘**（`git show 03ae053 -- hive/verify_runner.py` 无任何 diff；`-- hive/wm.py` 全部改动仅 +4 行 job_id 校验），现行代码确无对应防御，属「声称已修未落盘」的重开，非对已落盘项的重复报告
- **回归**：`python scripts/run_tests.py --jobs 4` 全量 **174/174 通过、5 跳过（依赖缺失/平台不符）**（材料口径退出码 0；**本报告会话独立实跑同命令，SUMMARY 行与材料逐字一致**；174 = v7 基线 172 + 本轮两个新守卫套件）；`cargo test` **未运行**（Rust 面零改动）；`npm test` **未运行**（TS 面零改动）；`npm run gate` **FAIL——R4 非追踪件面命中新守卫文件 `md_cg/test_p1x_ref_root.py`（非追踪=25、允许=24）**，本报告会话以第 2 环 `check_publish_artifact.py` 单独复现同因同置（R1/R2/R3 全 PASS）——时序性失败（新守卫尚未 `git add` 登记），处置归编排会话提交前裁决，同 v7 先例
- **行号口径**：缺陷行号为缺陷时点快照行号；本轮修复叠加致三文件行号漂移，涉及处本会话另注现行行号（hive/wm.py:236 → 现行 :253；hive/exec.py:1517 → 现行 :1541；compiler/lexer.py:393 → 现行 :393-394 不变、新增段 :396-410）；未修项所在文件本轮零改动或已逐一读核现行行号（见第五节）
- **编号口径**：legacy 项沿用原轮次号（v2 N5/N11、v2 N6、v2 N7、v2 N10、v2 N16、v3 N6、v5 N24、v6 N38）；本轮新发现 12 项独立缺陷沿历轮 N 口径续编 **N60-N71**（v7 用至 N59）
- **结构口径**（CCG 六要素）：功能名（标题）/ 生效条件（范围与口径行）/ 子功能（第二~五节）/ 执行与验证方式（第四节：实跑命令与结果）/ 不适用条件（第七节：跳过与未运行项）

---

## 一、执行摘要

本轮为安全专项：四区复核 22 条候选全部确认、0 项整项证伪（2 处证据形态修正单独列于第三节末，不动摇结论）。修复 5 个补丁（覆盖 6 个独立缺陷）、留档 14 项。三个主题：

1. **「声称已修未落盘」的 git 取证重开（legacy 区，3 修）**：v2 N5/N11（verify_runner iter_id 穿越，越界读 frozen.json + 仓库外 makedirs 写 verdict.json）与 v2 N10（快照失败 HEAD 残留 task 分支 → 旧产物绕三级闸进 main）在批次 37 提交信息中声称已修但 `git show` 证无 diff——本轮**实际落盘**（F3/F4）。同区 v3 N6（无空格「条件空间为伴侣」绕过情感权重上限）为第 3 轮遗留，本轮修复（F1）。
2. **P1-4 根白名单契约在读回面失效（auth+inject 双区独立命中，1 补丁修 2 缺陷）**：ingest/export/link/mdcg_ingest 四入口的 `check_path_root` fail-closed 闸在位，而 **op=ref（自报 root / inline ref / 绝对 path / `..` 上跳三形态全文回读白名单外文件）与 index_code/index_doc（白名单外目录直接进图、docstring/前置注释/md 摘要落 internal 密级节点对 guest 持久可读）三入口零校验**——auth 区（F1/F2，medium）与 inject 区（越权任意读原语，high）两份独立报告同一根因，修复时合并为一个补丁三处挂闸（F2，N60+N61）。
3. **诚实失败链断裂（legacy+fs 双报，1 修）**：v2 N6（exec.py write_result 裸 `open("w")` 非原子——强杀/崩溃留半截文件且先毁旧完整结果，成功任务被读者判 error 假失败）在 legacy 区与 fs 区三度命中、姊妹执行器 exec_cmd.py 的 tmp+fsync+os.replace 模板在位，本轮照抄补齐并加 Windows 读者面适配（F5）。

**对输入口径的核验**：输入称「确认 22 条目 → 修复 5 补丁 6 缺陷 → 留档 14」。本会话以 `git status`/`git diff --stat` + 逐处读核核对：改动面恰为——compiler/lexer.py +15、md_cg/mcp_server.py +20、hive/verify_runner.py +21/-3、hive/wm.py +37/-10、hive/exec.py +28/-2（五个修复补丁）+ 守卫四处（md_cg/test_p1x_ref_root.py +27 新增、md_cg/test_p39_verify_flow.py +27、hive/test_wm_progress.py +23、hive/test_exec_tools.py +47、compiler/tests/test_lexer_v3n6.py 新增）+ cogmap 锚点同步两文件（README.md、docs/mdcg/功能调用映射表_v0.1.md）+ scripts/verify_linux.sh 注册新守卫（40→41 套）——与五项修复一一对应；20 − 6 = 14 留档，无多无少。工作区另有 README.md / docs/mdcg 功能调用映射表 / code_solidified.json 的先前会话改动（cogmap build 只做标记段内替换、段外保留），未与本轮修复混淆。

---

## 二、修复清单（5 项，逐项 where / 攻击面 / 修复 / 攻击前后对比 / 守卫）

### F1（v3 N6，high）lexer 定向断开：「条件空间为X」无空格写法解锁名实校验

- **where**：缺陷时点 `compiler/lexer.py:393-394`（CJK 收集分支断开前瞻集合仅「大于/等于/小于/于」，:390-392 注释明示「为」不在集合）+ `compiler/name_checker.py:836`（`_check_condition_space_switch` 要求 condition.type==COMPARISON 且 left=='条件空间' 才设 current_condition_space，:812-819 情感权重上限检查依赖该状态）；修复落点 `compiler/lexer.py:396-410`（新增定向断开，本会话读核 :396-410 注释与断开逻辑在位、:393-394 既有集合不变）
- **攻击面**：任何协议源码作者——无空格写法使「条件空间为伴侣」整段吞作单 IDENTIFIER，parser 不生成 COMPARISON，条件空间切换检测永不触发，「伴侣」空间情感权重上限 0.15 对无空格源码整体失效（超限值通过编译进入条件空间）。批次 37 F1 只修了「于」断开（数值操作数分离），「为」漏在集合外。
- **修复**（最小 +15 行单处）：_segment 的 CJK 连续段收集中，在既有断开集合之后追加**定向断开**——仅当 `text[i-4:i]=="条件空间"` 且当前位置是「为」/「不为」起点时 break。断点落在关键字（WEI/BUWEI，均在 SORTED_KW/KEYWORD_MAP）起点，主循环最长匹配随后正常发出 WEI/BUWEI，使无空格与带空格写法产生相同 token 流 → parser 生成 COMPARISON → name_checker 按既有逻辑设置条件空间 → 0.15 上限对无空格源码生效。**未把「为」全局加入断开集合**（避免切碎「作为/成为/行为/认为/人为/信任为基」等合法名词），name_checker 侧零改动。
- **攻击前后对比**（临时守卫实跑，`python -X utf8` 调 compiler api）：修复前『若条件空间为伴侣，则止情感权重于0.9。』→ `success=True`、errors=[]、warnings=[]，token 流为单 `IDENTIFIER("条件空间为伴侣")`；修复后 → `success=False`、errors=['L1:C17 在「伴侣」条件空间中情感权重不可超过 0.15，当前值: 0.9']，token 流 → ID(条件空间)+WEI(为)+ID(伴侣)；『不为』无空格形态同拒（L1:C18）；合法值 0.15 无空格版不误报；「作为/成为/行为/认为/人为/信任为基」仍为单 IDENTIFIER、「无为」仍为单 WUWEI（断开不外溢）。
- **守卫**：临时攻击复现守卫（系统临时区，14 项，跑完已删）：修复前 6 项红（无空格 errors 非空/点名 0.15/不为形态/token 流三查），基线 6 项绿；修复后 14/14。**永久守卫 `compiler/tests/test_lexer_v3n6.py`**（15 项：攻击拒绝/不为形态/前缀名词无分叉/带空格不回归/合法值不误伤/token 流形态/误伤面钉死）——**本报告会话实跑：15/15 通过**。
- **范围注（如实）**：「我的条件空间为伴侣」等前缀名词形态无空格/带空格行为一致（均不切换条件空间）——name_checker 只认精确名「条件空间」为既有语义，非本缺陷回归面，按最小改动不扩大。

### F2（N60 medium/high 双评级 + N61 medium）op=ref / index_code / index_doc 三入口挂根白名单

- **where**：缺陷时点 `md_cg/mcp_server.py:2887`（_ref_call 直接调 `refindex.read_ref`，ref dict 与 root 参数透传）→ 实际读文件 `md_cg/refindex.py:439-466`（probe_ref `os.path.join(root,path)` 后 open 全文回读；绝对 path 时 join 丢弃 base、`..` 上跳同漏）+ `md_cg/mcp_server.py:2275`（index_code `root=a.get("path")`）+ `:2307`（index_doc 同型）；修复落点现行 `md_cg/mcp_server.py:2902-2906`（ref read/get 分支）、`:2280`（index_code）、`:2316`（index_doc）——三处挂闸本会话逐一读核在位（含 :2896 「两份独立报告合并」注释），refindex.py 零改动
- **攻击面**：auth 区口径——持 designer 令牌的 MCP 进程被提示注入（与 P1-4 自身威胁模型同款）或同机多会话宿主，`cg(op=ref, ref={path,root})` 全文读出白名单外哑样例凭据文件；record 令牌与 guest 身份均被 require_op 拒（本轮实测），边界为 designer 面。inject 区口径——任何含 ref 的 op 令牌（designer `ops_allow=["*"]`；签发者可经 `issue --ops-allow` 授自定义只读面）：三形态（自报 root / ref.path 绝对路径 / `..` 上跳）全文回读，对照组证明 op 闸本身工作、缺口在 ref 按只读面归类且路径零校验；index_code/index_doc 把白名单外目录（含哑样例密钥文件的 vault）直接进图，泄漏通道实测为**模块 docstring、函数/类前置注释、md 章节摘要首段**三条（普通 `#` 注释实测不进图，见第三节修正），进图节点 sensitivity=internal，无令牌 guest 经 op=read 召回命中泄漏串。
- **修复**（最小 +20 行单文件三处挂闸，复用既有 `security.check_path_root` 的 P1-4 fail-closed 语义）：① _ref_call read/get 分支按**最终将打开的完整路径**挂闸——`check_path_root(os.path.join(a.get("root") or ref.get("root") or "", ref.get("path") or ""), "MDCG_INGEST_ROOT", "ref")`，自报 root、inline ref 自带 root/path、node 登记 ref 三来源统一 realpath 落根判定（绝对 path 丢弃 join base 与 `..` 上跳在 realpath 归一后自然被拒）；② index_code 分支对模型可控的显式 `a.get("path")` 挂闸（未传回落服务器侧 cg.root 不校验，与 ingest 先例同语义）；③ index_doc 同②。**env 未设置 = 放开（部署开关，默认行为不变）；设置了 = 越界抛 PermissionError**，经 stdio 层 tools/call 既有异常处置（mcp_server.py:3690-3699）转 isError 响应。附带：cogmap 锚点同步（`scripts/cogmap_sync.py build` 重写 README COGMAP 段 + 功能调用映射表 3 段——插入代码致行号漂移，build 只做标记段内替换，段外用户文案改动保留）；新守卫注册进 `scripts/verify_linux.sh`（本会话读核 diff：清单 40→41 套）。
- **攻击前后对比**（临时哑数据 + MDCG_INGEST_ROOT 设白名单）：修复前——①自报 root 指白名单外 victim → `ok=True` 哑样例密钥全文漏出；②ref.path 绝对路径 → `ok=True` 全文漏出；③`..` 上跳 → `ok=True` 漏出；④index_code path=白名单外 → `ok=True` 越界索引；⑤index_doc 同。修复后——五形态全部 `PermissionError`（「ref/index_code/index_doc 路径超出 MDCG_INGEST_ROOT 白名单，拒绝访问」），哑样例不再出现在任何返回；白名单内 index_code+node 回读 `ok=True`（indexed=2）、白名单内 inline ref 回读 `ok=True` 不误伤；env 未设时 ref 保持既有放开语义；ingest 先例闸与 guest require_op 均不回归。
- **守卫**：临时攻击复现守卫（系统临时区，10 项，跑完已删）：修复前 5 项红（三形态+两索引越界全放行），基线 5 项绿；修复后 10/10。**永久守卫 `md_cg/test_p1x_ref_root.py`**（10 项：①a-c 三形态拒 / ②a-b 索引越界拒 / ③a-b 白名单内不误伤 / ④a ingest 先例不回归 / ④b env 未设行为不变 / ④c guest op 闸）——**本报告会话实跑：10/10 通过**；`git stash` 仅回退 mcp_server.py 对照，旧代码下该守卫 5/10（五项攻击全红）——红守卫确定性达成。
- **范围声明（如实）**：本轮只覆盖 _ref_call 的 read/get 分支与 index_code/index_doc 显式 path；ref action=check/prune 巡检分支读图内登记 ref（非 inline 模型可控）未挂闸，未在本任务扩大。

### F3（v2 N5/N11，high）verify_runner iter_id 白名单：CLI 直跑穿越读写

- **where**：缺陷时点 `hive/verify_runner.py:90`（`iter_id = argv[1]` 完全未校验）、`:103`（frozen_fp 裸拼——越界读）、`:161-162`（out_fp 裸拼 + os.makedirs——越界写）；修复落点 `hive/verify_runner.py:97-105`（本会话读核 :102-106 白名单与 rc=2 分支在位、:80-93 docstring 生效条件同步）
- **攻击面**：verifier 实例（半信任对等体）CLI 直跑 verify_runner.py——argv[1] 传 `..\` 穿越形态、自设 HIVE_ROLE=verifier 即过角色守卫（守卫只查环境变量值）；姊妹入口 write_verdict_to_repo（md_cg/interop.py:316 `re.fullmatch(r"[A-Za-z0-9_.-]{1,80}")`）有白名单而本入口没有，防御不一致。**git 取证**：`git log -- hive/verify_runner.py` 最后改动 d2dc6bd；`git show 03ae053 -- hive/verify_runner.py` 无任何 diff——批次 37 提交信息声称已修「verify_runner iter_id 穿越」但未落盘。
- **修复**：main() 取 argv[1] 后、任何路径拼接之前新增同模板白名单 `re.fullmatch(r"[A-Za-z0-9_.-]{1,80}", iter_id)`，不过即打印 `{"ok":false,"error":"iter_id 非法（入库路径组成部分）…"}` 诚实留痕并 rc=2 退出（**不触盘、不读 frozen、不 makedirs**，fail-closed 于路径拼接之前）。
- **攻击前后对比**（同盘仓库外临时目录，测毕清理）：修复前——iter_id=穿越形态 + HIVE_ROLE=verifier + `--smoke` → rc=0，stdout path 字段指向仓库外 verdict.json，盘面确认仓库外 frozen.json 被读入（越界读实锤）、verdict.json 被写入（越界写实锤）；修复后——同一形态复跑 → rc=2、输出含「iter_id 非法」、仓库外无任何文件创建（盘面零触达）。
- **守卫**：`md_cg/test_p39_verify_flow.py` 新增确定性红守卫 **2e**（穿越形态 + verifier 身份 → 断言 rc=2、输出含「iter_id 非法」、仓库外目标路径 os.path.exists 为 False 即不触盘）与 **2f**（绝对路径注入形态同断言），本会话读核 :110-121 在位；原有守卫（2d 角色守卫 rc=3、3c 姊妹入口注入拒绝）语义不受影响——**本报告会话实跑整套：11 passed, 0 failed**。

### F4（v2 N10，high）wm 快照提交链 try/finally：失败不再残留 HEAD 于 task 分支

- **where**：缺陷时点 `hive/wm.py:166-173`（checkout -B → add → commit → rev-parse → checkout main 之间无 try/finally，任一步 _git_ok 抛 WmError 即把 HEAD 永久留在 task/<job_id>）；修复落点 `hive/wm.py:167-192`（本会话读核 :174-181 try 四步 + :182-190 finally 无条件 checkout main 在位，:167-172 生效条件注释同步）
- **攻击面**：result.json 自报 job_id 的产物数据不可信 + 快照重试/重复执行确定性触发——典型：重复快照已合并任务 → commit「nothing to commit」退出码 1 → WmError 后 HEAD 残留 task 分支 → 三级闸 merge 被卡死（「merge 须在 main 上执行」）或更糟：后续新任务分支以残留分支为祖先，合并新任务把旧任务产物静默带进 main，绕过旧任务自己的三级闸合并审查。**git 取证**：`git show 03ae053 -- hive/wm.py` 全部改动为 +4 行 job_id 路径段校验——「快照 HEAD 残留」声称修复未落盘。
- **修复**：提交链四步包入 try，新增 **finally 无条件 checkout main**；还原失败不吞——try 内有正传播异常时抛消息合并（`from` 原异常）的 WmError、无则直接抛，均含「HEAD 可能残留于 <branch>」提示（留痕）。诚实失败语义不变：nothing-to-commit 仍如实抛 WmError。
- **攻击前后对比**（系统临时区临时 wm 仓；修复前对照经 `git show HEAD:hive/wm.py` 提取临时副本加载，不动工作区，测毕清理）：修复前——snap1 ok → merge ok → snap2 同 job 重复快照抛 WmError → 终态 HEAD=task/<job> ≠ main（残留实锤）→ cmd_merge 被残留卡死 → snap3 新任务成功且以残留分支为祖先（旧产物绕闸进 main 路径成立）；修复后——snap2 仍如实抛同一 WmError 但 HEAD=main（不残留）、cmd_merge ok=True（三级闸不再卡死）、snap3 正常且新任务分支从 main 主线创建（含已合并提交）。
- **守卫**：`hive/test_wm_progress.py` 新增 **[G] 段**五项（本会话读核 :249-269 在位）——G1 重复快照如实抛 WmError；G2 失败后 HEAD==main（红守卫判定点）；G3 失败后三级闸不被卡死；G4 失败后新任务快照正常；G5 新任务分支从 main 主线创建——**本报告会话实跑整套：PASS=49 FAIL=0**（原 44 项无回归）。

### F5（v2 N6，medium）exec write_result 原子写：强杀/崩溃不再留半截 result.json

- **where**：缺陷时点 `hive/exec.py:103-105`（`with open(...,"w"): json.dump` 裸写，全文件 grep tempfile/fsync/os.replace 零命中）；修复落点 `hive/exec.py:102-131`（本会话读核 :116-129 tmp+fsync+replace 与 WinError 5 重试环在位、:102 生效条件注释同步）；exec.py 内六处调用（:1447-1560 缺陷时点口径）+ `hive/orch.py:570/588` 复用同一函数，一处修多处受益
- **攻击面**：rust serve 超时/kill 强杀（`hive/src/scheduler.rs:430-441` kill_requested 与 timeout 两分支均 kill_tree 强杀 worker）落在写入窗口内 → 半截文件且**先毁旧完整结果**（重投第二次执行同路径）；读者面——`wm.py` _read_json 不捕获解析异常且 main 只捕获 WmError → 半截 result.json 使 wm snapshot 裸 traceback 崩溃；rust 轮询、hive_mcp._result_view、orch._card 同样暴露（成功任务被读成 error 假失败）。姊妹执行器 `exec_cmd.py:39-48` `_write_result` 已用 tmp+flush+fsync+os.replace 且注释明示原子语义——两套口径。
- **修复**：照抄姊妹模板 + Windows 适配——修复过程实撞发现：纯 os.replace 在 Windows Python 读者（open 无 FILE_SHARE_DELETE）高频打开下抛 PermissionError WinError 5（exec_cmd 无此问题因其读者是 rust），故 replace 撞瞬态句柄时 10ms×递增重试至多 50 次（约 12.5s），耗尽才抛（此时旧完整结果仍在位，fail-safe）。
- **攻击前后对比**（系统临时区，测毕清理）：①并发撕裂压测（1 写者 3MB×30 + 4 并发读者高频 json.load）——修复前 torn=125 + empty=7176（半截解码失败 + 旧结果被截断成空文件的窗口），对照组 exec_cmd 同场景 0 撕裂；修复后 **torn=0 empty=0**，线程形态最严苛压测（2 读者线程 + 1MB×20）33551 次读零异常；②强杀截断旧结果（确定性）——先落完整旧结果 → 写 100MB 中途 terminate 模拟 kill_tree → 修复前 result.json=0 字节、旧完整结果被毁；修复后旧结果完整存活、无 tmp 残留；③读者面崩溃链（半截 result.json → wm snapshot 裸 Traceback）的**源头已断**——wm 对「手工半截文件」的裸 Traceback 属读者面既有行为，不在本修复 where 范围，如实说明未改（见遗留建议）。
- **守卫**：`hive/test_exec_tools.py` 新增 **[K] 段**（本会话读核 :655-692 在位）——K1 result.json 写后完整可解析且无 .tmp 残留；K2 双读者线程高频读 + 主线程 1MB×20 连写压测 → torn==0 且 empty==0（旧码此压测必现撕裂，线程形态实测灵敏度为 torn=22+empty=11）——**本报告会话实跑整套：87 通过 / 0 失败**（原 85 项无回归）。

---

## 三、发现明细（四区确认 22 条目 / 整项证伪 0 / 证据形态修正 2 处）

四区复核 rejected 数组均为空——**零整项证伪**，如实记录。以下每项「证据」为上游复核会话的读码+实跑（材料原文，攻击复现均在系统临时区/哑令牌/哑数据、测毕清理，环境细节按仓库口径转录）；标注★者本报告会话另做了现行代码读核。

### legacy 区（8 条目，4 项已修、4 项留档）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| v2 N5/N11★ | hive/verify_runner.py:90 | high | iter_id 取自 argv[1] 完全未校验即裸拼读/写路径：`..\` 穿越可从仓库外读 frozen.json、仓库外 makedirs+写 verdict.json；姊妹入口有白名单本入口没有 | 本轮实跑复现（同盘临时目录测毕清理）：iter_id=穿越形态 + HIVE_ROLE=verifier + --smoke → rc=0、stdout path 字段=仓库外 verdict.json、盘面确认越界写+越界读；git 取证 03ae053 声称已修无 diff | **已修**（F3） |
| v3 N6★ | compiler/lexer.py:393 + name_checker.py:836 | high | 「伴侣」条件空间情感权重上限 0.15 对无空格中文写法完全失效：『若条件空间为伴侣，则止情感权重于0.9。』编译成功零错误零警告 | 本轮实跑：无空格版 success=True/errors=[]/warnings=[]；带空格版 success=False 点名 0.15 上限——守卫在位但只对分词形态生效；断开集合仅四词、_check_condition_space_switch 恒不触发 | **已修**（F1） |
| v2 N10★ | hive/wm.py:166 | high | cmd_snapshot 无 try/finally 还原 HEAD：checkout -B 后任一 git 步骤失败即把 HEAD 永久留在 task/<job_id>（典型：重复快照 nothing to commit）→ 三级闸卡死或残留分支为祖先致旧产物绕闸进 main | 本轮实跑复现（临时 wm 仓测毕清理）：snap2 抛 WmError 后终态 HEAD=task 分支≠main、merge 被卡死、snap3 新任务以残留分支为基；git 取证 03ae053 只落了 +4 行 job_id 校验、「HEAD 残留」未落盘 | **已修**（F4） |
| v2 N6★ | hive/exec.py:103 | medium | write_result 直接 open("w") 覆盖写 result.json 非原子：并发读者（rust poll/status、wm 快照拷贝）可读到截断 JSON 被判 error 终态；exec_cmd.py:39-48 同契约已原子写，exec.py 漏修 | 本轮实跑（4 秒并发对撞）：424 次坏读（JSONDecodeError 截断形态样例）；全文件 grep tempfile/fsync/os.replace 零命中 | **已修**（F5；与 fs 区第 1 条为同一缺陷双报） |
| v2 N16 | hive/hive_mcp/mcp_server.py:566 | medium | _rpc 入口 req.get("method") 对非 dict JSON 形态抛 AttributeError 且早于只包工具调用的 try；main 循环只护 json.loads 的 ValueError——JSON-RPC 2.0 规范允许的批量请求（数组）或任意 str/int 行使整个 MCP server 进程崩溃退出，一行即 DoS | 本轮实跑复现（临时子进程逐 payload 喂 stdin）：dict 形态 rc=0；数组形态 rc=1 + AttributeError 'list' object；str/int 同崩——崩溃仅由非 dict 形态触发 | 留档（本会话读核 :565-566 现状未变：无形态守卫） |
| v6 N38 | src/lib/token_store.ts:76 | medium | auxRoot() 用 resolve(env)：不做波浪线家目录展开且锚定宿主 cwd，与文件头声称「同口径」的 md_cg/datapath.py aux_root()（expanduser+abspath）不一致——MDCG_AUX_ROOT 设波浪线/相对形态时密钥环与令牌记录分居两处，触发重复自动签发永不吊销的 designer 令牌 | 隔离探针（node 侧仅计算路径不读写真实令牌）：波浪线形态 node 返回字面波浪线目录（锚 cwd）、python 返回真家目录展开——分居实锤；相对形态 node 结果随 chdir 变化；token_store.ts 全文 grep revoke 仅用户提示文案 | 留档（本会话读核 :76-79 resolve(env) 现状未变） |
| v5 N24 | swarm/swarm_cli.py:78 | medium | cmd_run 调 make_swarm_config 未传 condition_space：用户在 swarm.json 声明的 G-R2 条件空间卡被 CLI 静默丢弃——不进生成配置、不触发 Rust 侧负路由校验、执行链 VM 符号注入失效 | 实跑探针（monkeypatch 截获 cfg 不跑真 swarm）：截获 cfg keys 无 condition_space 键；rust_swarm.py:31/:46-47 库层支持完整；绿套件直连 make_swarm_config 绕过 CLI 故全绿而缺口在位 | 留档（本会话读核 :78-81 实参表现状未变） |
| v2 N7 | hive/exec.py:1517（现行约 :1541，F5 修复上方 +28 行漂移） | medium | call_llm 单发路径（无 tools）缺 _empty_turn 防护：call_llm 对空/缺 choices 回 content=""，无条件 ok=True 落盘 EXIT_OK——LLM 服务故障回空包时任务被记成功，空内容 result.json 流入下游合并/验证链（假成功）；工具路径 :1348 有完整防护，两分支不对称 | 本轮实跑复现（本地 http.server mock 200+空 choices、哑令牌、临时 job 目录测毕清理）：python hive/exec.py → rc=0、result.json ok=true、content=''；_empty_turn 存在（:1200-1206 缺陷时点）且 docstring 明言不冒充有效终答，但仅工具路径使用 | 留档（本会话 grep 核实 _empty_turn 现仍仅工具路径一处调用） |

### auth 区（4 条目，2 项已修并入 F2、2 项留档）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N60（本报告编号；与 inject 区第 1 条同一缺陷双报） | md_cg/mcp_server.py:2887（ref 入口；实际读文件 refindex.py:439-465） | medium（auth 区评级；inject 区对同一缺陷定 **high**，取高者处置） | cg op=ref 的 ref/path/root 均为模型可控参数，直通 refindex.read_ref→probe_ref 的 os.path.join+open，全程无 check_path_root——P1-4 根白名单契约（配置 MDCG_INGEST_ROOT/MDCG_EXPORT_ROOT 即 fail-closed）在读回路径失效，可读白名单外任意本地文件任意行区间；check_path_root 全文仅 5 处调用，ref 分支零调用 | 独立复现（临时目录哑数据测毕清理）：对照 ingest 同路径被 PermissionError 拦（P1-4 生效）；攻击形态经真实分发路径返回 ok=true 且哑样例密钥两行全量漏出；root 参数覆盖同法读出 victim 首行；record 令牌与 guest 身份均被 require_op 拒——边界为 designer 面 | **已修**（F2） |
| N61 | md_cg/mcp_server.py:2275（index_code root=a.get("path")；index_doc 同型 :2307） | medium | cg op=index_code/index_doc 的 path 参数不经 check_path_root，白名单外目录直接进图（P1-4 点名的『/etc/passwd 进图』形态）；进图节点 sensitivity=internal，对无令牌 guest 持久可读——一次 designer 调用即把本地敏感文件变成全 root 低权可读面 | 独立复现：①index_code 白名单外 vault 返回 ok=true indexed=1（对照 ingest 同路径被拦）——越界进图成立；②泄漏通道实测修正见第三节末；③index_doc 对 .md 章节摘要首段进 content（受 MAX_DOC 截断）；④guest cg.get 经 op=read 召回命中泄漏串 | **已修**（F2 同补丁顺带，留档清单的处置建议被采纳） |
| N62 | md_cg/tokens.py:410（tenant=tenant or rec.get("tenant")）+ mcp_server.py:3448（verify_token 传 env MDCG_TENANT） | medium | 令牌的租户绑定被 env 参数静默覆盖（签发时记录的 tenant 仅作缺省值）；同时租户登记表的 clearance_cap 夹紧（security.py:316-334 cap_of/principal_for）在权威令牌路径从未接线（全仓 grep 授权路径零调用）——一枚 tenantA 签发的 secret/designer 令牌可对 cap=public 的 tenantB 登记根以 secret+can_admin 运行，多租户密级上限契约失效 | 独立复现（临时哑令牌+临时租户登记表测毕清理）：verify_token(tok, tenant="tenantB") → principal{tenant:tenantB, clearance:secret, can_admin:true} 覆盖成立无夹紧；对照 TenantRegistry.principal_for 夹紧到 public（接线后本应有的防护缺席）；_resolve_root 不设 MDCG_ROOT 时随 env 切到 tenantB 登记根（设 MDCG_ROOT 冲突时有 fail-closed 拒启一层缓解） | 留档（本会话读核 :410 现状未变；修复涉授权链语义设计超出本轮范围） |
| N63 | src/lib/token_store.ts:97-105（writeKeyring） | low | 密钥环写入 writeFileSync 直写（无 tmp+rename），偏离本仓原子写纪律（md_cg/tokens.py _write_secret、security.py TenantRegistry._save 均 tmp+os.replace）；首启自动签发窗口进程中断留半截令牌文件，后续激活把残缺内容当 keyring 凭据原样喂 MCP，verify_token fail-closed 拒启——写入凭据状态损坏，需手工删密钥环恢复 | 读码核实：:99 直写目标文件；readKeyring 任意非空内容原样返回无格式校验并注入 MDCG_TOKEN；python 侧影响链实测（哑令牌）：截断令牌 verify_token 抛 TokenError、格式损坏 parse_token 抛「令牌格式非法」，对应 fail-closed 拒启。**TS 侧中断注入复现未运行（无 TS 运行环境）——读码证据，如实标注** | 留档（本会话读核 :96-105 现状未变） |

### inject 区（4 条目，1 项已修并入 F2、3 项留档）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N60 重复条目（inject 区独立报告） | md_cg/refindex.py:439-466（入口 mcp_server.py:2880-2887） | **high**（越权任意文件读原语口径） | op=ref 是越权任意文件读原语：probe_ref 以调用方自报的 root 与 ref.path 直接 join 后 open 全文回读，无租户根校验、无 check_path_root、无密级闸；designer 令牌 ops_allow=["*"] 恒可触发，签发者可授自定义只读面含 ref；inline ref 字典与 root 参数均模型可控，提示注入即可让常驻 agent 读宿主机任意文件并全文进入模型上下文。同族缺口：index_code/index_doc（即 N61） | 临时目录实测（只读 Principal actor=attacker, ops_allow 含 ref，测毕清理）：三形态全部 ok=True 全文回读——①自报 root 指记忆根外 victim；②ref.path 绝对路径（join 丢弃 root）；③`../` 上跳；对照组 ops_allow 不含 ref 的令牌被 require_op 拦（op 闸工作、缺口在路径零校验）；对照同库 add() 的 node_id 白名单+realpath 双闸有效未见旁路 | **已修**（F2；修复注释自称「两份独立报告合并」本会话读核在位） |
| N64 | compiler/codegen.py:390（同族 :490-491） | medium | 假成功掩盖失败：赋值目标与裸标识符表达式不经 _py_name 校验直接拼入产物——ASCII Python 关键字作目标/表达式时编译报 success=True 且验证单元终裁 passed=True/confidence=0.85，但产物 compile() 必 SyntaxError；函数名有 _py_name 闸（:525）而赋值目标/表达式标识符漏挂——终裁权威被击穿为橡皮图章 | 本轮实测（python -X utf8，临时脚本测后删）：compile_source('class = 5')/('import = 5')/('lambda = 3')/('x = lambda') 全部 success=True、errors=[]、verdict.passed=True/confidence=0.85，对产物 compile 均抛 SyntaxError；对照 'x = 5' 产物可正常 compile；api.py:194 success=len(errors)==0 对产物无 compile 终验 | 留档（本会话读核 :390 f"{stmt.target} = " 现状未变） |
| N65 | src/lib/roleplay_web.ts:745（同族 :952-960） | low | HTTP 入口校验不一致：/roleplay/api/roles/:id/import 提取的 role_id 与 POST /roleplay/api/roles（role_create）的 body role_id 不做 validRoleId 白名单校验，任意键直落 _roles.json 并透传外部 capability 后端；/meta(:804) 与 chat(:845) 均已挂白名单 | 读码核实：/import 分支仅 decodeURIComponent 取段无 validRoleId 即写 meta 落盘+callTool('role_import')；POST /roles p.role_id 未校验直传 role_create 与 writeRole。node 实测：null-proto 对象上赋 __proto__ 键会持久化（JSON.stringify 输出该键、hasOwnProperty 为 true）；JSON.parse 回读不污染原型。缓解确认在位：readRoleMeta 空原型（:606-620）；mdcg 侧 node_id 白名单拦截 roleplay_${role} 模板注入 | 留档（本会话读核 :745 与 :952-960 现状未变） |
| N66 | src/lib/roleplay_web.ts:829-891 | medium | /roleplay/api/chat 无 CSRF 防护：不校验 Origin/Referer/Host，readBody 不检查 Content-Type，写操作零鉴权——恶意网页可对本地 webServer 跨站静默写入受害者转录并触发 LLM 生成（持久记忆投毒面）；写类端点有 x-edit-key 闸兜底，chat 端点无任何等价防线 | 临时目录 mock 实测（tsc 编译到临时目录，mock webServer/capability/mdcg，哑后端测毕清理）：①text/plain 简单请求形态（无 Origin/Referer）POST /roleplay/api/chat → 200、roleplay_chat 被调用、writeTranscript 落图两次、转录文件落盘两行；②伪造 Origin 同样 200；③对照同形态 POST /roleplay/api/translate → 403（P1-2 闸工作）。**未实测前提（如实标注）：本机无 Cordis 宿主未做浏览器端到端验证** | 留档（本会话读核 :829-835 现状未变；修复涉合法跨源接入面决策） |

### fs 区（6 条目，1 项已修即 F5、5 项留档）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| v2 N6（fs 区条目，与 legacy 区第 4 条同一缺陷双报） | hive/exec.py:103 | medium | result.json 非原子写：open("w") 直接截断写，无 tmp+rename，超时强杀/崩溃留半截文件且先毁旧完整结果；读者面 wm._read_json 不捕获解析异常 → wm snapshot 裸 traceback；rust 轮询/hive_mcp._result_view/orch._card 同暴露 | 临时目录实测（测毕清理）：旧完整结果在位后写 3MB×30 + 4 并发读者 → 145 次读命中 131 次撕裂（28 半截解码失败 + 103 空文件窗口）；对照组 exec_cmd._write_result 同场景 2762 次读 0 撕裂；半截 result.json 实跑 wm snapshot → 裸 Traceback（JSONDecodeError 逃出 except WmError） | **已修**（F5） |
| N67 | hive/wm.py:236（现行 :253——同文件 F4 修复上方增行致漂移，本会话读核 join 在 :253） | medium | cmd_progress 的 --job-id 无路径段校验，os.path.join(wm,"jobs",job_id,...) 可穿越读 wm 仓外任意 JSONL 文件并全文回显；cmd_snapshot 对同款输入有「防穿越删库」校验、hive_mcp._valid_job_id 已修同族穿越（poll/kill），progress 分支漏同类闸 | 临时目录实测（测毕清理）：job_id=两穿越形态均返回 ok=True、entries 与 handoff 聚合卡全文回显仓外 progress.jsonl（哑密文标记漏出）；对照实测 _valid_job_id 对同形态 =False | 留档（**处置建议未于本轮采纳**：留档材料建议修 wm.py:166 时同 patch 顺带挂闸约三行，本轮 F4 修复面未含——如实记录，见第五节） |
| N68 | hive/orch.py:417 | medium | _spawn 白名单键透传 mdcg_root 给编排者模型：认知图读写面落点由不可信 LLM 控制可在任意目录创建文件/写入模型可控内容；exec.py 工具分发对编排工具无键过滤，子任务经 spec.get("mdcg_root") → env MDCG_ROOT 优先、spec 兜底——serve 未设 MDCG_ROOT 的部署即生效 | 读码+临时目录实测（测毕清理）：pop MDCG_ROOT 后以 mdcg_root=临时 evil_root 调 op=write → 目标目录被创建 _audit.jsonl/_keys.json/hippocampus/inbox.jsonl（含模型可控哑正文标记）等；op=read 同样接受任意目录为图根。补强 nuance（如实）：_spawn_schema 未声明 mdcg_root 键，正常调用面不常传，但代码层零过滤、注入可达；.md 节点直写需 audit ACCEPT，medium 定级成立 | 留档（本会话读核 :417-418 键元组现状未变） |
| N69 | hive/orch.py:417（context_files 通道 :408-411） | medium | context_files 通道绕过 read_file 的全部敏感文件防线：编排者模型可指定凭据类文件全文注入子代理上下文发往外部 LLM 网关——read_file 工具面三道防线（scope_roots+roots+_sensitive_read+PII 脱敏）在 spec.context_files 通道零检查，且该参数在 _spawn_schema 明确声明为模型正常可用 | 读码对比+临时目录实测（哑数据测毕清理）：伪造 .ssh 私钥与 .env 经 build_messages → 私钥块与哑样例密钥全文进 messages、无任何脱敏标记；同两路径 tool_read_file 均被 _sensitive_read 拒读（「敏感凭据目录」「密钥/环境凭据文件」）——同文件双通道防线差异实拍。**未实测外发（不触真实网关/密钥）** | 留档（本会话读核 :408-411/:417 现状未变） |
| N70 | hive/orch.py:269 | low | _save_children 静默吞写盘失败（except OSError: pass，无 log 无重试）→ children 清单丢失，换人续跑重复派发子任务；tmp 固定名 _children.json.tmp 与 fsutil.py 记录的固定临时名教训同款 | 读码+临时环境实测（测毕清理）：monkeypatch os.replace 抛 PermissionError → 静默返回 None（无异常无日志）；目录不可写同样吞；_load_children 对缺失文件返回 []（接管者视为无子任务→重复派发）；对照组实测裸 os.replace 30/30 抛 WinError 5 佐证 Windows 持锁真实存在。**未做真实磁盘锁注入（不可控），风险路径代码级确证** | 留档（本会话读核 :269-276 现状未变） |
| N71 | md_cg/mdcos.py:2158 | low | review_decide 的 merge 分支为无互斥的读-改-写（读 target→拼接→写回），多实例并发 merge 同一目标可丢失一方合并内容而两条裁决均记 accepted；FileLock 只罩 decisions.jsonl 追加不罩节点读写窗口 | 临时目录实测（测毕清理）：两独立实例 + Barrier 确定性交错并发 merge 同一 target → A 的哑合并载荷丢失（终态只含 B 的）、decisions.jsonl 两条裁决均 accepted——结构上无锁即丢合并实拍；此前 15 轮自然并发未复现（窗口亚毫秒）与 low 定级一致：结构性风险、非高频触发 | 留档（本会话读核 merge 分支现状未变） |

### 证据形态修正与如实记录（不动摇结论，2 处）

1. **N61 泄漏通道修正**（auth 区复核自纠）：原证据「模块顶部普通 `#` 注释进图」实测**不成立**（节点判『无注释』）；实际进 content 的三条通道为**模块 docstring、函数/类定义上方的前置注释、签名**，及 index_doc 的 **md 章节摘要首段**（受截断）——敏感内容经这三条通道确实进图且 guest 可读，核心结论不变。
2. **N64 子例证修正**（inject 区复核自纠）：原子例证 'ⁱ'（isalpha 但非 XID）实测产物**可** compile（CPython 标识符 NFKC 归一兜底），该子例不成立；但 ASCII 关键字（class/import/lambda）在两处漏挂位置均实测成立，核心结论不变。

另如实声明：legacy 区 8 项的「本轮实跑复现」为上游复核会话执行（材料原文）；批次 37 声称已修未落盘的 git 取证（03ae053 对 verify_runner.py 无 diff、对 wm.py 仅 +4 行）亦为材料给定，本报告会话未重跑 git 考古、只对现行代码做了读核确认（防御确实不在位 → 重开正当）。

---

## 四、验证（门禁结果）

以下「本报告会话实跑」均为本报告写作会话在仓库根目录实际运行（第一手）；「材料口径」为编排/修复会话已跑、本会话未重跑（如实区分）：

| 命令 | 结果 | 来源 |
|------|------|------|
| `python scripts/run_tests.py --jobs 4`（全量） | `===== SUMMARY 174/174 通过，5 跳过（依赖缺失/平台不符） =====`（尾部 PASS 含 hive.test_wm_progress / hive.test_exec_tools / swarm.tests.test_rust_swarm，与材料逐字一致；174 = v7 基线 172 + 本轮两个新守卫套件 compiler/tests/test_lexer_v3n6.py 与 md_cg/test_p1x_ref_root.py 自动发现入列；材料口径退出码 0，本会话因管道 tail 未捕获退出码——如实注明） | **本会话实跑** |
| `python -X utf8 compiler/tests/test_lexer_v3n6.py` | `=== v3 N6 词法定向断开回归: 15/15 通过 ===` | **本会话实跑** |
| `python -X utf8 -m md_cg.test_p1x_ref_root` | `P1-X 根白名单守卫: 10/10 通过` | **本会话实跑** |
| `python -X utf8 -m md_cg.test_p39_verify_flow` | `=== verify flow tests: 11 passed, 0 failed ===`（含新 2e/2f） | **本会话实跑** |
| `python -X utf8 hive/test_wm_progress.py` | `结果：PASS=49 FAIL=0`（含新 G1-G5） | **本会话实跑** |
| `python -X utf8 hive/test_exec_tools.py` | `结果：87 通过 / 0 失败`（含新 K1/K2） | **本会话实跑** |
| `python scripts/check_publish_artifact.py`（`npm run gate` 第 2 环单独复现） | R1 凭据/密钥面 PASS（文件面=0 内容面=0）、R2 私有数据面 PASS、R3 隐私文本 PASS；**R4 非追踪件面 FAIL 命中=1；非追踪=25，允许=24——命中恰为 md_cg/test_p1x_ref_root.py**；`VERDICT=FAIL`——与材料门禁输出同因同置 | **本会话实跑** |
| `npm run gate`（全链六环） | 材料口径 FAIL（R4 时序性：新守卫文件尚未 git add 登记追踪面）；本会话未重跑全链、仅单独复现 R4 环（上 row）——处置：按守卫自身要求 `git add` 登记后复跑即过（v7 先例），**归编排会话提交前裁决，本报告会话未擅自登记/提交** | 材料 + 本会话部分复现 |
| `cargo test` | **未运行**——Rust 面零改动（`git status` 证实改动面为 compiler/ hive/ md_cg/ scripts/ docs/ 与两个新守卫文件，rust/ 与 swarm/rust_runtime/ 未动） | 双方一致 |
| `npm test` | **未运行**——TS 面零改动（src/ 与 test/ 未动；遗留 N63/N65/N66 的 TS 文件本轮只读核未改） | 双方一致 |

**上游会话已运行、本会话未重跑的验证**（材料原文，如实区分）：五项修复的临时攻击复现守卫（修复前红态：F1 六项红→14/14、F2 五项红→10/10、F3/F4/F5 同脚本修复前后各一跑）；F2 的 `git stash` 回退对照（旧码下永久守卫 5/10）；`verify_linux.sh` 同口径 python suites 41 套（清单 40 + 新守卫）pass=41 fail=0；gate 四段 cogmap/link/wsidx/disc 全 0（F2 中途 cogmap 曾 =1——插入代码致 README 锚点行号漂移，跑 cogmap_sync build 写回标记段后归零，README 用户未提交文案改动保留段外，如实记录）；hive/scripts 四入口（orch/et/se/jm）全 0；compiler/tests 全部 9 文件逐一 OK；F3 的 `python -m md_cg.test_interop_judgment` 31/0；F5 全量首跑 173/174（md_cg.test_review_conformance 单套偶发失败→单独复跑 38/0、全量再跑 174/174，该套件不在 hive/exec.py 依赖链上，判定偶发非回归——如实报告）。

**git 工作树核对**（本会话 `git status`/`git diff --stat`）：改动 12 文件——五个修复补丁（compiler/lexer.py +15、md_cg/mcp_server.py +20、hive/verify_runner.py +21/-3、hive/wm.py +37/-10、hive/exec.py +28/-2）+ 守卫四处（md_cg/test_p39_verify_flow.py +27、hive/test_wm_progress.py +23、hive/test_exec_tools.py +47）+ 两个新守卫文件（compiler/tests/test_lexer_v3n6.py、md_cg/test_p1x_ref_root.py，untracked）+ cogmap 同步两文件（README.md、docs/mdcg/功能调用映射表_v0.1.md）+ scripts/verify_linux.sh（注册新守卫）+ md_cg/whitebox_kb/wisdom/code_solidified.json（先前会话改动，与本轮修复无关）——与五项修复一一对应，无多余改动面。未做任何 commit。

---

## 五、遗留清单（14 项：medium 10 / low 4）

### medium（10 项）

| # | where（现行，本会话读核） | 问题 | 留档理由 / 处置线索（材料口径） |
|---|------|------|------------------------------|
| v2 N16 | hive/hive_mcp/mcp_server.py:566 | 非 dict JSON 形态（批量数组/str/int）一行打崩 MCP server 常驻进程（DoS） | 材料明示：修复面小（_rpc 入口加 isinstance(req, dict) 守卫），**竞争第 5 席败于 exec.py:103（三报告命中+模板在位）**；本轮仅读码核实未独立复现 |
| v6 N38 | src/lib/token_store.ts:76 | auxRoot 波浪线/相对路径形态 node 与 python 分居两处 → 重复自动签发永不吊销的 designer 令牌 | 材料未附分诊注；影响面为采用波浪线/相对 MDCG_AUX_ROOT 的沙箱/多用户部署 |
| v5 N24 | swarm/swarm_cli.py:78 | CLI 静默丢弃 swarm.json 的 condition_space 声明（G-R2 卡不生效） | 材料未附分诊注；修法明确（实参表补一项透传），库层支持完整、仅 CLI 缺口 |
| v2 N7 | hive/exec.py:1541（缺陷时点 :1517） | call_llm 单发路径缺 _empty_turn 防护：空回包记 ok=true 假成功 | 材料未附分诊注；_empty_turn 与工具路径防护模板均在位（:1224/:1372），单发分支补对称防护即可 |
| N62 | md_cg/tokens.py:410 + mcp_server.py:3448 | 租户绑定被 env 覆盖 + clearance_cap 夹紧从未接线（多租户密级上限失效） | 材料明示：多租户接线方案涉及授权链语义设计，超出本轮低风险修复范围；_resolve_root 冲突 fail-closed 为一层缓解 |
| N64 | compiler/codegen.py:390（同族 :490-491） | 赋值目标/裸标识符表达式漏挂 _py_name → ASCII 关键字假成功（终裁橡皮图章） | 材料未附分诊注；函数名闸模板在位（:525），两处补对称校验即可 |
| N66 | src/lib/roleplay_web.ts:829-891 | /roleplay/api/chat 无 CSRF 防护（跨站静默写转录 + 记忆投毒 + 计费触发） | 材料明示：修复需引入 Origin 校验策略（涉及合法跨源接入面决策），超出低风险范畴；写类端点 x-edit-key 闸在位可参照 |
| N67 | hive/wm.py:253（材料缺陷时点 :236） | cmd_progress --job-id 无路径段校验：穿越读 wm 仓外 JSONL 全文回显 | **留档材料的处置建议（与 F4 同 patch 顺带挂 _valid_job_id 式闸约三行）本轮未被采纳**——F4 修复面只含 try/finally，如实记录；模板 hive_mcp/mcp_server.py:100-111 在位 |
| N68 | hive/orch.py:417 | _spawn 白名单键透传 mdcg_root：不可信 LLM 控制认知图落点（任意目录建文件+内容注入） | 材料未附分诊注；.md 节点直写需 audit ACCEPT 为一层缓解，schema 未声明该键降低常态可达性 |
| N69 | hive/orch.py:417（通道 :408-411） | context_files 绕过 read_file 全部敏感文件防线：凭据类文件全文进 LLM 上下文发往外部网关 | 材料未附分诊注；read_file 三道防线（scope_roots/_sensitive_read/PII 脱敏）模板在位，通道侧补同款检查即可 |

### low（4 项）

| # | where（现行） | 问题 | 留档理由 / 处置线索 |
|---|------|------|---------------------|
| N63 | src/lib/token_store.ts:97-105 | writeKeyring 直写非原子：首启中断留半截令牌 → 后续激活 fail-closed 拒启（可用性，非越权） | 影响为记忆写通道瘫痪直至手工删密钥环；python 侧同款纪律模板（_write_secret/TenantRegistry._save）在位；TS 侧复现受无运行环境所限（读码证据） |
| N65 | src/lib/roleplay_web.ts:745（同族 :952-960） | import URL 段与 role_create body 的 role_id 不做 validRoleId 白名单：任意键直落 _roles.json 并透传外部后端 | /meta 与 /chat 的白名单+400 模板在位（:804/:845），两处补对称校验即可；readRoleMeta 空原型与 mdcg node_id 白名单两层缓解在位 |
| N70 | hive/orch.py:269 | _save_children 静默吞写盘失败 → children 清单丢失 → 接管者重复派发 | fsutil.py 的短重试+唯一临时名模板在位；未做真实磁盘锁注入（不可控），风险路径代码级确证 |
| N71 | md_cg/mdcos.py:2158 | review_decide merge 分支读-改-写无互斥：并发 merge 同目标丢一方内容而双裁决 accepted | 结构性风险、非高频触发（自然并发 15 轮未复现、Barrier 确定性交错实拍）；修复需节点级写锁设计 |

**优先级建议**（据材料处置线索汇总）：v2 N16（修复面最小+DoS 面）与 N67（三行模板在位）为下轮速收候选；N64/N65 为「模板在位补对称校验」类低风险项；v2 N7 假成功与 N69 凭据外发面建议同轮处置（同属「结果/上下文诚实性」）；N62 多租户接线与 N66 CSRF 策略需先做接入面设计决策再动代码。

---

## 六、纪律合规声明

1. **定位先读索引**：本报告写作前已读 WORKSPACE_INDEX.md（按职责定位 docs/eval 为报告归域、hive/ 为蜂巢并发引擎、md_cg/ 为记忆系统本体、compiler/ 为编译器、src/ 为 TS 插件源码），未做全盘浏览。
2. **结论引证**：所有修复与缺陷均引 path:line（缺陷时点快照行号，本轮三文件行号漂移处另注现行行号并经本会话读核）；第三节证据要点摘自任务输入的四区复核材料（上游会话读码+实跑，环境细节按仓库口径转录）；第四节「本会话实跑」七项为第一手命令与输出原文，材料口径与未跑项（cargo/npm test/gate 全链/git 考古/攻击复现重跑）均如实标注来源，无一冒充。
3. **修复落盘核验**：五项修复的现行代码本报告会话逐处读核（lexer.py:393-394 既有集合+ :396-410 定向断开、mcp_server.py:2280/:2316/:2902-2906 三闸 + verify_linux.sh 注册、verify_runner.py:102-106 白名单、wm.py:174-190 try/finally、exec.py:116-129 原子写+重试环），14 项遗留的现行现状亦逐一 grep/读核确认（第五节「现行」列），git 工作树改动面与五项修复一一对应。
4. **安全测试纪律**：材料中全部攻击复现均在系统临时区/同盘仓库外临时目录/临时子进程，使用哑令牌与哑数据（本报告以「哑样例」占位描述，不复写字面量），不触真实数据目录与真实令牌，不触真实网关/密钥外发，测毕清理（材料逐项声明）；本报告会话只运行守卫套件与门禁检查器，未做任何攻击复现。
5. **已修项不重复报**：本轮 8 项 legacy 中 v2 N5/N11 与 v2 N10 经 git 取证证实批次 37 声称已修未落盘（材料给定；本会话读核现行代码防御确实不在位），属重开非重复；#34 AccessDenied hint、#35 会话隔离三层、#37 互验六项、V21 安全审计系列、第 7 轮性能修复均已落盘，本报告未再报。
6. **隐私脱敏**：全文只写仓库相对路径；材料的本机绝对路径（盘符/用户目录/临时区路径）一律转录为「系统临时区/仓库外同盘临时目录」；哑令牌/哑密钥/哑密码样例一律以占位描述（「哑样例密钥」「哑密文标记」「模型可控哑正文」等），无任何 key/token 字面量；环境变量名与标识符（MDCG_INGEST_ROOT/HIVE_ROLE 等）为源码标识符非凭据值，为证据必需而保留；定稿前经机械扫描复查（见下方自查）。

---

## 七、跳过项及原因

- **cargo test 未运行**：Rust 面零改动（本会话 git status 证实 rust/ 与 swarm/rust_runtime/ 未动，F3-F5 修复会话亦声明无 Rust 改动、cargo 不适用）。
- **npm test 未运行**：TS 面零改动（src/ 未动；N63/N65/N66 仅读核留档）。
- **`npm run gate` 全链未于本会话重跑**：材料口径 R4 时序性 FAIL（新守卫 md_cg/test_p1x_ref_root.py untracked、非追踪=25>允许=24）；本会话以第 2 环 check_publish_artifact 单独复现同因同置（R1/R2/R3 PASS）——按守卫自身要求 `git add` 登记后复跑即过（v7 先例），**处置归编排会话提交前裁决，本会话未擅自登记或提交**。
- **TS 侧中断注入复现未运行**（N63）：无 TS 运行环境——材料如实标注为读码证据；python 侧影响链已实测。
- **浏览器端到端 CSRF 未实测**（N66）：本机无 Cordis 宿主，webServer 实际监听地址属宿主行为（仓库内无该配置）；已实测 mock 服务端形态（text/plain 简单请求直达 + 伪造 Origin 照常处理），跨站「简单请求无预检」为 web 平台标准语义。
- **真实磁盘锁注入未做**（N70）：不可控环境注入，以 monkeypatch 确定性证明 + 对照组裸 os.replace 30/30 WinError 5 佐证。
- **凭据外发未实测**（N69）：不触真实网关/密钥，以 build_messages 侧全文进 messages 实拍 + read_file 双通道对照实拍为界。
- **ref action=check/prune 巡检分支未挂闸**（F2 范围声明）：读图内登记 ref（非 inline 模型可控），按报告修复范围只覆盖 read/get 分支与两索引显式 path，未扩大。
- **md_cg/whitebox_kb/wisdom/test_navigate_self_bootstrap.py 直跑失败**（F1 验证中途处置）：ModuleNotFoundError（缺 aeis_core 依赖）——git stash 仅回退 lexer.py 对照，旧代码同败，为预存环境问题（该文件不在 verify_linux.sh 清单内），与本次改动无关，如实报告。
- **git 未提交**：commit 需任务方指令；工作树保持五补丁+守卫+cogmap 同步原状。

---

*报告依据：任务输入的第 8 轮材料（四区复核 22 条确认明细及证据、五项修复的 where/攻击前后对比/守卫/verified_by、留档 15 条中 1 条被 F2 顺带覆盖、门禁结果）；本报告会话的修复落盘读核（lexer/mcp_server/verify_runner/wm/exec 五补丁锚点 + 三闸 + verify_linux.sh 注册 + 守卫段落 2e/2f、[G]、[K]）、14 项遗留现行读核（第五节「现行」列）、五套守卫实跑（15/15、10/10、11/0、49/0、87/0）、全量回归实跑（174/174）、R4 门禁单独复现（FAIL 同因同置）、git 工作树核对、报告脱敏机械扫描。*

*自查双清单：①材料覆盖——四区 22 条目全部入文（legacy 8 / auth 4 / inject 4 / fs 6），0 证伪如实记录，证据形态修正 2 处单列，5 补丁 6 缺陷逐项五要素齐全，留档 15−1（N61 并入 F2）=14 与第五节数目吻合，门禁四项（python/cargo/npm test/gate）与跳过原因一一对应；②纪律——全部结论引 path:line 或注明材料来源，八项第一手实跑命令与输出原文在第四节，未跑项（cargo/npm test/gate 全链/git 考古/攻击复现）无一冒充为已跑，全文无本机绝对路径、无 key/token 字面量（机械扫描盘符/用户目录/token 形态零命中）。*

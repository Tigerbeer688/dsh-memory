# 缺陷挖掘报告 · 自主迭代 v9（md_cg 二轮深挖）

- **日期**：2026-09-25
- **范围**（生效条件）：第 9 轮 = md_cg 二轮深挖，四区复核（recent / retrieval / write / legacy）共 **22 条目**（confirmed 19 / 整项剔除 3）→ 去重后独立缺陷 **16**（本轮新发现 10 项 **N72-N81**：high 1 / medium 6 / low 3；legacy 复测确认 6 项：v8 N62 medium，v8 N71、v7 N51、v7 N52、v7 N50 残腿、v7 负层残余均 low）→ 本轮修复 **5 个补丁覆盖 5 个新缺陷**（N72-N76：high 1 / medium 4）→ 遗留 **11**（medium 3 / low 8）＋留档清单中 2 条占位去重说明（跨区重复条目，不承载独立缺陷）
- **编号口径**：本轮新发现沿历轮 N 口径续编 **N72-N81**（v8 用至 N71）；legacy 项沿用原轮次号（v7 N50/N51/N52、v8 N62/N71）
- **第 7 轮留档两项复测结论**（任务点名显式记录，详见第三节末专节）：①**负层全读每查询磁盘 IO**——IO 已被第 7 轮读缓存默认化覆盖，残余为 `_emit` 尾部裸 open ×3 与 `scanned` 审计口径盲区（留档）；②**四路召回重复读盘（v7 N50）**——`_read` 系四腿已全部覆盖（稳态增量 0），唯一残腿为 `_path_graph` 种子经 `MdCG.get` 裸 open 恒 ≤5 次（留档）；N50 原「同节点重复 open+parse 税」形态经对照实跑**证伪剔除**（见 rejected）
- **回归**：`python scripts/run_tests.py --jobs 4` 全量 **179/179 通过、5 跳过（依赖缺失/平台不符）、退出码 0**（**本报告会话独立实跑，SUMMARY 行与材料逐字一致**；179 = v8 基线 174 + 本轮 5 个新守卫套件）；`cargo test` **未运行**（Rust 面零改动）；`npm test` **未运行**（TS 面零改动）；`npm run gate` **FAIL——R4 非追踪件面命中恰为本轮 5 个新守卫文件**（未 `git add` 的时序性失败；本会话以第 2 环 `scripts/check_publish_artifact.py` 单独复现同因同置，R1/R2/R3 全 PASS），处置归编排会话提交前裁决（v7/v8 先例）
- **行号口径**：缺陷行号为缺陷时点快照行号；本轮修复使 `mdcg.py`（:3171 起 +7 行）与 `mdcos.py`（缓存段 +34 行净增，其后行号整体下移）漂移，涉及处本会话另注**现行行号**（均已逐一读核，见第五节「现行」列）
- **结构口径**（CCG 六要素）：功能名（标题）/ 生效条件（范围与口径行）/ 子功能（第二~五节）/ 执行与验证方式（第四节：实跑命令与结果）/ 不适用条件（第七节：跳过与未运行项）

---

## 一、执行摘要

本轮为 md_cg 二轮深挖：四区复核 22 条目、3 项整项剔除（含对 v7 留档两项的复测定谳），修复 5 补丁、留档 11 项。四个主题：

1. **第 7 轮读缓存默认化的「治理写前重查」盲区（N72，high，1 修）**：第 7 轮把读缓存改为默认开（28.3×）时，只为 backfill 15 处与 mreview.govern 3 处装配了 `direct_read` 穿透直读，而 consolidate / ccgc / crosscheck / vision_evidence 四文件的治理写面共 **14 处读-改-写读点仍走缓存版 `cg._read`**——常驻进程「检索装载缓存 → 另一进程经统一写口落盘修正 → 同实例执行治理写」的时序真实可达，B 的修正被 A 进程内陈旧代际缓存**静默覆盖丢失**（以真实函数 `ccgc.recalibrate` 复现：默认开组 B 修正幸存=False，`MDCG_READ_CACHE=0` 对照组幸存=True——丢失由默认开引入属实）。本轮 14 处全部改 `direct_read`（与第 7 轮同口径）。
2. **「落盘了但对账面不可见」三连（3 修，同族缺陷）**：N75——`verify()` 在节点已在目标验证态时的直写分支落盘后不置 `_dirty`，索引增量日志与读缓存代际双双看不见本次写盘（重开后索引 evidence_count 与盘面漂移、同实例 `_read` 命中旧 frontmatter）；N74——`discard` 摘除分支索引只 pop 内存不落 tombstone，close 时 compact 的「计数对账重扫+日志重放」把已物理移走节点的 upsert 叠回，**幽灵条目被固化进 _index.json 快照永久存活**；N73——审核队列四增量对账缓存的水位存「读前 stat 的 size」而 `read_jsonl_tail` 读到真实 EOF，stat→read 窗口内的跨进程 append 在下一轮被**重复并入且进程内永不自愈**（review_list 重复 pid、review_stats 虚高）。三者同属「写盘生效、但对增量日志 / 缓存代际 / 快照对账不可见」家族。
3. **连接层 RMW 无互斥 + 固定临时名（N76，1 修）**：links 全部变更原语（handshake/observe/promote/degrade/isolate/withdraw/decay_all）是 load→改内存→save 的读-改-写，无任何跨进程锁，且 save 用固定临时名 `p+".tmp"`（`fsutil.py` 头部明载的已实名反模式）——并发下后写者整文件覆盖前写者（**20/20 静默丢一方信任更新**）、两写者互踩同一 .tmp 产生内容交织（audit 超单写者合法上限实拍）、`_publish` 只捕 PermissionError 致 rename 被抢先时抛 FileNotFoundError。本轮改 `atomic_write` + `_locked` 连接级全程互斥。
4. **留档复测与证伪（如实记录）**：**第 7 轮留档两项复测结论显式定谳**（页首口径行 + 第三节末专节）；v8 N71（merge 无互斥）三区独立复测全部成立、维持留档不重报（确定性交错：双 ok=True 而一方合并载荷丢失；与本轮 N72 相互独立——merge 基底走 `self.get` 直读盘无缓存陈旧成分，纯无互斥竞态）；v8 N62 / v7 N51 / v7 N52 复测维持；两项整项剔除（v7 N50 原「重复 open+parse 税」形态已被第 7 轮读缓存默认开消除；负层「每查询全量 open+parse」已被覆盖，残留仅口径 nit 非缺陷级）。

**对输入口径的核验**：输入称「四区 19 confirmed + 3 rejected = 22 条目 → 去重 16 独立缺陷 → 修 5 → 留档 11 + 2 占位」。本会话以 `git status` / `git diff --stat` + 逐处读核核对：改动面恰为——九个修复文件（consolidate.py +13、ccgc.py +5、crosscheck.py +7、vision_evidence.py +7、mdcg.py +7、branches.py +16、fsutil.py +33/-15、mdcos.py +71/-37、links.py 245 行改写）+ `scripts/verify_linux.sh`（+3 行注册 5 个新守卫，现行 :21-23）+ 五个新守卫文件（untracked）——与五项修复一一对应；工作树另有三件非本轮修复的改动如实注明（见第四节 git 核对）。16 − 5 = 11 留档，无多无少。

---

## 二、修复清单（5 项，逐项 where / 问题 / 修复 / 前后对比 / 守卫）

### F1（N72，high）治理写面 14 处读点改 direct_read：读缓存默认开下另进程修正不再被静默覆盖

- **where**：`md_cg/readcache.py:57-97`（install 默认开、包装 `cg._read` 为脏代际缓存）× 四文件治理写面 14 处读点——缺陷时点 consolidate.py:678/814/894/965/1067/1125、ccgc.py:757/841、crosscheck.py:344/786/941、vision_evidence.py:296/495/579；修复落点（现行，本会话 grep 逐一读核 14 处 + 4 行 import 全在位）consolidate.py:679/815/895/966/1068/1126、ccgc.py:758/842、crosscheck.py:345/787/942、vision_evidence.py:297/496/580
- **问题**：第 7 轮读缓存默认化只覆盖了 backfill（15 处）与 mreview.govern（3 处）的写前重查面；四文件治理写面（consolidate 主流/验证基底回填/晋升及回滚/上下文化及回滚、ccgc link/recalibrate、crosscheck 扫描/主流程写前重查/回滚、vision_evidence 收集/执行/回滚）读-改-写读点仍走缓存版 `cg._read`——写前重查/回滚比对命中旧代际缓存即写回陈旧 frontmatter/content，跨进程合作写者的修正被静默覆盖。`readcache.py:24-25` 的「跨进程改写不保证可见」诚实边界**不覆盖本形态**：B 是全合作写者（走 `_write_node` 统一写口、durable=True 落盘），且第 7 轮已为同语义面装 direct_read，治理写前重查不在豁免范围。crosscheck.rollback 的 write_id 防护所验 fm 本身出自缓存，不构成防护。
- **修复**（最小改动：14 处 `cg._read(e)`/`_cg._read(entry)` → `direct_read(cg, e)`/`direct_read(_cg, entry)`，四文件各加一行 `from .readcache import direct_read`）：穿透缓存直读盘上真值，与第 7 轮 backfill/govern 同口径；未动 readcache.py 本体与任何写路径。
- **前后对比**（守卫实测，临时库测毕清理）：前——A 常驻实例检索装载缓存 → B 另一实例以 `_write_node(durable=True)` 落盘「重要修正」→ A 执行治理写（recalibrate/link/crosscheck/vision_evidence）时写前重查命中旧代际缓存，B 修正被静默覆盖（旧代码守卫 6 红：recalibrate 即缺陷报告原样复现幸存=False；`MDCG_READ_CACHE=0` 对照组同流程幸存=True，证实丢失由默认开缓存引入）。后——14 处读点全部走 direct_read 穿透读，B 修正幸存=True 且 A 自身治理写入照常生效，同一守卫 40/40 全绿；检索读面（search/_read_many）缓存行为零变化。
- **守卫**：新增 `md_cg/test_govern_directread.py`——红守卫=缺陷指定口径的 recalibrate 双实例复现（G1，含缓存关对照组 G1e）+ 同形态覆盖 ccgc.link / crosscheck 主流程与回滚 / vision_evidence 执行与回滚（G2-G6，旧代码确定性红）；G7-G12 为 consolidate 六读点口径守卫（root 入口内部自建新实例、单次调用首触即盘上真值，断言为「B 修正幸存且流程不回归」，修前修后均绿、非红证明——守卫文件头已如实注明）。执行轨迹：修前 PASS 34 / FAIL 6（G1c/G2d/G3b/G4b/G5b/G6b）→ 修后 PASS 40 / FAIL 0。已注册 `scripts/verify_linux.sh`（现行 :21-23）。**本会话实跑：PASS 40 / FAIL 0**。

### F2（N75，medium）verify() 直写分支显式置 _dirty：noop 形态写盘对索引与缓存可见

- **where**：缺陷时点 `md_cg/mdcg.py:3166-3170`（confirmed/weakened 直写分支落盘后只原地改 index 条目 `e["evidence_count"]`）；修复落点现行 `md_cg/mdcg.py:3171-3177`（注释 5 行 + 修复行 `self._dirty[node_id] = e`——材料口径 :3175，**本会话读核实为现行 :3177**）
- **问题**：节点已在目标验证态（verified→verified / doubted→doubted）时，verify 尾部 `set_verification` 走 stamp noop 提前返回（trust.py:192-193/695-696，不写盘不调 `_sync_index` 标脏），本次直写落盘对索引增量日志**完全不可见**——重开后索引 evidence_count/confidence 与盘面漂移（forgetting.py:358、scrub.py:181/582/716、metacognition.py:176/193 均按索引条目 evidence_count 做决策即拿旧值），且 readcache 代际不推进、同实例 `_read` 返回旧 frontmatter。
- **修复**（一行 + 注释）：直写分支落盘并原地更新 `e["evidence_count"]` 后显式 `self._dirty[node_id] = e`——`_DirtyDict.__setitem__` 使 write_gen 单调自增（mdcg.py:805-811）→ ① readcache 代际哨兵失效（同实例 `_read` 不再命中旧 frontmatter）② `_dirty` → flush/close → `_index_log`/compact 快照可见本次写盘。口径与既有 `_sync_edge_entry`（:1664）/ goal 状态写回（:1962）相同；不走 `_stage`（对已存在条目会重复计桶）。未动 trust.py 的 noop 幂等语义（语义锁断言见守卫）。
- **前后对比**（守卫实测，临时库测毕清理）：前——noop 形态 verify 落盘后 write_gen 0→0、_dirty 不含节点、同实例 `_read` 缓存视图 evidence_count=0 vs 盘面 1、close 重开后索引 0 / 盘面 1 漂移固化（compact 计数对账只对账 .md 数量，内容级改写不触发重扫，mdcg.py:1003）。后——write_gen 推进、节点标脏入增量日志、缓存视图即读盘上真值（=1）、重开后索引与盘面对账一致（均 1）；noop 幂等语义与 verify 返回载荷形态不变，置信度步长（+0.05/−0.15）与降级门槛行为不变。
- **守卫**：新增 `md_cg/test_verify_dirty_reconcile.py`——三段会话（S1 预置验证态并 close 落快照 → S2 noop 形态 verify → S3 close 重开对账）；红项 8 个（cf3-cf6/wk3-wk6：写代际不推进/未标脏/缓存视图陈旧/重开索引漂移），语义锁 10 项（noop code/changed=False、返回载荷、置信度、验证态保持、未误降级）修前修后均绿。实现过程一次修正如实注明：初版单会话双 verify 的重开对账项被 close() 无快照时的 rebuild 全量扫描兜底掩蔽，改为「先有快照再 noop 会话」三段形态即如实见红。修前 PASS 10 / FAIL 8 → 修后 PASS 18 / FAIL 0。已注册 verify_linux.sh。**本会话实跑：PASS 18 / FAIL 0**。

### F3（N74，medium）discard 摘索引走 _unstage 落 tombstone：幽灵条目不再固化进快照

- **where**：缺陷时点 `md_cg/branches.py:261`（单行 `(cg.index.get("nodes") or {}).pop(nid, None)`）；修复落点现行 `md_cg/branches.py:261-276`（本会话读核：`_unstage` 调用 + 防御式回落 + 注释在位）
- **问题**：discard 摘除分支索引只 pop 内存、不写删除记录（未走 `_unstage`），且其内部 flush（branches.py:262-267 缺陷时点）把 fork 阶段仍在 `_dirty` 的分支条目作为 **upsert 持久化**进 `_index_log`；close 时 compact 的计数对账（盘面 .md 数 2 ≠ 索引节点数 3）触发重扫，`_apply_log(self._scan_nodes())`（mdcg.py:1010-1012）把已物理移走（publish 至 `_branches/br1/`）的分支 upsert **叠回干净扫描之上**——幽灵条目连同目录指纹固化进 `_index.json` 快照，此后重开指纹校验放行、幽灵永久存活（get()=None、检索候选白跑、dangling 不归零）。对照正确实现：forget 移文件后调 `_unstage` 落删除记录（mdcos.py forget 注释明载同构坑）。
- **修复**（最小改动）：摘除改走 `cg._unstage(nid)`（getattr 防御式获取，异常/不可用时回落原 pop，鸭型 cg 兼容）——`_unstage`（mdcg.py:1971-1989，本会话读核）= pop 内存 + 桶计数减一 + `_dirty[nid]=None`（tombstone）+ 立即 flush，与 forget 的正确模式同口径。修复后时序：fork 阶段 upsert 被 tombstone 覆盖（重放按序 pop）→ discard 尾部 flush 只落 branch_summary 等合法 upsert → close 的 compact 不再把幽灵叠回。未动 discard 其余语义（必填标记校验/权限/冷归档/审计）。
- **前后对比**（守卫实测，临时库测毕清理）：前——discard 返回时 `_index_log=[('mem_base@br1','upsert'),('branch_summary_br1','upsert')]`，close 重开后索引节点含幽灵 `mem_base@br1`（get=None）。后——discard 返回时日志含该 nid 的删除记录（tombstone），重开后索引节点=['branch_summary_br1','mem_base']，索引与盘面 .md 集合对账一致，主支与教训节点完好可读，分支文件冷归档物理保留，返回载荷形态不变。
- **守卫**：新增 `md_cg/test_branch_discard_tombstone.py`——三段（主支入库落快照 → fork→discard（fork 后不 flush，忠实保留前置）→ close 重开断言）；红项 3 个（B2 未落 tombstone / B3 重开后索引含幽灵 / B5 集合对账不一致），语义锁 7 个修前修后均绿。修前 PASS 7 / FAIL 3 → 修后 PASS 10 / FAIL 0。已注册 verify_linux.sh。**本会话实跑：PASS 10 / FAIL 0**。

### F4（N73，medium）read_jsonl_tail 返回真实 EOF + 四缓存水位改存消费位置：尾窗并发 append 不再重复并入

- **where**：`md_cg/fsutil.py:227-259`（read_jsonl_tail，现行 def :228，本会话读核）× mdcos 四增量对账缓存（`_pid_status` 现行 :1708、`_inbox_phash_index` :1744、`_inbox_records` :1776、`_decisions_records` :1807；水位行现行 :1739/:1771/:1802/:1833）——缺陷时点四缓存定义 :1708/:1739/:1768/:1793；根因配套缺陷时点 fsutil.py:228（无界 yield 读到 EOF）+ 水位存回 stat 时刻旧值
- **问题**：四缓存均 stat 先于读、水位存回 **stat 时刻的 size**，而 `read_jsonl_tail` 读到真实 EOF——跨进程 append 恰落在 stat 与 read 之间时，新记录本轮已并入但水位偏小，下一轮增量从旧水位**重读同一条**：列表型缓存（`_inbox_records`/`_decisions_records`）重复并入且**进程内永不自愈**（稳态 size 只增不触发全量重建）→ review_list 待审清单重复 pid、review_stats records/noop 计数虚高翻倍、`_cascade_dedup` 对同一兄弟提案连写两条 reject 裁决（mdcos.py:1982-2001 快照在循环前取、写 decisions 后不刷新——读码推演成立）。write 区同对象条目（缺陷时点 :1786-1789）为同一缺陷，随本修复一并消解。
- **修复**：① `read_jsonl_tail` 由无界 yield 生成器改为一次性读到 EOF 后返回 `(解析记录列表, offset+读到的字节数)`——end_offset 是真实消费位置（窗口内并发 append 的字节已计入）；坏行容错、ends_mid_line 补 \n 语义不变；文件不存在返回 `([], None)`。docstring 明载「读后再 stat 不可取：re-stat 可能大于已消费位置，下轮跳过未读记录（丢账比重复更糟）」。② 四缓存统一：full 路径 `read_jsonl_tail(path, 0)`、增量路径 `read_jsonl_tail(path, 水位)`，水位一律存 end（end 为 None 时回落 stat size），mtime 仍存读前 stat 值。零解析快路径与 size 回退全量重建兜底不变。全库 grep 证实 `read_jsonl_tail` 仅 mdcos.py 一处导入、四处调用——签名变更无其他消费方。
- **前后对比**（守卫实测，临时库测毕清理；注入法=monkeypatch 拦 mdcos 模块级名字，目标函数被调（=stat 已发生）时先 append 一条再委托原函数，等价真实交错）：前——decisions 装载后 stat 前注入 p3、读窗内注入 p4 → 盘上 3 条而 review_stats records=4、noop=4（虚高），第三次调用仍 4（自愈=False）；inbox 同窗后 review_list 中同一 pid 出现 2 次；首装全量路径同形翻倍。后——窗口轮 records==盘上且下一轮/第三次调用不变（4==4、3==3）、review_list 无重复 pid、noop 计数与盘上一致；`_pid_status` dict 后写覆盖免疫（proposals==盘上去重 pid 数）语义锁保持。
- **守卫**：新增 `md_cg/test_tail_watermark_race.py`——四场景（A decisions 尾窗 / B inbox 尾窗 / C dict 覆盖免疫锁 / D 首装全量路径同窗）；红项 6 个（A3/A4/A5/B3/B4/D2，旧代码实跑红），A1/A2/B1/B2/C1/D1 基线/窗口轮/免疫锁修前修后均绿。修前 PASS 6 / FAIL 6 → 修后 PASS 12 / FAIL 0。已注册 verify_linux.sh。**本会话实跑：PASS 12 / FAIL 0**。

### F5（N76，medium）links save 原子写 + _locked 全程互斥：并发连接变更不再丢更新/撕裂

- **where**：缺陷时点 `md_cg/links.py:85-99`（save 固定临时名段）；修复落点现行 `md_cg/links.py:85-118`（save 改 `atomic_write` + 新增 `_locked` 上下文管理器，本会话读核）× 六变更原语五处调用点（现行 :333 handshake、:360 observe、:433 `_transition`（覆盖 degrade/isolate/withdraw）、:446 promote、:489 decay_all）
- **问题**：连接层全部变更是 load→改内存→save 的读-改-写，无任何跨进程锁；save 用固定临时名 `p+".tmp"`（违反 `fsutil.atomic_write` 以 mkstemp 唯一临时名规避的已实名反模式，fsutil.py:6-8 明载「第二写者截断第一写者内容」）——并发下后写者整文件覆盖前写者（**丢一方信任更新，20/20 实证**），两写者互踩同一 .tmp 产生内容交织（audit 超单写者合法上限实拍）；`_publish` 只捕 PermissionError，tmp 已被另一写者 rename 走时抛 FileNotFoundError 直接外抛（缺陷报告读码成立、当时未实证，本轮守卫红跑实际触发）。
- **修复**：① save 废除固定临时名，改走 `fsutil.atomic_write`（mkstemp 唯一临时名、同目录 rename 原子替换、失败清理；POSIX 下 mkstemp 天然 0600 等价旧 chmod 兜底）。② 新增 `_locked`：以 `FileLock(links_file(path))` 为 load→mutate→save 全程连接级互斥（既有基建，锁文件=连接文件+".lock"，超时/严格性沿用 FileLock 默认 10s/best-effort——与全库写路径口径一致）；五调用点各自包住完整读-改-写段。③ save 不自持锁（FileLock 不可重入，docstring 明载「调用方持锁」口径；外部直接调 save 单次写自身仍原子）。
- **前后对比**（守卫实测，临时库测毕清理）：前——（a）双线程 Barrier 对齐 save 点并发 observe 不同连接，双方都返回 ok=True 而终态一方 evidence_count=0（静默丢更新，20/20 同形）；（b）同窗并发下 `_links.json` 内容交织致 load 解析失败两连接全丢，且实拍 publish 抛 FileNotFoundError（WinError 2——读码分支实证）；audit 侧互踩可超限。后——两写者 RMW 完全串行（对齐窗自然消失，barrier 3s < FileLock 10s 无死锁）；终态两连接 evidence_count 均 1、audit 恰为顺序重放基线、跨原语（observe×degrade）两方效果都幸存、文件恒可解析、固定临时名不再出现；单写者路径行为零变化（序列化格式不变）。
- **守卫**：新增 `md_cg/test_links_concurrent_write.py`——复现口径=缺陷报告 Barrier 对齐双线程（monkeypatch save 为对齐版，BrokenBarrierError 捕获放行）；S1 observe×observe（静默丢更新红项 + audit 顺序重放对照基线 + 固定 .tmp 不再出现）、S2 observe×degrade 跨原语家族。红证跑法：`git stash push -- md_cg/links.py` 后跑守卫 PASS 4 / FAIL 5（含 FileNotFoundError 实拍、撕裂两连接全丢、丢降级），stash pop 还原修复后 PASS 9 / FAIL 0。实现过程一次基线修正如实注明：audit 基线初版硬编码 2 是错的（未声明版本会多记一条），改为顺序重放对照动态测定（=3）。已注册 verify_linux.sh。**本会话实跑：PASS 9 / FAIL 0**。

---

## 三、发现明细（四区 confirmed 19 / 整项剔除 3）

以下每项「证据」为上游复核会话的读码+实跑（材料原文，复现均在临时库/临时环境、测毕清理）；标注★者本报告会话另做了现行代码读核。

### recent 区（3 confirmed：2 新 + 1 legacy 复测；1 rejected）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N72★ | readcache.py:57-97 × consolidate.py:678 等 14 读点 | high | 读缓存默认开后治理写面读-改-写未同步 direct_read（详 F1） | 临时库真实函数复现：A 建节点装载缓存→B 经 `_write_node(durable=True)` 落盘修正→A 执行 recalibrate → 默认开组 B 幸存=False、`MDCG_READ_CACHE=0` 对照组幸存=True；crosscheck.rollback 的 write_id 防护所验 fm 出自缓存不构成防护；读点实为 14 处而非候选所列 7 处 | **已修**（F1） |
| N73★ | mdcos.py:1708/1739/1768/1793 四缓存 + fsutil.py:228 | medium | 尾部增量缓存水位存读前 stat 旧值致窗口 append 重复并入、进程内永不自愈（详 F4） | monkeypatch 注入 stat→读窗口内并发 append（等价真实交错）：盘上 3 条而 records=4、noop=4，第三次调用仍 4；inbox 同窗后 review_list 同 pid 出现 2 次；受损面=review_stats/review_list/审计口径（`_pid_status` dict 后写覆盖不受损） | **已修**（F4；write 区 :1786-1789 条目同对象随修消解） |
| v8 N71★ | mdcos.py:2157-2173（现行 :2178 读 / :2190 写） | low | 【留档复测·维持 v8 留档不新报】merge 裁决分支无互斥读-改-写仍成立 | 确定性交错复现（monkeypatch MdCGOS.get 在 cg1 首读返回前让 cg2 完整执行 merge）：B 先 merge ok=True、A 后 merge ok=True，盘上含甲增量=True 含乙增量=False、decisions 两条均 accepted（双 ok+丢一方）；git show 9102df8 证实第 7 轮该分支唯一 hunk 为收尾 upsert 化未加锁。与本轮 N72 独立：merge 基底走 self.get 直读盘（mdcg.py），无缓存陈旧成分，纯无互斥竞态 | 留档维持（见第五节） |
| ~~v7 N50 形态~~（rejected） | mdcos.py:951/968 | — | **证伪剔除**：N50「同节点重复 open+parse 税」形态已被第 7 轮读缓存默认开消除 | 40 节点临时库（带 tags+edges）连续两次同参 search_rrf：`MDCG_READ_CACHE=0` 对照=87 次节点 open/查询（重复税形态）；默认开第 1 次 7 次（装载）、第 2 次 `_read` 面 0 次；稳态残余 5 次全部来自 `_path_graph` 种子 get 直读——盘上真值设计语义、每次查询恒定量与库规模无关。备注：原证据「节点文件 open 降为 0」仅对无边库成立，不改变 rejected 处置 | 剔除（残腿见 v7 N50 残腿条目） |

### retrieval 区（6 confirmed：4 新 + 2 legacy 复测；0 rejected）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N77★ | writepipe.py:436-437（全仓唯一失效点）；mdcos.py:1264/1407（query 缓存读写）；mdcg.py:1196-1580（add 无失效） | medium | query 热缓存失效缺口：直接写 API（add / review_decide 的 accept·edit·merge）全部不失效——`MDCG_HOTCACHE=1` 下写后 ≤300s 内 search_rrf（含生产 mdcg_recall→recall 链）返回写前陈旧结果且仅带 cached=True 标记无陈旧告警 | 实跑复现（MDCG_HOTCACHE=1）：首查 ids=[nodeA]→add(nodeB)+flush 后再查 cached=True 仍=[nodeA]、invalidations=0，新实例真值=[nodeA,nodeB]；生产链 e2e：propose+review_decide(accept) ok=True 后同实例 recall 仍 [nodeA]；grep 全仓源码唯一 `_hc.invalidate` 在 writepipe.py:437（仅 op=='write' 走 default_pipeline）；默认 MDCG_HOTCACHE 未设不挂载（opt-in），开即错 | 留档（medium，暴露面窄于默认路径项） |
| N78★ | hotcache.py:61-62（_ENV_SWITCHES）；mdcg.py:364（MDCG_CN_GRAMS 每次调用读 env） | medium | query 缓存键进程级开关快照漏 MDCG_CN_GRAMS：运行期翻转后同 query 命中另一口径缓存（静默错答）；同型暴露：SCORE_MODE 模块级属性切换同样不在键内 | 实跑复现：首查（默认开）ids=[fullnode, gramnode]→同进程设 MDCG_CN_GRAMS=0 后同 query 再查 cached=True 仍旧值，新实例（同开关）真值=[fullnode]——gramnode 系另一口径陈旧结果；`_ENV_SWITCHES` 只枚举 5 个 env 名确认缺登（与 2026-09-24 补 env_switch 键的修复同类漏登） | 留档（medium，同 opt-in 前提；修复=补登一行） |
| N79★ | mdcg.py:2648-2649（S3 提前记，现行 :2649）+ :2930-2931（_emit 再记，现行 :2931） | low | S3 扩散层 record_access 双记：每个 S3 tier 查询 access_log 落两条同 ids 同 tier 记录、access_counts 翻倍落盘；且提前记录发生在 _emit 的 S5 降权重排之前，记录集可能与最终 results 不一致 | 实跑复现（MdCG 基类 + 双开关开）：单次 search tier=T2b_spread_activation，access_log 恰 2 条完全相同记录→该批节点各计 2；对照开关关走 T2_global_like 仅 1 条。暴露面补充：S3 段仅存在于基类 MdCG.search，生产 MdCGOS.search 覆写（mdcos.py:733）不含 S3——生产 MCP 检索不经此段，暴露面为直接使用基类且显式开 S3 的调用方 | 留档（low；修复=删提前记录一处） |
| N80★ | mdcg.py:2454（现行 :2455）+ :2134（get 同型裸调）；对照 _read 契约 :2146-2150 | low | P2-20 越界处置契约不一致：_read 对越界 path 返回 (None,None) 跳过，但 MdCG.search 负层段与 MdCG.get 把 `_node_disk_path` 放 try 外且只捕 OSError——索引被污染（P2-20 威胁模型，快照无完整性校验）时一条越界条目使基类整库检索 / get 以 ValueError 崩溃而非降级跳过 | 实跑复现（投毒 index['nodes'] 两条 `../escaped*.md` 条目）：`_read(bad)`→(None,None)（契约内）；基类 search→ValueError 且栈顶 _node_disk_path raise（:1188）；**生产暴露面实测修正**：同投毒下 MdCGOS.search/search_rrf 均 ok（负层经 _neg_coverage→self._read 优雅跳过），生产可达的是 get——op=get 与 mdcg_get 工具均 cg.get(nid) → ValueError 直达调用方 | 留档（low，触发前提为索引被污染） |
| v7 负层留档复测★ | mdcos.py:718-730（已覆盖）；残余 mdcg.py:2917-2921（现行 :2918-2924 _emit 尾部裸 open ×3） | low | 【v7 留档复测·**IO 已被覆盖，审计口径未消**】残余有二：① _emit 尾部对 covered_neg[:3] 裸 open 直读（不经 _read 不入读缓存，每查询恒 ≤3 次，S5 开启另有 ≤10 次同型——读码确认未实跑计数）；② scanned 审计口径盲区——负层读取不计入 meta.scanned | 复测实跑（60 knowledge+8 rejected，包装 nodefile.loads 计数）：首查 loads=71（60 全池+8 负层+3）；第 2 查同 query 增量恰 3 次=covered_neg[:3] 裸 open；负层 8 次读经 self._read 全部缓存命中（增量 0）——IO 覆盖结论成立；include_neg False/True 的 meta.scanned 均=60（首查真实多读 8 个文件但 scanned 不变），盲区原样存在 | 留档（残余两项，low） |
| v7 N50 残腿★ | mdcos.py:979（_path_graph 种子 self.get，现行 :979）+ mdcg.py:2130-2142（get 裸 open） | low | 【v7 留档复测·**_read 系重复读已覆盖，get() 种子读未覆盖**】四路中经 self._read 的部分（lexical 全池、entity 重读、graph 邻居读）已被读缓存覆盖（稳态第 2 查增量 0）；唯一残余：≤5 个种子经 MdCG.get 裸 open+loads（读缓存不覆盖），每查询固定 ≤5 次（graph 零产出也在边循环前发生） | 复测实跑（40 节点含 tags/edges）：首查 loads=45（lexical 40+entity 首见 5）；第 2 查增量恰 5 次、结果 ids 逐位一致；严格归因（patch MdCGOS.get 计数+栈采样）：5 次全部来自 search_rrf→_path_graph:979 self.get→mdcg.py:2134-2137 裸 open；`_path_entity`/`_path_graph` 邻居均 self._read（增量 0）——v7 遗留表「读缓存默认开后自动受益」假设对 get 腿为假 | 留档（残腿，low；修复=种子腿改走 _read_many 即并入缓存） |

### write 区（5 confirmed：4 新独立 + 1 同对象重复条目；1 rejected）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N74★ | branches.py:261 | medium | discard 幽灵固化：摘索引只 pop 内存不写 tombstone，内部 flush 把 fork 阶段分支条目作为 upsert 持久化，close 时 compact 重扫+日志重放把已移走节点叠回——幽灵固化进 _index.json 快照，重开指纹校验放行、永久存活（详 F3） | 独立复现（系统临时区临时库，测毕清理）：discard 返回时 _dirty={}（无 tombstone）、_index_log=[分支 upsert, summary upsert]；盘面 LAYERS 内 .md 计数 2，close 计数对账 2≠3 触发重扫叠回；重开后索引含 mem_base@br1 且 get(mem_base@br1)=None | **已修**（F3） |
| N75★ | mdcg.py:3166-3170 | medium | verify() 直写分支不置 _dirty：noop 形态写盘对索引增量日志与读缓存代际完全不可见（详 F2） | 独立复现 ×2：set_verification(verified) 并 close 后重开，verify(n1,'第二次外部证据','confirmed') 返回 confirmed、verification.code=noop/changed=False；盘面 evidence_count=1/confidence=0.65，重开后索引 0/None——漂移固化实拍（计数对账只对账数量不触发重扫）；readcache 子主张同库复现：_read 缓存视图 0 vs _read_uncached 盘面 1 | **已修**（F2） |
| N76★ | links.py:85-99 | medium | 连接层全部变更 load→改→save 无锁 + save 固定临时名：并发丢一方信任更新（20/20）、互踩 .tmp 内容交织（audit 超上限实拍）、FileNotFoundError 分支（读码成立当时未实证）（详 F5） | 独立复现：双线程 Barrier 对齐 save 点并发 observe → 20/20 轮静默丢一方更新（终态一方 evidence_count=0、无异常）；双 Barrier 对齐 save 与 publish → 终态 audit=3 条超单写者合法上限 2——两写者共享同一 .tmp 交织写的直接证据；fsutil.py:6-8 明载固定临时名反模式 | **已修**（F5） |
| （N73 同对象重复条目） | mdcos.py:1786-1789 | low | 审核队列水位存旧值致重复并入（review_list 重复 pid、review_stats 翻倍、_cascade_dedup 双 reject）——与 recent 区 N73 为**同一缺陷对象**（同根因同修法），随 F4 一并消解 | 独立复现（双实例，注入等价并发落窗）：A propose 装载水位 249 → B propose(n2) → 读前注入第三实例 propose(n3) → 第一次并入 n2、n3 但水位存 498（读前 stat size）→ 第二次从旧水位重读 n3：缓存 4 条 vs 盘面 3 条；二次后果维持读码推演定性（本轮未单独实跑，推演依据本轮读核成立） | **已修**（随 F4 消解，不重复占坑） |
| N81★ | mdcos.py:2239（现行 :2258） | low | deletions_log（restore 强制检查闸门与 is_tombstoned 依据）经 append_jsonl 裸追加无 strict 锁——并发 forget 交错丢 tombstone 记录，丢失使已删节点绕过「恢复时删除检查」被无 force 恢复 | 读码+多进程压测（系统临时区临时库，测毕清理）：forget 全程无 FileLock、裸追加；fsutil.py:189-197 文档自述 Windows CRT _O_APPEND 两步交错丢记录；对照组 _record_decision 因「裁决记录不能丢」专门用 strict FileLock（:1950-1951）；本轮压测复现（6 进程×40 条、每条 ~250B 加大窗口）：期望 240、完整解析 232、**丢失 8**（坏行 0=覆盖式丢失）；restore 删除检查闸门按 read_jsonl(deletions_log) 命中判定，tombstone 丢失即无 force 放行恢复 | 留档（low；修复=改走带锁 ShardedLog 或 FileLock，_record_decision 已有同型模式） |
| ~~v8 N71 重报~~（rejected） | mdcos.py:2157-2191 | — | **剔除（不作为本轮新发现重报）**：与本缺陷同对象的 recent 区复测条目已确认维持留档处置；按「已修项/留档勿重复报」纪律不重复立项 | 本轮读核现状与 v8 留档时未变：merge 分支仍是 self.get→内存拼接→_write_node 定向 upsert 的无互斥读-改-写；FileLock 仅罩 inbox/decisions 追加不罩节点读写窗口 | 剔除（留档维持，见 recent 区条目） |

### legacy 区（5 confirmed：全部为历轮留档复测；1 rejected）

| # | where（缺陷时点） | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| v8 N62★ | tokens.py:410 + mcp_server.py:3468（均现行读核在位） | medium | 【留档复测成立】租户绑定被 env 覆盖且静默 + 注册表 clearance_cap 夹紧从未接线（多租户密级上限失效）：verify_token 的 tenant 形参直取 env（MDCG_TENANT），`tenant=tenant or rec.get("tenant")` 使 env 值优先于令牌记录绑定且无一致性校验（对照 MDCG_CLEARANCE 有冲突告警而 MDCG_TENANT 静默）；TenantRegistry.cap_of/principal_for 全仓无生产调用点 | 临时环境复现（临时令牌文件/租户登记表/双根，测毕清理）：issue designer clearance=secret tenant=tenA → 设 MDCG_TOKEN/MDCG_TOKEN_FILE/MDCG_TENANT=tenB 后 `_build_principal()` 输出 err=None、tenant='tenB'（绑定被静默覆盖）、clearance='secret'（cap_of('tenB')='public' 未夹紧）、_resolve_root 解析到 rootB，全程无 stderr 告警；principal.tenant 下游参与可见性过滤（mdcos.py recent_events）；git log 证实 tokens.py v8（批次 40）零改动未在已修清单内 | 留档（medium；建议下轮专项，至少先补 MDCG_TENANT 冲突告警对照既有模式） |
| v8 N71★ | mdcos.py:2157-2191（现行 :2178-2190） | low | 【留档复测成立，=recent 区同对象】review_decide merge 分支读-改-写无互斥（详 recent 区） | 双进程确定性复现（monkeypatch 屏障：_write_node 等双方 read_done 标记齐才放行——栈级等价放大亚毫秒窗口）：child A/B 均返回 ok=true、重开读终态 content 丢失一方载荷、decisions.jsonl 两条均 accepted（假成功留痕）；mdcos.py FileLock 使用点仅 audit 轮转/inbox/decisions 日志，不罩目标节点；第 7/8 轮修复面均未含（F4 只改收尾为定向 upsert 未加锁） | 留档维持（low，坑满顺延） |
| v7 N51★ | mdcos.py:800 + 872-887（现行 :800 + :875-887） | low | 【留档复测成立】T2/T3 截断后重打分仍在位：try_stage 无 scored 形参（内部自调 _score），T2 在 cut_by_relevance 内已对全量 hits 打分、try_stage 对截断后集合再打；T3 对 docs_all 打分截断后又对 picked 重打 | 600 节点临时库实测（计数器包装 MdCGOS._score）：一次 search tier=T3_global_scan、_score 调 4 次、per-call docs=[0,0,600,500]、共打分 1100 文档；结构读核与 v7 留档一致；v7 五修 F1-F5 面与 v8 安全专项均不含，未修 | 留档（low 性能项，坑满顺延） |
| v7 N52★ | postings.py:122-159（现行读核在位） | low | 【留档复测成立、维持窄受益判断】发布表指纹校验仍逐目录 os.stat（无 TTL 化/折叠为索引 mtime 单点校验），S7 启用者每查询恒付 O(目录数) stat；S7 仍只接在父类 MdCG.search（双开关默认关），生产覆写 MdCGOS.search 零 postings 引用 | 读核：stale_reason 逐目录 _mtime_ns、逐根级文件 stat、再 stat _index.json，无 TTL/折叠；临时库实测（双开关开、建真快照后）3 次 search → stale_reason 恰调 3 次，单目录库单次走满指纹校验 stat 2 次；postings.py 自 cd9cce0 后未动 | 留档（low 性能项且触发面窄，坑满顺延） |
| v7 N50 残腿（同对象）★ | mdcos.py:979 + mdcg.py:2130-2136（现行读核在位） | low | 【legacy 区同对象记录，=retrieval 区 v7 N50 残腿】稳态每次 search_rrf 恒 5 次种子节点文件 open+parse（不经读缓存）；与 retrieval 区条目为同一缺陷对象，随其处置不重复占坑 | 临时库实测（30 知识+10 add_rejected，builtins.open 计数限临时 root 前缀）：search 同查询 Q1 opens=40/scanned=30、Q2 opens=0；栈级归因稳态每次 search_rrf 仍 5 次 open 全部来自 mdcos.py:979→mdcg.py:2136（MdCG.get 直接 open，不走 readcache 包装的 _read）；四条 _read 腿零残留 | 留档（随 retrieval 区条目处置） |
| ~~负层全读~~（rejected） | mdcos.py:718-725（调用点 :797） | — | **剔除（已被覆盖）**：负层 `_neg_coverage` 走 self._read（构造期默认装配 readcache），同进程稳态零磁盘读，「每查询全量 open+parse」形态不再成立 | 临时库实测（30 知识+10 rejected，默认环境读缓存缺省开）：同查询 search Q1 opens=40/scanned=30、Q2 opens=0；残留仅口径 nit（首查负层读盘 opens=40 vs scanned=30 不计入，但负覆盖结果集已经 meta.covered_neg 无条件披露，mdcg.py:2946）——非缺陷级，不重复报 | 剔除（残余口径 nit 归入 v7 负层留档复测条目） |

### 第 7 轮留档两项复测结论（显式定谳，任务点名记录）

1. **负层全读每查询磁盘 IO（v7 遗留项）**：**IO 面已被第 7 轮读缓存默认化覆盖**——60 knowledge+8 rejected 库实测首查 loads=71（全池 60+负层 8+_emit 尾部 3）、第 2 查增量恰 3 次（即 `_emit` 尾部裸 open），负层 8 次读经 `self._read` 全部缓存命中（增量 0）；**残余未消**：①`_emit` 尾部 covered_neg[:3] 裸 open 每查询恒 ≤3 次（S5 另 ≤10 次，读码确认）②`scanned` 审计口径盲区（负层读取不计入 meta.scanned，include_neg True/False 均=60）→ 残余两项留档（low）。legacy 区对该缺陷原形态的「每查询全量 open+parse」主张同步**整项剔除**（如上表）。
2. **四路召回重复读盘（v7 N50）**：**`_read` 系四腿（lexical 全池 / entity 对 tag 命中重读 / graph 邻居 / chain）已全部被读缓存覆盖**——稳态第 2 查磁盘读增量 0；**唯一残腿未覆盖**：`_path_graph` 的 ≤5 个种子经 `MdCG.get` 裸 open+loads（mdcos.py:979→mdcg.py:2130-2136，不经缓存包装的 `_read`），每查询固定 ≤5 次（graph 路零产出也在边循环前发生）；N50 原「同节点重复 open+parse 税」形态经 `MDCG_READ_CACHE=0` 对照（87 次/查询）vs 默认开（第 2 次 `_read` 面 0 次）**证伪剔除**——v7 遗留表「读缓存默认开后自动受益」的假设对 `_read` 腿为真、对 get 腿为假。

### 占位去重说明（留档清单 2 条，不承载独立缺陷）

- `branches.py:261`（跨区重复条目）：与已选修复目标 N74 为同一缺陷对象（同 where 同根因，证据完全一致），留档槽仅为保持候选清单与输出一一对应。
- `mdcos.py:1786-1789`：与已选修复目标 N73 同根因同修法，随 F4 一并消解，不重复占坑。

---

## 四、验证（门禁结果）

以下「本报告会话实跑」均为本报告写作会话在仓库根目录实际运行（第一手）；「材料口径」为编排/修复会话已跑、本会话未重跑（如实区分）：

| 命令 | 结果 | 来源 |
|------|------|------|
| `python scripts/run_tests.py --jobs 4`（全量） | `===== SUMMARY 179/179 通过，5 跳过（依赖缺失/平台不符） =====`，退出码 0——**与材料口径逐字一致**（尾部 PASS 含 hive.test_wm_progress / hive.test_exec_tools / swarm.tests.test_rust_swarm；179 = v8 基线 174 + 本轮 5 个新守卫套件自动发现入列） | **本会话实跑** |
| `python -X utf8 -m md_cg.test_govern_directread` | `结果：PASS 40 / FAIL 0`（=材料修后绿态；材料红证 34/6） | **本会话实跑** |
| `python -X utf8 -m md_cg.test_verify_dirty_reconcile` | `结果：PASS 18 / FAIL 0`（材料红证 10/8） | **本会话实跑** |
| `python -X utf8 -m md_cg.test_branch_discard_tombstone` | `结果：PASS 10 / FAIL 0`（材料红证 7/3） | **本会话实跑** |
| `python -X utf8 -m md_cg.test_tail_watermark_race` | `结果：PASS 12 / FAIL 0`（材料红证 6/6） | **本会话实跑** |
| `python -X utf8 -m md_cg.test_links_concurrent_write` | `结果：PASS 9 / FAIL 0`（材料红证 git stash 回退旧码 4/5，含 FileNotFoundError 实拍） | **本会话实跑** |
| `python scripts/check_publish_artifact.py`（`npm run gate` 第 2 环单独复现） | R1 凭据/密钥面 **PASS（文件面=0 内容面=0）**、R2 私有数据面 PASS、R3 隐私文本 PASS；**R4 非追踪件面 FAIL 命中=5**——恰为 `md_cg/test_branch_discard_tombstone.py`、`test_govern_directread.py`、`test_links_concurrent_write.py`、`test_tail_watermark_race.py`、`test_verify_dirty_reconcile.py` 五个新守卫（非追踪=29、允许=24）；`VERDICT=FAIL`——与材料门禁输出同因同置，且补全了材料截断的命中清单 | **本会话实跑** |
| `npm run gate`（全链） | 材料口径 FAIL（R4 时序性：5 个新守卫尚未 `git add` 登记追踪面，材料输出被截断仅见 4 文件名）；本会话未重跑全链、仅单独复现 R4 环（上 row）——按守卫自身要求 `git add` 登记后复跑即过（v7/v8 先例），**归编排会话提交前裁决，本报告会话未擅自登记/提交** | 材料 + 本会话部分复现 |
| `cargo test` | **未运行**——Rust 面零改动（本会话 `git status` 证实改动面为 md_cg/ hive/ scripts/，rust/ 与 swarm/ 未动） | 双方一致 |
| `npm test` | **未运行**——TS 面零改动（src/ 与 test/ 未动；遗留项中 TS 文件零涉及） | 双方一致 |

**上游会话已运行、本会话未重跑的验证**（材料原文，如实区分）：五项修复的守卫红证（修前各一跑：govern_directread 34/6、verify_dirty_reconcile 10/8、branch_discard_tombstone 7/3、tail_watermark_race 6/6、links_concurrent_write 首跑 6/3 → 基线修正后 `git stash push -- md_cg/links.py` 旧码复跑 4/5）；F5 的 git stash 回退对照；修复核对 diff（F1 恰 14 处替换+4 行 import、F2 单行+hunk@3168、F3 hunk@258、F4 `--stat` +33/+71、F5 import+save+五调用点）；相关既有套件（test_ccgc 84/0、test_p34_crosscheck 51/0、test_p39_vision_evidence 53/0、test_p6_consolidate 82/0、test_trust 127/0、test_branches 43 通过、test_propose_tail_index 8/0、test_p23_links 58/0、test_readcache_default_on 10/0 等）；md_cg/test_*.py 全目录逐套执行 147 文件全绿（含五个新守卫）。四区复核的全部攻击/竞态复现（临时库+注入法，测毕清理）亦为上游会话执行（材料原文）。

**git 工作树核对**（本会话 `git status` / `git diff --stat` + 逐处读核）：改动面 = **九个修复文件**（md_cg/consolidate.py +13、ccgc.py +5、crosscheck.py +7、vision_evidence.py +7、mdcg.py +7、branches.py +16、fsutil.py +33/-15、mdcos.py +71/-37、links.py +245/-165）+ **scripts/verify_linux.sh**（+3 行注册 5 个新守卫，现行 :21-23）+ **五个新守卫文件**（untracked）——与五项修复一一对应。另有三件非本轮修复组成的改动，如实注明：① `hive/test_exec_tools.py` +22——本轮全量回归超时归因后的 K2 守卫加固（reader 线程 daemon 化防进程挂死、Windows 读者瞬态句柄把 replace 50 次重试耗尽时计数不中断、新增「写成功 ≥1」断言防压测全饿死时 K2 空转），diff 内注释自证日期 2026-09-25，非五项修复触点；② `md_cg/whitebox_kb/wisdom/code_solidified.json`——材料明示为本轮之前已存在的工作区变更（本会话 diff 读核：仅删除一行缓存命中指纹记录）；③ 一件与工程无关的非追踪图片文件（R4 允许清单内，不涉本轮改动）。未做任何 commit。

---

## 五、遗留清单（11 项：medium 3 / low 8）

### medium（3 项）

| # | where（现行，本会话读核） | 问题 | 留档理由 / 处置线索（材料口径） |
|---|------|------|------------------------------|
| N77 | writepipe.py:437（全仓唯一 `_hc.invalidate`）；mdcos.py query 缓存读写 | query 热缓存失效缺口：add / review_decide（accept·edit·merge）不失效，`MDCG_HOTCACHE=1` 下写后 ≤300s 内 search_rrf/recall 返回写前陈旧结果且仅 cached=True 无陈旧告警 | 以 MDCG_HOTCACHE=1 opt-in 为前提（默认不挂载），暴露面窄于默认路径入选项；修复方向明确（add/review_decide 写点补 _hc.invalidate），可下轮带 |
| N78 | hotcache.py:61-62（_ENV_SWITCHES）+ mdcg.py:364（CN_GRAMS 每调用读 env） | query 缓存键进程级开关快照漏 MDCG_CN_GRAMS（及 SCORE_MODE 模块级切换）：运行期翻转开关后同 query 命中另一口径缓存（静默错答） | 同 opt-in 前提（缓存挂载方才有键问题）；修复为 _ENV_SWITCHES 补登一行，可下轮顺手带 |
| v8 N62 | tokens.py:410 + mcp_server.py:3468（均现行在位） | 租户绑定被 env 覆盖且静默 + clearance_cap 夹紧从未接线（多租户密级上限失效）；tenB(cap=public) 借 MDCG_TENANT 即可以 secret 密级读 rootB | medium 安全项，但暴露需多租户部署形态（令牌文件+租户登记表），且接线 cap_of 含行为决策（夹紧 vs 拒绝），修复风险高于本轮所选五项；建议下轮专项（至少先补 MDCG_TENANT 冲突告警，对照 MDCG_CLEARANCE 既有模式） |

### low（8 项）

| # | where（现行，本会话读核） | 问题 | 留档理由 / 处置线索 |
|---|------|------|---------------------|
| N79 | mdcg.py:2649 + :2931（S3 提前记/_emit 再记） | S3 扩散层 record_access 双记：access_log 两条同 ids 同 tier、access_counts 翻倍落盘；提前记录在 S5 降权重排前，记录集可能与最终 results 不一致 | 生产 MdCGOS.search 覆写不含 S3（暴露面仅显式开 S3 的直接引擎调用方）；修复=删提前记录一处，改动面小可下轮带 |
| N80 | mdcg.py:2455（现行）+ :2134（get 裸调） | P2-20 越界处置契约不一致：索引被污染时 MdCG.get（生产 op=get/mdcg_get 直达）以 ValueError 崩溃、基类 MdCG.search 整库检索崩溃而非按 _read 契约 (None,None) 降级跳过 | 触发前提为索引被污染（P2-20 威胁模型）；修复=两处调用移入 try/捕 ValueError 降级，可下轮带 |
| N81 | mdcos.py:2258（现行；缺陷时点 :2239） | deletions_log 裸追加无 strict 锁：并发 forget 交错丢 tombstone（240 条丢 8 条实拍），丢失使已删节点绕过恢复删除检查被无 force 恢复 | 修复=forget 的追加改走带锁 ShardedLog 或 FileLock（_record_decision :1950-1951 已有同型模式） |
| v8 N71 | mdcos.py:2178 读 / :2190 写（现行；缺陷时点 :2157-2191） | review_decide merge 分支读-改-写无互斥：并发 merge 同目标丢一方内容而双裁决 accepted（假成功留痕）；本轮三区复测均成立（确定性交错双实拍） | low 留档项坑满顺延；修复=merge 段加节点级 FileLock（与 N72 相互独立：merge 基底直读盘，无缓存陈旧成分） |
| v7 N51 | mdcos.py:800 + :875-887（现行） | T2/T3 截断后重打分仍在位（600 节点库一次 search 打分 1100 文档：600+500 双打分） | low 性能项坑满顺延；修复=try_stage 增加 scored 形参透传 |
| v7 N52 | postings.py:122-159（现行） | 发布表指纹校验仍逐目录 os.stat（无 TTL/折叠），S7 启用者每查询恒付 O(目录数) stat；S7 仍只在父类 MdCG.search（双开关默认关） | low 性能项且触发面窄，坑满顺延 |
| v7 N50 残腿 | mdcos.py:979 + mdcg.py:2130-2136（现行） | 稳态每次 search_rrf 恒 5 次种子节点文件 open+parse（MdCG.get 裸 open 不经读缓存，graph 零产出也发生） | 本轮复测证明 v7 遗留表「读缓存自动受益」假设对 get 腿为假；修复=种子腿改走 _read_many 即并入缓存 |
| v7 负层残余 | mdcg.py:2918-2924（现行） | ① _emit 尾部 covered_neg[:3] 裸 open 每查询恒 ≤3 次（S5 另 ≤10 次）；② 负层读取不计入 meta.scanned 的审计口径盲区 | low 性能/口径项坑满顺延（IO 主面已被读缓存覆盖，见第三节复测专节） |

**优先级建议**（据材料处置线索汇总）：N77/N78 为「opt-in 开关面的缓存正确性」同族（修复面小：补失效挂点 + 补登键），建议同轮速收；v8 N62 多租户接线需先做夹紧语义设计决策（或先补 MDCG_TENANT 冲突告警最小止血）；N79/N80/N81 均为单点小修；v7/v8 性能与竞态留档项（N51/N52/N50 残腿/N71/负层残余）坑满顺延。

---

## 六、纪律合规声明

1. **定位先读索引**：本报告写作前已读 WORKSPACE_INDEX.md（按职责定位 docs/eval 为报告归域、md_cg/ 为记忆系统本体、scripts/ 为工程管线），未做全盘浏览。
2. **结论引证**：所有修复与缺陷均引 path:line（缺陷时点快照行号，本轮 mdcg.py/mdcos.py 行号漂移处另注现行行号并经本会话读核——F2 修复行材料称 :3175、本会话读核实为现行 :3177，以读核值为准）；第三节证据要点摘自任务输入的四区复核材料（上游会话读码+实跑）；第四节「本会话实跑」八项为第一手命令与输出原文（全量回归、五守卫、R4 复现、git 核对），材料口径与未跑项（cargo/npm test/gate 全链/红证重跑/攻击复现重跑）均如实标注来源，无一冒充。
3. **修复落盘核验**：五项修复的现行代码本报告会话逐处读核——F1 四文件 14 处 direct_read + 4 行 import（grep 计数 6/2/3/3 逐一列出）、F2 mdcg.py:3171-3177、F3 branches.py:261-276（_unstage 调用+防御回落）、F4 fsutil.py read_jsonl_tail 返回 (records, end) + mdcos 四缓存水位行（:1739/:1771/:1802/:1833）、F5 links.py save→atomic_write + `_locked` 五调用点（:333/:360/:433/:446/:489）；11 项遗留的现行现状亦逐一 grep/读核确认（第五节「现行」列）；verify_linux.sh 5 守卫注册（:21-23）读核在位。
4. **安全测试纪律**：材料中全部复现均在临时库/临时环境（tempfile 系统临时区），使用哑令牌与哑数据（哑租户标识以 tenA/tenB 等占位描述），不触真实数据目录与真实令牌，测毕清理（材料逐项声明）；本报告会话只运行守卫套件与门禁检查器，未做任何攻击/竞态复现。
5. **已修项不重复报**：本轮 legacy 项全部为历轮留档的**复测记录**（v7 N50/N51/N52、v8 N62/N71），复测结论为「维持留档」或「部分剔除」，不作为新缺陷重复立项；v8 N71 在 write 区的重报条目按「留档勿重复报」纪律剔除；第 1-8 轮已修清单（见 docs/eval/缺陷挖掘_自主迭代_v1~v8.md 第二节）未再报；F1 与第 7 轮 backfill/govern 的 direct_read 为**补齐漏装面**（第 7 轮修复未覆盖的 14 处），非重复修复。
6. **隐私脱敏**：全文只写仓库相对路径；材料的本机绝对路径（盘符/用户目录/临时区路径）一律转录为「系统临时区临时库/临时环境」；哑令牌/哑密钥样例以占位描述，无任何 key/token 字面量；环境变量名与标识符（MDCG_HOTCACHE/MDCG_TENANT 等）为源码标识符非凭据值，为证据必需而保留；定稿前经机械扫描复查（见下方自查）。

---

## 七、跳过项及原因

- **cargo test 未运行**：Rust 面零改动（本会话 git status 证实 rust/ 与 swarm/ 未动）。
- **npm test 未运行**：TS 面零改动（src/ 与 test/ 未动，本轮缺陷与修复全部在 md_cg/python 面）。
- **`npm run gate` 全链未于本会话重跑**：材料口径 R4 时序性 FAIL（5 个新守卫文件 untracked、非追踪=29>允许=24，材料输出截断）；本会话以第 2 环 `scripts/check_publish_artifact.py` 单独复现同因同置（R1/R2/R3 全 PASS、R4 命中恰为五个新守卫）——按守卫自身要求 `git add` 登记后复跑即过（v7/v8 先例），**处置归编排会话提交前裁决，本会话未擅自登记/提交**。
- **守卫红证未在本会话重跑**（含 F5 的 git stash 回退旧码跑法）：上游修复会话已跑且逐项留痕（修前红/修后绿数字见第二节），本会话重跑会反复 stash/unstash 工作树，风险大于收益——以修复落盘读核 + 修后绿态实跑 + 全量回归替代佐证，如实标注。
- **四区复核的攻击/竞态复现未在本会话重复**：上游复核会话已在临时库执行并逐项声明「测毕清理」，本报告会话未重跑（第三节证据来源均标注为材料原文）。
- **N77 生产链 e2e / N79 S5 计数 / N73 `_cascade_dedup` 双 reject 二次后果**：上游材料自身声明的「读码推演成立、本轮未单独实跑」部分，本报告照实转录并保留其定性标注，未升格为实证。
- **git 未提交**：commit 需任务方指令；工作树保持五补丁 + 五守卫 + 守卫注册 + K2 加固原状。

---

*报告依据：任务输入的第 9 轮材料（四区复核 19 confirmed + 3 rejected 明细及证据、五项修复的 where/问题/修复/前后对比/守卫/verified_by、留档未修 15 条中 2 条占位去重 + 2 条跨区重复、门禁结果）；本报告会话的修复落盘读核（五补丁 14+1+1+四缓存+五调用点锚点 + verify_linux.sh 注册）、11 项遗留现行读核（第五节「现行」列）、五套新守卫实跑（40/0、18/0、10/0、12/0、9/0）、全量回归实跑（179/179、退出码 0）、R4 门禁单独复现（FAIL 同因同置且补全命中清单）、git 工作树核对、报告脱敏机械扫描。*

*自查双清单：①材料覆盖——四区 22 条目全部入文（recent 3+1 / retrieval 6 / write 5+1 / legacy 5+1），confirmed 19 与剔除 3 一一对应，去重后 16 独立缺陷（新 N72-N81 共 10 + legacy 复测 6）数目吻合，5 补丁逐项五要素齐全，修 5 + 留档 11 = 16 无多无少，2 条占位去重说明单列，第 7 轮留档两项复测结论显式定谳（专节 + legacy/rejected 双向记录），v8 N71 三区复测与不重报处置如实入文，门禁四项（python/cargo/npm test/gate）与跳过原因一一对应，材料截断的 R4 命中清单经本会话复现补全；②纪律——全部结论引 path:line 或注明材料来源，八项第一手实跑命令与输出在第四节（全量回归、五守卫、R4 复现、git 核对），未跑项（cargo/npm test/gate 全链/红证/攻击复现）无一冒充为已跑，F2 行号材料口径（:3175）与读核值（:3177）的偏差如实注明，工作树三件非修复改动（K2 加固/先前会话 json/无关图片）如实注明，全文无本机绝对路径、无 key/token 字面量（机械扫描盘符/用户目录/token 形态零命中）。*

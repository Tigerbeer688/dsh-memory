# 缺陷挖掘报告 · 自主迭代 v5

- **日期**：2026-09-25
- **范围**（生效条件）：第 5 轮 = swarm 域深挖（新发现 5 项确认）+ 历轮遗留收口（v1/v2/v4 遗留清单中 5 项修复的落盘核验与效果实测）。新发现集中 swarm 消息投递面（收件箱保真/CLI 契约/符号表解析）；遗留修复横跨 compiler（2）/ swarm（1）/ md_cg（2）。
- **总量**：遗留修复 5/5 落盘；新发现候选 5 → 确认 5（零差值）→ 新修 2 → 遗留新增 3
- **严重度分布**（新发现确认 5 项）：high 2 / medium 2 / low 1
- **回归**：全量 `python scripts/run_tests.py` 本会话实跑 **164/164 通过、5 跳过（依赖缺失/平台不符）、退出码 0**——与任务输入「回归全绿」一致；较 v4 的 163 增 1，系本轮新红守卫套件 test_swarm_inbox_delivery 入列（见第六节）
- **行号口径**：任务输入所附新发现行号为缺陷时点快照行号（swarm.rs :759-774/:991-996）；两项已修复者本会话另注修复后现行行号，三项未修者快照行号与现行一致（本会话逐处读核）
- **结构口径**（CCG 六要素）：本报告按仓库惯例组织——功能名（标题）/ 生效条件（范围与口径行）/ 子功能（第二~五节：遗留修复、新发现、新修复、遗留清账）/ 执行与验证方式（第六节：实跑命令与结果）/ 不适用条件（第七节：跳过与未运行项）

---

## 一、执行摘要

本轮做两件事：先收口历轮遗留清单中 5 项（L1 api codegen 错误回流、L2 WAL 验签零容错〔v1 #23 / v2 N4〕、L3 name_checker 分派缺口〔v4 N21〕、L4 mdcg `_read` 越界读〔v1 #3〕、L5 mdcos propose 缺省层错位〔v1 #4〕），全部以本会话探针实测修复效果；再对 swarm 域深挖，确认 5 个新缺陷（N22-N26）。

5 项新发现同属「**消息投递保真族**」：两项 high 直击蜂群消息语义正确性——protocol 拓扑下发给 verifier 的消息被每轮『顺延一轮』直至随蜂群结束消亡（从未投递、从未 ACK、水位永不推进，N22）；同轮同源多条路由在收件箱处互相覆盖（载荷静默丢弃而 WAL 两条事件均已 HMAC 签名落盘，审计留痕与实际投递不符，N23）。两项均本轮修复（swarm.rs 修复 #1/#2），并落盘红守卫套件 test_swarm_inbox_delivery.py（两条判别器场景，本会话单跑 12/12）。余三项在位未修：CLI 静默丢弃 condition_space 卡（N24）、CLI 异常面破坏单行 JSON 契约（N25）、`--symbols` 字符串值含 `,`/`}` 时续读位错位（N26）。

**对输入口径的核验**：输入称「修复遗留 5 + 新发现 5 确认→新修 2」。本会话以 `git diff` 工作树 + 逐处读核 + 探针实跑核对：遗留修复恰 5 项（其中 4 项带「2026-09-25 缺陷 #N」编号注释，L1 无编号注释、按 HEAD/工作树对照归位，见第二节 L1 行）；新发现恰 5 项、其中恰 2 项（N22/N23）落盘修复——与输入口径一致，无多无少。

---

## 二、遗留修复（5 项全部落盘，本会话逐项读核 + 效果实测）

| # | 历轮编号 | 位置（现行） | 修复内容 | 本会话验证 |
|---|---------|------|----------|-----------|
| L1 | v4 F3 收尾（codegen 错误通道回流，无历轮独立编号） | compiler/api.py:182-184 | `result.errors.extend(gen.errors)`——codegen 错误（未支持表达式类型、非法函数名）回流主结果。HEAD 基线 codegen.py 已声明 `self.errors` 但全文件零 append（死通道）、api 从不读取；v4 轮补了 append（codegen.py:510/:550）仍无人读取——「记 error 令编译失败」名不副实。回流后编译如实失败，且第六步终裁的否决分支对 codegen 错误可达（v3 N8 的「否决不可达」在该路径缓解） | 探针：`定义 +（甲，乙）：返回 甲 加 乙。` → success=False，errors 含「L1 名字 '+' 不是合法的 Python 标识符」与「验证单元否决: 编译错误: …」（终裁否决分支实测触发）；对照 `定义 加（甲，乙）：…` → success=True。归位依据：`git show HEAD:compiler/api.py` 第二步仍为旧写法且无 gen.errors 读取，v1/v4 报告均未记此改动，工作树注释自述回流意图——按排除法归本轮（该项无缺陷编号注释，如实声明） |
| L2 | v1 #23 / v2 N4（low） | swarm/rust_swarm.py:116-133（except :128） | WAL 验签器逐行 try/except（ValueError/KeyError/TypeError）：畸形/被篡改行（非 JSON、缺 `"payload":`、缺必填键、hmac 非串）计入 bad 并 continue，不再打崩整轮验签——验签器面对的正是被篡改的 WAL，不得自己先崩。注释标记「2026-09-25 缺陷 #2」 | 探针：手工构造 4 行 WAL（签名不符 1 + 半行截断 1 + 非 JSON 1 + 缺必填键 1）→ 返回 `{total:4, bad:4, all_valid:false}`，无异常（旧实现第二条即 JSONDecodeError 崩出）。全套件内亦经 test_rust_swarm 覆盖运行面 |
| L3 | v4 N21（medium 候选） | compiler/name_checker.py:513-528 | `_check_statement` 补 FUNC_DEF（body 列表/单节点两形态递归）/RETURN_STMT（value 过表达式校验）/CALL_EXPR（函数名 + args 逐个）三分派，注释标记「2026-09-25 缺陷 #3」 | 探针：`若 条件空间 为 伴侣，则 定义 增（）：止 情感权重 于 0.9。` → success=False、errors=〔「L1:C34 在「伴侣」条件空间中情感权重不可超过 0.15，当前值: 0.9」〕、warnings=[]（v4 时点同源零拦截、仅「未处理的语句类型: FUNC_DEF」warning） |
| L4 | v1 #3（medium） | md_cg/mdcg.py:2039-2046 | `_read` 由裸 `os.path.join(self.root, entry["path"])` 改经 `_node_disk_path(entry)` 统一越界校验（P2-20 同口径），越界抛 ValueError → 返回 (None, None)（不可读即不存在）——检索主读路径与 `_emit` 负记忆读取不再可被污染索引带出 root 外文件。注释标记「2026-09-25 缺陷 #4」 | 探针：临时 root 内投毒索引条目 `{"path": "../outside.md"}`（root 外文件真实存在）→ `_read` 返回 (None, None)，不读外部文件 |
| L5 | v1 #4（medium） | md_cg/mdcos.py:3574-3578 | `MdCGSecure.propose` 的层校验缺省由 `kw.get("layer") or "contextual"` 改 `kw.get("layer", "knowledge")`——与基类 MdCGOS.propose 的 `layer="knowledge"` 缺省一致，校验层与落盘层不再错位。注释标记「2026-09-25 缺陷 #5」 | v1 复现脚本 `_repro_verify_3536.py` 实跑：S1（knowledge-only 不传 layer）propose **成功**（v1 时点误拒）、S2（contextual-only）**AccessDenied 拒绝**（v1 时点入队成死提案）——两象限均翻正；S3 等价探针：knowledge-only propose→accept → ok=True；S4 对照：显式 layer="contextual" 落盘 layer='contextual' 一致。（该脚本 S4 段自身有 `_j` 未定义脚本缺陷——S2 被正确拒绝后其行内 import 不再执行所致，脚本问题非产品问题，如实声明） |

---

## 三、本轮新发现（确认 5 项，N22-N23 已修 / N24-N26 未修）

证据口径：**5 项全部经本报告写作者本会话独立验证**（读码 + 运行复现/红守卫实跑；N26 为读码 + 调用环逻辑证明，见该行声明）。

### high（2 项，均已修复，见第四节）

| # | 位置（缺陷时点 → 修复后现行） | 缺陷摘要 | 状态 | 证据（本会话） |
|---|------|----------|------|------|
| N22 | swarm/rust_runtime/src/swarm.rs:759-774 → 现行 :765-776（注释）/:813-833（记账） | protocol 拓扑下发给 verifier 的消息被每轮重复『顺延一轮』，实际永久丢失：round r 把 verifier 自身收件箱搬到 inboxes[r+1]，round r+1 又原样搬到 r+2，直至 rounds+1 随蜂群结束消亡——消息从未投递、从未 ACK、verifier 消费水位永不推进；原注释声称「自身收件箱顺延一轮不丢失」与实现矛盾 | **已修复**（F1） | 红守卫实跑（test_swarm_inbox_delivery 场景②）：3 轮 protocol 蜂群，WAL 发往 verifier 路由事件 3 条，修复后 verifier ACK = 2 条（轮 2、3；**旧实现恒 0**）、消费水位 = 2（**旧实现永不推进**）、复算 3 轮全过 mismatches=0（修复不破坏逐位复算） |
| N23 | swarm/rust_runtime/src/swarm.rs:991-996 → 现行 :172-176（类型）/:801-812（平铺）/:1016-1025（投递）/:520-523（replay） | 同轮同源多条路由在收件箱处互相覆盖：Inboxes 结构为 (round→target→from→(payload,seq))，以 from 为单值 key，同一实例发出的第二条路由 insert 直接覆盖第一条——消息 A 载荷被静默丢弃，但 WAL 中两条事件均已 HMAC 签名落盘为「已投递」，审计留痕与实际投递不符 | **已修复**（F2） | 红守卫实跑（场景①）：实例甲双路由（载荷 11/22）发实例乙，WAL 2 轮 × 2 条 = 4 条留痕（旧代码也过——正是「留痕与投递不符」的留痕侧），修复后乙 `已收消息数` = 2（**旧实现同源覆盖后仅 1**）；WAL 全验签 bad=0 |

### medium（2 项，未修复）

| # | 位置 | 缺陷摘要 | 状态 | 证据（本会话） |
|---|------|----------|------|------|
| N24 | swarm/swarm_cli.py:78-81 | cmd_run 调 make_swarm_config 未传 condition_space：用户在 swarm.json 声明的 G-R2 条件空间卡被 CLI 静默丢弃——既不进生成的 swarm.json，也不触发 Rust 侧「四要素缺一不可、缺失即拒绝运行」的负路由校验（main.rs:351 读 cfg['condition_space']，swarm.rs:155 对 None 返回 Ok(None) 直接放行），执行链的 VM 符号注入（swarm.rs:613-623）随之失效 | 未修复（遗留） | 读核：swarm_cli.py 全文 grep `condition_space` **0 命中**（:78-81 实参表无该项）；rust_swarm.py:31/:46-47 形参与写入在位；main.rs:351-357 + swarm.rs:152-170 校验链、swarm.rs:613-623 注入点逐行读核。旁证：现行绿套件 test_swarm_condition_space.py:57 **直连 make_swarm_config** 传卡（绕过 CLI），故套件全绿而 CLI 缺口在位 |
| N25 | swarm/swarm_cli.py:66-67 | cmd_run 多处异常未捕获，破坏文件头承诺的「stdout 恒为单行 JSON（机器面）」契约：config 不存在（:66 open）、config 非 JSON（:67 json.load）、@file 源缺失（:60 _load_source 的 open）、instances 项缺 id / routes 项缺 from-to（rust_swarm.py:40 `i["id"]`/:43 `r["from"]`/`r["to"]` KeyError）、cargo 失败/超时（rust_codegen.py:61-65 raise RuntimeError、:62 TimeoutExpired，经 rust_swarm.py:71 build_rust_exe 调用——该行在 run_swarm 的 try 块**之外**，v2 F3 的守卫只包 swarm 子进程本身）——harness 收到 Python traceback + 空 stdout 而非结构化错误 | 未修复（遗留） | 实跑复现：`python -m swarm.swarm_cli run --config no_such_config.json` → FileNotFoundError traceback 直出（cmd_run :66），stdout 无单行 JSON；其余四个抛点的代码路径逐行读核（rust_swarm.py:40/:43、rust_codegen.py:61-65、rust_swarm.py:71 与 :78-90 try 块的相对位置） |

### low（1 项，未修复）

| # | 位置 | 缺陷摘要 | 状态 | 证据（本会话） |
|---|------|----------|------|------|
| N26 | swarm/rust_runtime/src/main.rs:141-167（parse_json_scalar） | 标量结束定位 `s[start..].find([',','}'])` 不跳过字符串字面量：`--symbols` 的字符串值内含 ',' 或 '}' 时，end 落在字符串内部——值本身经 parse_json_string 解析正确，但返回的续读位置错位，下一轮按「符号表键须为字符串」（main.rs:92）误报，合法 JSON 被拒且报错文案指向错误原因 | 未修复（遗留） | 读码 + 调用环逻辑证明（本会话）：对 `{"甲":"x,y"}`，标量解析返回 (Str("x,y"), end=串内逗号处)；parse_symbols_json 主循环（main.rs:82-110）跳逗号后 pos 落在 `y` 处，:91 `bytes[pos] != b'"'` 成立 → Err("符号表键须为字符串")——机制逐行核对在位。**运行时复现未做**（需构建独立形态 exe 后构造 --symbols 入参，本轮未跑，如实声明） |

---

## 四、新修复清单（2 项，工作树未提交改动）

### F1（对应 N22，high）verifier 自身收件箱到达轮即消费记账
- swarm.rs:765-776（修复注释）+ :813-833（own_ack_seq_max/has_inbox/max_seq）：删除旧的每轮顺延段；verifier 到达轮的自身收件箱**计入 ACK 与消费水位**（own_ack_seq_max 取该轮自身收件箱各条投递 seq 的最大值），**载荷不进复算执行输入**（verifier 执行输入仍是 primary 输入，混入会破坏逐位复算）——:827 `has_inbox` 与 :828-833 `max_seq` 均并入该记账，ACK/水位如实推进。
- 配套红守卫：swarm/tests/test_swarm_inbox_delivery.py 场景②（判别器：ACK 计数、水位 >0、复算 mismatches=0），本会话单跑通过。

### F2（对应 N23，high）收件箱来源值改列表——同轮同源逐条保留
- swarm.rs:172-176：`Inboxes` 的来源值由 `(String, u64)` 改 `Vec<(String, u64)>`；投递点 :1016-1025 由 insert 改 entry 链 push；实例输入构造 :801-812 把收件箱平铺为 (来源,载荷) 序列并按来源稳定排序（同源内保持全局事件序，单路由场景与旧输出逐字节一致）；replay_wal :520-523 断点续跑路径同步 push。run_round 注入面（:600-608）随之按平铺序列逐条生成「收件箱」JSON 与「已收消息数」如实计数。
- 配套红守卫：同文件场景①（判别器：双路由乙实收 2 条、WAL 4 条留痕、验签 bad=0），本会话单跑通过。

---

## 五、遗留清账（历轮遗留 + 本轮新增）

| 来源 | 修复进展 | 余量（编号沿各轮原口径） |
|------|----------|--------------------------|
| v1 遗留 21+1 部分 | 累计修 #10/#11/#12/#13（v2）、#6/#18/#21/#22（v3）、**#3/#4/#23（本轮 L4/L5/L2）** | **11**：#5 同桶计数半边（low，部分）、#8 trust 审计时间戳（medium）、#9 propagate 帽穿透（low）、#14-#17 scripts 四项（medium 1 + low 3）、#24 condition_space CLI 丢弃（medium，即 N24 同一缺陷——v1 已记录、两轮未修）、#25 report_path 指针错位（medium）、#26 build_rust_exe 路径（low）、#27 聚合数值分歧（low） |
| v2 遗留 14（N5/N11 同路径） | **N4（本轮 L2，与 v1 #23 同一缺陷）** | **13**（去重 12）：N5/N11 verify_runner iter_id 穿越读写（high+medium）、N6 result 非原子写、N7 单发空回包假成功、N8 read_roots 单串切分、N9 suite 空跑放行、N10 wm HEAD 残留（high）、N12 断言面误标、N13 裸 int() 打死编排、N14 敌对字符裸崩、N16 一行 DoS、N17 jobs 池分裂、N18 timeout_s 无校验 |
| v4 遗留 15 | **N21（本轮 L3）**；N8 部分缓解（codegen 错误路径终裁否决分支实测可达，词法/语法/名实错误的 verdict=None 缺席面仍在） | **14**：N3 毒 token 裸 float()、N4 条件/函数体泄漏顶层、N5 字符串 EOF 零诊断、N6 伴侣上限无空格失效（high）、N7 strict 死配置、N8 终裁橡皮图章（部分）、N9 LLM 建议丢弃、N10 validate_source 全流水线、N14 同名 def 覆盖、N16-N20 测试面五项 |
| 本轮新增 | N22/N23 已修 | **3**：N24（= v1 #24，双列声明：v1 时点已发现、第 5 轮 swarm 深挖在 CLI 现行代码复核确认仍在位）、N25、N26 |

**优先级建议**：N24（= v1 #24）修复成本极低（cmd_run :78-81 补 `condition_space=cfg_in.get("condition_space")` 一个实参）且负路由校验链/注入链均已就位、只缺 CLI 透传——建议随下批顺手清账；N25 与之同文件同函数（cmd_run 整体包 try/except→_fail），宜与 N24 一并处置；N22/N23 修复后，swarm 消息语义面已闭环，N26 为独立入口健壮性、可缓。

---

## 六、测试验证结果

以下命令均为本会话实际运行（Windows，仓库根目录，`python -X utf8`）：

| 命令 | 结果 | 退出码 |
|------|------|--------|
| `python scripts/run_tests.py`（全量入口） | **SUMMARY 164/164 通过，5 跳过**（依赖缺失/平台不符的既定跳过；较 v4 的 163 增 1 = 新套件 test_swarm_inbox_delivery 入列） | **0** |
| `python swarm/tests/test_swarm_inbox_delivery.py`（本轮新红守卫，单跑） | 12 passed, 0 failed（场景①同源双路由 5 项 + 场景②protocol verifier 5 项 + 项目生成/WAL 验签） | 0 |
| `python compiler/tests/test_c4_tooling.py` … `test_pbc.py`（8 套逐件单跑） | 8/8 全 PASS（L1/L3 改动的 compiler 面回归） | 0 × 8 |
| `python _repro_verify_3536.py`（v1 复现脚本，非套件） | S1 propose 成功 / S2 AccessDenied——L5 修复后两象限翻正（S4 段因脚本自身 `_j` 未定义中止，见第二节 L5 行） | —（复现脚本） |

**修复/缺陷探针**（本会话内联命令，关键输出已在第二、三节逐项引用）：L1 非法函数名编译失败 + 终裁否决触发 + 正常函数对照；L2 四行畸形 WAL 计 bad 不崩；L3 函数体内条件空间拦截；L4 投毒索引越界读封死；L5 S3eq accept/S4 层落盘对照；N24 grep 零命中 + 校验链读核；N25 CLI traceback 实跑复现；N26 调用环逻辑证明。

**git 工作树核对**：`git diff`（vs HEAD d3296ec）累积 v1-v5 未提交改动；本轮新增部分 = swarm.rs（修复 #1/#2，均带「2026-09-25 修复 #N」注释）、rust_swarm.py（WAL 容错，缺陷 #2）、name_checker.py（缺陷 #3）、mdcg.py `_read`（缺陷 #4）、mdcos.py propose（缺陷 #5）、api.py gen.errors 回流（无编号注释，归位依据见第二节 L1 行）、test_swarm_inbox_delivery.py（新增未跟踪）、test_rust_swarm.py ⑤（v2 F3 配套，非本轮）。另：md_cg 16 文件的 `os.replace`→`fsutil.publish()`（fsutil.py:44-53，Windows WinError 5 短重试，注释标「2026-09-25 全量回归实测」）与 .gitignore +`.env` 亦在树——属回归稳定性工程/环境项，不计入缺陷修复 5，如实记录；publish 重构是否即 v4 输入「全量有失败」所指的随机 PermissionError 源，无法确证，不展开推断。

---

## 七、跳过项及原因

- **修复跳过：0**——遗留修复 5 项全部落盘验证；新发现 5 项中 2 项（N22/N23，均为 high）本轮修复，N24-N26 三项列入遗留清单（第五节），无「确认后主动跳过」项。
- **未运行项（如实说明）**：
  - N26 未做运行时复现（需构建独立形态 exe + 构造 --symbols 入参），以读码 + 调用环逻辑证明在位，见第三节该行声明；
  - `scripts/verify_linux.sh`（Docker Linux 口径 40 套）本轮未跑——清单为 md_cg/hive/scripts 面，本轮改动中 md_cg 两项（L4/L5）有其覆盖面，如需 Linux 口径确认可后续补跑；
  - bench_swarm_parallel/bench_swarm_scale 两套为全量入口既定跳过（负载敏感仅串行），非本轮新增；
  - hive Rust 面 `cargo test` 本轮未单跑——hive 域本轮零改动（工作树 hive diff 均为 v2 轮遗留的未提交修复，v2 已验），全量入口的 hive Python 套件（test_orch/test_serve_entry/test_wm_progress/test_exec_cmd 等）本轮全绿。

---

*报告依据：任务输入的第 5 轮结论（修复遗留 5 / 新发现 5 确认→新修 2 / 回归全绿，及五项缺陷明细与时点行号）；本会话的现行代码逐处读核（swarm.rs/swarm_cli.py/rust_swarm.py/rust_codegen.py/main.rs/name_checker.py/api.py/mdcg.py/mdcos.py）、`git diff` 工作树逐 hunk 核对与 HEAD 对照、五项遗留修复的探针实测（含 v1 复现脚本翻正）、五项新发现的复现/红守卫实跑（N26 为逻辑证明）、新红守卫套件单跑 12/12、compiler 8 套单跑、全量 run_tests.py 164/164 实测。*

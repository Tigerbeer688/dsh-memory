# 缺陷挖掘报告 · 自主迭代 v6

- **日期**：2026-09-25
- **范围**（生效条件）：第 6 轮 = 四区复核（scripts-infra / scripts-ops / ts-lib / ts-entry）新发现 21 项确认（候选 21 → 确认 21 → 证伪 0）+ 本轮修复 5 项（high 3 + medium 2）落盘核验与守卫实跑 + 门禁三件（python 全量 / npm test / npm run gate）复现
- **总量**：新发现确认 **21**（high 3 / medium 6 / low 12）→ 新修 **5**（N31/N32/N33/N42/N44）→ 遗留新增 **16**
- **回归**：本会话实跑全量 `python scripts/run_tests.py` **SUMMARY 167/167 通过、5 跳过（依赖缺失/平台不符）、退出码 0**（较 v5 的 164 增 3 = 三个新 python 守卫套件入列）；`npm test` **49 tests / 48 pass / 0 fail / 1 skipped、退出码 0**（增 2 = 两个新 TS 守卫）；`npm run gate` **退出码 0**（报告文件登记 git 追踪后复跑，两遍过程见第四节）
- **行号口径**：新发现行号为缺陷时点快照行号；两项所在文件本轮被改（src/lib/roleplay_web.ts +56/-13、src/bridge.ts +33），涉及处本会话另注修复后现行行号（N43 → 现行 :542；N44 → scheduleRetry 现行 :295/:328）；其余文件本轮零改动，快照行号与现行一致（关键处本会话抽读核实）
- **代码注释口径**：本轮 python/roleplay 侧修复注释标「V22 修复」、bridge 侧标「V23 修复」（沿用缺陷挖掘内部批次编号）；本报告编号 N27-N47 沿历轮报告 N 口径（v5 用至 N26）
- **结构口径**（CCG 六要素）：功能名（标题）/ 生效条件（范围与口径行）/ 子功能（第二~五节）/ 执行与验证方式（第四节：实跑命令与结果）/ 不适用条件（第七节：跳过与未运行项）

---

## 一、执行摘要

本轮对四个区做缺陷复核：scripts-infra（判据面）4 项、scripts-ops（运维面）7 项、ts-lib（令牌/互维护面）4 项、ts-entry（入口/桥/角色扮演面）6 项，合计 21 项全部确认、0 项证伪。本轮修复 5 项、留档 16 项。

修复主题两条：

1. **数据保全与自愈闭环**（scripts-ops）：迁移 `--apply` 经 `cg.add` 全量重建 frontmatter 把存量 private 节点的 tags/condition_space/importance/confidence/edges/non_applicable_conditions/created_at/protected 等 8 类字段全部清空重置（N31，high，数据丢失）；通道 B verifier 首次失败即 AttributeError 崩溃、队列不回写、条目永久 pending 每轮重试再崩（N32，high，死循环）；通道 A 固化误传全量补丁、验证失败者也写进 wisdom 源文件（N33，medium，闸失效）。三项均修复并各配红守卫。
2. **入口可用性与进程保真**（ts-entry）：角色扮演网页全部编辑操作（创建/保存角色/保存翻译）因前端无携带 x-edit-key 途径开箱即 403，「详情保存」还无条件显示成功——用户被告知已保存但数据从未落盘（N42，high，错误结果+虚假成功）；桥在 initialize 握手超时重试时不清理上一个仍存活的子进程，最多累积 8 个僵尸（N44，medium）。两项均修复并各配红守卫（对旧代码实测红：5 fail / 1 fail）。

**对输入口径的核验**：输入称「新发现 21 确认（四区）→ 修复 5 → 留档 16」。本会话以 `git status`/`git diff --stat` + 逐处读核 + 守卫实跑核对：改动面恰为 4 文件（scripts/migrate_restricted.py +23、scripts/bootstrap_loop.py +12-3、src/bridge.ts +33、src/lib/roleplay_web.ts +56-13）+ 5 个新守卫文件（scripts/test_migrate_restricted.py、scripts/test_bootstrap_channel_b.py、scripts/test_bootstrap_persist_gate.py、test/roleplay_edit_guard.test.ts、test/bridge_retry_leak.test.ts），与 5 项修复一一对应；新发现 21 − 修复 5 = 留档 16，无多无少。

---

## 二、修复清单（5 项，逐项 where / 问题 / 修复 / 守卫 / 验证）

### F1（N31，high）migrate_restricted --apply 透传旧 frontmatter

- **where**：scripts/migrate_restricted.py:61-83（缺陷时点 :62 的 `cg.add` 调用；现行修复段含「V22 修复」注释与排除集注释，本会话读核 :78-83）
- **问题**：`--apply` 只传 content/layer/sensitivity/override，而 `cg.add` 是全量重建 frontmatter（mdcg.py add docstring 自认；created_at 未传即重置、condition_space 只剩 time_window、importance/confidence/tags/edges/non_applicable_conditions 归默认、protected/protection_reason 清空）——存量 private 节点 8 类字段丢失/重置（数据丢失）。
- **修复**：旧 frontmatter 全部字段经 `**passthrough` 透传给 `cg.add`；排除集及理由（注释在位）：id/layer/sensitivity（显式给值）、state/lifecycle_state/verification_state（add 覆写时默认继承旧值，显式传同值反要走迁移裁决）、bucket/bucket_zh（路由按 tags/condition_space 重算）、writer（语义=最后写入者）。
- **守卫**：scripts/test_migrate_restricted.py:88-122——先证旧代码红（9 项 FAIL 精确复现 8 类字段丢失主诉），修复后绿。
- **验证**（材料：上游会话运行）`python -X utf8 scripts/test_migrate_restricted.py` 修复前 exit 1/9 FAIL → 修复后 15 通过 0 失败；`scripts/run_tests.py scripts` 4/4；md_cg 回归五套（test_p19_migrate_roleplay 13/13、test_p9_forget_protect 37/37、test_security_audit 13/13、test_security_audit_b26 17/17、test_security_audit_v21 49/49）无回归。**本会话复跑该守卫：15 通过 / 0 失败**（见第四节）。

### F2（N32，high）通道 B verifier 失败分支 _rej_list 初始化修复

- **where**：scripts/bootstrap_loop.py:370（`_rej_list = ""` → `[]`；现行该行带「V22 修复」注释，本会话读核 :367-377）
- **问题**：cases 物理验证未过分支把 `_rej_list` 初始化为空串，`rejected_log.json` 不存在时首次失败即 `AttributeError: 'str' object has no attribute 'append'`——崩溃点在队列回写与拒绝留痕之前，条目永 pending 每轮重试再崩（通道 B 死循环），run_once 捕获后整轮通道 B 作废。
- **修复**：初始化改为 `[]`（对照同文件 :319 沙箱拒绝分支的正确写法）。
- **守卫**：scripts/test_bootstrap_channel_b.py:57-95——cases 未过分支专测（与 V21-4 沙箱拒绝分支守卫互补）；先证旧代码红（AttributeError），修复后绿。
- **验证**（材料：上游会话运行）修复前 exit 1/AttributeError → 修复后 5 通过 0 失败；test_security_audit_v21 49/49 不回归；`run_tests.py scripts` 5/5。**本会话复跑该守卫：5 通过 / 0 失败**（见第四节）。

### F3（N33，medium）通道 A 固化只传验证通过者

- **where**：scripts/bootstrap_loop.py:416-421（`persist_triggers(patches)` → `persist_triggers(persisted)`；现行该段带「V22 修复」注释，本会话读核 :415-421；:394 生效条件 docstring 同步）
- **问题**：run_once 固化时传全量 patches，verify_patch 失败的补丁也被写进 WISDOM 下 6 个 *_units.py 源文件——绕过模块 docstring 声明的「补丁→验证→固化」闸。
- **修复**：只传只含验证通过者的 `persisted` 列表。
- **守卫**：scripts/test_bootstrap_persist_gate.py:36-72——monkeypatch 台架与原缺陷复现同款；先证旧代码红（固化入参收到全量 `['gap-unit-x','gap-unit-ok']`），修复后只含 `['gap-unit-ok']`。
- **验证**（材料：上游会话运行）修复前 exit 1/2 FAIL → 修复后 4 通过 0 失败；test_bootstrap_channel_b 5/5（同文件 F2 不回归）；test_security_audit_v21 49/49；`run_tests.py scripts` 6/6。**本会话复跑该守卫：4 通过 / 0 失败**（见第四节）。

### F4（N42，high）roleplay 编辑密钥携带 + 响应检查（虚假成功封死）

- **where**：src/lib/roleplay_web.ts:350（saveRoleDetail；缺陷时点 :338，修复后注释锚点现行 :356；改幅 +43/-13 全在内嵌 HTML 字符串内，服务端 handler 与 fail-closed 鉴权语义未动——P1-2 不可回退）
- **问题**：服务端写操作要求 x-edit-key 头（未配置 ROLEPLAY_EDIT_KEY 时拒绝全部写），但前端页面无任何输入/携带密钥途径——全部编辑操作（创建角色/保存详情/保存翻译）开箱即恒 403 不可用；且 saveRoleDetail 无条件显示「已保存」——用户被告知已保存但服务端 403 在写盘之前 return、数据从未落盘（错误结果 + 虚假成功反馈）。
- **修复**（五步，全部在内嵌 HTML）：① header 新增 🔑 按钮（现行 :104）+ setEditKey（prompt 输入存 localStorage，现行 :203）；② apiHeaders 注入 x-edit-key（现行 :211-216）；③ saveRoleDetail 的 meta/import 两个 POST 从裸 Content-Type 改走 apiHeaders() 并逐段检查响应 ok（含网络异常 catch），任一失败 alert 服务端原因（403 文案含 ROLEPLAY_EDIT_KEY 指引）并中止，不再无条件显示已保存/关闭面板；④ saveTrans 与 ⑤ createRole 的 POST 改走 apiHeaders()，失败 alert 追加密钥输入引导。
- **守卫**：test/roleplay_edit_guard.test.ts:148-278（node:test，6 用例：apiHeaders 注入 / setEditKey 持久化 / 403 时不显示已保存+alert+不关面板 / 配密钥后带头且成功 / saveTrans·createRole 带头 / 服务端 403 不落盘回归线）。实现方式：mock 挂载 GET /roleplay 取 HTML，提取 script 在 mock DOM/localStorage/fetch/alert 沙箱求值，直接执行真实前端函数验证。对旧代码实测 **5 红 1 绿**（红在核心断言：服务端 403 未落盘时页面消息却为「角色「…」已保存：… 1 条」的既定成功文案——虚假成功确凿复现）。
- **验证**（材料：上游会话运行）修复后 6/6 pass；`npm run build` tsc 通过；全量 `node --import tsx --test "test/*.test.ts"` 48 tests/47 pass/0 fail/1 skipped；附加对照 test/verify_web.ts 改动前后均 2/5（②④⑤ 为 HEAD 既有失败，git stash 对照确认非本次引入，无回归）。**本会话经 npm test 全量复现终态：该守卫 6 用例全 ✔（输出见第四节）**。

### F5（N44，medium）桥重试换代前回收旧子进程

- **where**：src/bridge.ts:162-189（新增 killStaleProc + spawnAndHandshake 换代前调用；缺陷时点 :284 handshake catch → scheduleRetry → 重 spawn 覆盖引用的泄漏链由此截断；现行 scheduleRetry :295/:328、spawnAndHandshake 内调用点 :189，本会话读核）
- **问题**：initialize 握手超时重试直接 spawn 新进程覆盖 this.proc 引用，旧进程仍存活（卡死不退出）时既不 kill 也不关 stdin——僵尸进程泄漏，最多 MAX_RETRIES=8 个并存，dispose 也只收尾最后一个（原报告动态复现：8 次重试后存活子进程 = 8，dispose 后仍剩 7）。
- **修复**（三处最小改动 +33 行）：① killStaleProc——先摘 this.proc 引用再对仍存活旧进程 stdin.end + kill，幂等（exitCode/signalCode 非 null 跳过）；② spawnAndHandshake 开头换代前统一调用，覆盖握手超时重试、spawn error 重试、exit 重试、冷却恢复全部路径；③ proc.on('exit') 加非当前进程早退守卫——被回收旧进程的 exit 事件只写探针，不再误关新进程 readline/误拒挂起请求/多触发重试。dispose 与 gaveUp 语义未动。
- **守卫**：test/bridge_retry_leak.test.ts:44-79（node:test）：spawn 真实 `node -e 'setInterval(()=>{},1<<30)'`（永不退出、不响应协议）驱动握手超时重试换代，10ms 轮询收集每代 this.proc，达 gaveUp 后断言除最后一代外全部旧进程已退出。旧代码红（AssertionError：7 个旧子进程存活，与原报告吻合）；新代码绿（0 存活）；finally 兜底 kill 防红跑泄漏。
- **验证**（材料：上游会话运行）修复前 0 pass/1 fail → 修复后 1/1 pass；`npm run build` 通过；全量 49 tests/48 pass/0 fail/1 skipped（含 bridge.test.ts 真实 python 集成回归——正常握手/调用/dispose 路径未被 exit 守卫破坏）。**本会话经 npm test 全量复现终态：49/48/0/1、退出码 0**。

---

## 三、本轮新发现缺陷明细（确认 21 项 / 证伪 0 项）

四区复核 21 项候选全部确认，**rejected 为空**（四个区的复核结果 rejected 数组均为 0 项）——如实记录：本轮零证伪。以下每项的「证据」均为上游复核会话的读码 + 实跑复现（材料原文，证据中的环境细节已按仓库相对路径口径转录）；标注★者本会话另做了关键行抽读核实。

### scripts-infra 区（4 项，全部留档未修）

| # | where | 严重度 | 问题摘要 | 证据要点 |
|---|------|--------|----------|----------|
| N27★ | scripts/judgment_manifest.py:129 | medium | `--verify` 缺冻结文件参数时不报用法错误：`len(sys.argv) >= 3` 为 False 落入 :142 兜底分支，打印当前盘面全量 manifest 并 return 0——CI/钩子参数拼空时守卫假通过（fail-open） | 实跑 `--verify` 无参 → exit 0、stdout 为全量 manifest 非判决；对照：带空 files 冻结文件 → verdict FAIL、exit 1（判决分支正常，唯缺参 fail-open）；docstring :16-17 声明需参数。本会话抽读 :129/:142 在位 |
| N28★ | scripts/run_tests.py:216 | medium | SUMMARY 输出格式与下游解析契约断裂：hive/verify_runner.py:56-60 的 `_parse_counts` 两个正则（`(\d+) passed` / `通过 X / 失败 Y`）均不命中 `SUMMARY 164/164 通过，N 跳过` 实际形态（失败只打名字无计数）——verdict.json 的 passed/failed 计数恒失真（python 侧恒 0，fail 态靠 `failed + 1` 硬补；verdict 本身因 rc_py 条件仍正确） | importlib 加载后实测 `_parse_counts` 对真实 SUMMARY 形态 → (0,0)、对假设形态才命中；git log --follow -p（483 行 diff）核实「通过 X / 失败 Y」形态从未存在过。本会话抽读 :216-218 与 verify_runner.py:55-62 在位 |
| N29 | scripts/interop_gate.py:61 | low | PASS 分支直接取 `v['failed']` 但该字段不在 `_REQUIRED`(:26) 也不在 _load 校验——缺 failed 字段的 pass 态 verdict 触发未捕获 KeyError（traceback + exit 1），违反自身「exit 3 = 形状非法」契约 | 实测：构造缺 failed 的 pass 态 JSON → KeyError: 'failed'、exit 1；生产方 verify_runner.py:148 总写该字段，触发面限旧版/手工产物 |
| N30 | scripts/criteria_fingerprint.py:44 | low | `worktree_of` 用 tempfile.mkdtemp 解包 git archive 后从不删除——每次 `--commit` 调用泄漏一个含完整仓库快照的临时目录，跨运行累积 | 实测：系统临时区 criteria_* 目录基线 0 → 连跑两次 `--commit` 后 2 个（抽样含 27 个顶层条目）；附带检查：指纹输出 files=97 与 docstring 声明一致；复现后已清理本轮创建的 2 个目录 |

### scripts-ops 区（7 项，前 3 项已修见第二节，余 4 项留档）

| # | where | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N31 | scripts/migrate_restricted.py:62 | high | --apply 经 cg.add(override=True) 全量重建 frontmatter，存量 private 节点 tags/condition_space/importance/non_applicable_conditions/created_at/protected 等 8 类字段全部清空重置（数据丢失） | 实跑复现（临时库 + importlib 原样调 main）：BEFORE tags/importance=0.9/confidence=0.8/cs={env,region,…}/nac/protected/edges 齐备 → AFTER 全部为空/默认/重置（created_at 变更），8 项实证 | **已修**（F1） |
| N32 | scripts/bootstrap_loop.py:370 | high | 通道 B verifier 失败分支 _rej_list 初始化为空串，rejected_log.json 不存在时首次失败即 AttributeError 崩溃，队列回写不执行 → 条目永 pending 每轮重试再崩（死循环） | 实跑复现：CRASH AttributeError；崩溃后队列 status 仍 None、rejected_log.json 不存在；对照实验：预置空数组再跑 → 不崩（证明崩溃条件=文件不存在） | **已修**（F2） |
| N33 | scripts/bootstrap_loop.py:416 | medium | run_once 固化传 persist_triggers(patches) 全量而非只含验证通过者的 persisted——verify_patch 失败的补丁也写进 wisdom 源文件，绕过「补丁→验证→固化」闸 | 只读实跑复现（monkeypatch 台架）：gaps=2、verified=1/failed=1，但固化入参收到全量 2 个单元（含验证失败者）；未实跑真实 persist（避免改写仓库源文件） | **已修**（F3） |
| N34 | scripts/retr_time_coverage.py:179 | low | --out 传裸文件名时 os.path.dirname 为空串，os.makedirs('') 抛 FileNotFoundError，脚本写结果前崩溃（docstring :14 明示 [--out FILE] 用法） | 实跑：--out bare_out.json → FileNotFoundError 指向 :179；同型实测 retr_baseline_diff.py:444 同款崩溃 | 留档 |
| N35 | scripts/check_unreachable.py:46 | low | ROOTS 为相对路径按 cwd 解析，非仓库根目录调用时扫描 0 文件仍报「命中 0」exit 0——死代码守卫可空转通过（假绿）；grep 全仓无自动调用入口，属手动守卫 | 实跑三种 cwd：仓库根 385 文件/0 命中；在 scripts 子目录与系统临时目录下运行均 0 文件 0 命中 exit 0 | 留档 |
| N36 | scripts/bootstrap_watchdog.py:82 | low | resolve_python 兜底 glob 模式 `[系统盘]:\Python3*\python.exe` 中 [系统盘] 是单字符字符类，真实盘符（拉丁单字母+冒号）永不命中——「常见安装位置扫描」第二分支是死代码 | 实测：该 glob → 空列表；对照实验证明 [..] 按单字符类匹配（构造盘符形态目录名不在类内即不命中）；该分支仅在 sys.executable basename 不含 'python' 的罕见情形可达 | 留档 |
| N37 | scripts/bootstrap_watchdog.py:289 | low | degraded 分支只在 procs 非空时 kill+restart；进程已死（not_running）且日志新鲜降级时不拉起 return 0——与 docstring :21「进程缺失 → 直接拉起」矛盾，死循环要等日志陈旧（2×interval+120s，interval=600 时约 22 分钟）才被分支③恢复 | 代码证据核实（未实跑——需真实 kill/拉起进程，如实声明）；场景可达性由 bootstrap_loop.py:531-533 loop_error 持续写入机制保证 | 留档 |

### ts-lib 区（4 项，全部留档未修）

| # | where | 严重度 | 问题摘要 | 证据要点 |
|---|------|--------|----------|----------|
| N38★ | src/lib/token_store.ts:76-79 | medium | auxRoot() 用 resolve(env)：不做波浪线家目录展开且锚定宿主 cwd，与文件头/token_store.ts:71、index.ts:293 声称「同口径」的 md_cg/datapath.py aux_root()（expanduser+abspath，锚 python 子进程 cwd=runRoot()）不一致——MDCG_AUX_ROOT 设为波浪线家目录引用或相对路径时密钥环与令牌记录分居两处，宿主 cwd 变化即触发重复自动签发永不吊销的 designer 令牌 | 探针实测（隔离目录 import 编译产物）：MDCG_AUX_ROOT 设波浪线值时 node 得字面波浪线目录（锚 cwd）、python 得真家目录——两侧分居两处；设相对路径时 node 结果随宿主 cwd 变即变；后果链逐行读核（readKeyring 读错处 → issueToken 落另一处 → 全文无 revoke）；佐证：同仓 mdcgRoot 已锚 repoRoot 并写明历史教训（src/lib/datapath.ts:122），auxRoot 与既定纪律相悖。本会话抽读 :76-79 在位 |
| N39 | src/lib/token_store.ts:97-99 | low | writeKeyring 的 mkdirSync/writeFileSync 无 try 包裹（对比 :100-104 chmodSync 有 try），keyring 父路径不可写（被普通文件占用/磁盘满/只读）时 resolveToken 抛裸 fs 错误，index.ts apply()（:246 调用，无 try）激活中断——且令牌已先签发进 _tokens.json，每次重试再泄漏一枚不吊销令牌 | 探针实测（隔离目录 + 父路径放 blocker 文件）：输出 EEXIST 裸错误冒出；崩溃前后对照隔离 _tokens.json：新增 1 枚 actor=dsh-memory 且 revoked=None 的令牌记录；探针产物已清理（未触碰本机真实令牌目录） |
| N40 | src/lib/mutual.ts:33 | low | restartCooldownMs 在接口与 DEFAULTS（60s）声明、头注承诺「幂等：拉起前确认不存在 + 60s 冷却」，但 ensureHarness（129-195 行）无任何冷却逻辑——契约未实现；heartbeatMs 无最小值约束（index.ts:186 可配 1000ms）+ harnessRunning 漏检边界（:96-99 自认）叠加时每拍 spawn 一个 detached guardian 进程累积 | grep 全仓该字段仅声明/赋值/测试填充三处、无冷却行为断言（不构成消费点）；调用链逐行读核；缓解：config.mutual.enabled 默认 false + 默认 10min 心跳是事实节流 |
| N41 | src/lib/mutual.ts:305-310 | low | processTask 用 renameSync 把 task-<id>.json 改名 processing-<id>.json 认领，B 侧进程在认领后、写回 result/done 前崩溃（断电/重启/kill）时无任何 stale processing-* 恢复机制——scanTasks 只认 task- 前缀，任务滞留既不重试也无消费者清理 | grep 全仓 "processing-" 唯一命中构造点、无清理消费者；边界事实如实记录：A 侧 guardian 不在本仓无法核实重发自愈；实测 renameSync 目标已存在时覆盖成功（若 A 侧重发同 id 可覆盖 stale）——但 B 侧自身无恢复机制的事实成立；缓解：mutual.enabled 默认 false + 需恰在 await 窗口死亡 |

### ts-entry 区（6 项，2 项已修见第二节，余 4 项留档）

| # | where | 严重度 | 问题摘要 | 证据要点 | 状态 |
|---|------|--------|----------|----------|------|
| N42 | src/lib/roleplay_web.ts:338 | high | 角色扮演网页全部编辑操作开箱即 403 不可用（服务端 fail-closed 要求 x-edit-key，前端无携带途径），且「详情保存」无条件显示成功——数据从未落盘（错误结果+虚假成功反馈） | 读码全链：服务端 403 闸四处（/import :700-704、/meta :758-762、/translate :872-876、POST /roles :908-912）、前端 meta/import POST 裸 headers、HTML 无密钥输入路径；saveRoleDetail 的 addMsg 无条件执行、fetch 对 403 不 reject；服务端 403 在写盘（:719-723/:794）之前 return 确证数据未写 | **已修**（F4） |
| N43 | src/lib/roleplay_web.ts:512（现行 :542） | medium | 角色扮演数据面（转录 :543-548/_roles.json :566/翻译配置 :613）落在相对路径 `<cwd>/data/roleplay_data`，随宿主启动 cwd 漂移分裂成互不可见的多处——本仓 datapath.ts :22-25 把同类问题定性为「数据丢失级历史事故」并为 mdcg.root 专门修过（anchor()），此处未走同一口径 | 实测 join(dirname('data/lingshu.db'),'roleplay_data') → 相对路径；index.ts:157 dbPath 默认相对、:421 原样传 config、全文件无绝对化；capability 桥 spawn cwd undefined 继承宿主 cwd 同样漂移。本会话抽读现行 :542 在位 | 留档 |
| N44 | src/bridge.ts:284 | medium | initialize 握手超时重试不清理上一个仍存活子进程——旧进程不 kill 不关 stdin，僵尸泄漏最多 8 个并存，dispose 只收尾最后一个 | 动态实跑：timeoutMs=800 + 永不退出的 node 子进程驱动重试，9 秒后存活子进程 = 8，dispose 后仍剩 7，与 MAX_RETRIES=8 上限吻合；复现后残留进程已清理（终查 0） | **已修**（F5） |
| N45 | src/lib/roleplay_web.ts:715 | low | /import、GET /items、POST /api/roles 三个入口 role_id 未做 validRoleId 白名单校验（P2-23 只修了 /chat 与 /meta）——非法 role 经 node_id 模板被 md_cg 白名单（mdcg.py:1095 `^[A-Za-z0-9_.@-]{1,128}$`）拒绝后落图静默失败、接口仍返回 ok；危险键还会写进 _roles.json 并进角色下拉列表 | grep validRoleId 仅 4 处（:571/:638/:774/:815），三入口不在内；:745-749 fire-and-forget 落图 + :750-754 仍返回 ok 逐行读核；缓解：写路径均在 403 闸之后，需持密钥者手动构造请求才可达 | 留档 |
| N46 | src/lib/whitebox_llm.ts:195 | low | 白箱输入实现与自述契约不符：注释称「取最后一条用户文本」，实际逆序拼接所有角色（含 assistant）全部文本块后截尾 | node -e 复现：messages=[U1,A1,U2] → lastUserText 为三段混拼（契约应仅 U2）；文件头 :2-5 声明已下线、grep installWhiteboxLlm index.ts = 0（无运行时调用方），保留库内真实逻辑错误。本会话抽读 :194-201 在位 | 留档 |
| N47 | src/hooks.ts:239 | low | 自动召回注入在 system-prompt/assemble waterfall 内同步 await 认知图时间线，桥挂死（进程存活不响应、exit 不触发）时每次模型请求组装阻塞至 toolCallTimeoutMs（默认 60s），与文件头「失败静默（不影响请求）」声明不符 | 读码核实（未动态复现 60s 阻塞——需 mock Cordis waterfall + wedge 子进程，如实声明）：await 在 next() 之前阻塞组装链；超时链逐环实读（hooks:239-240 → mdcg_client:317-319 → bridge:340/:344-348，默认 60_000 见 index.ts:174）；low 定级：需握手成功后 wedge 的少见场景，后果为 +60s 延迟非功能失败。本会话抽读 :239-241 在位 | 留档 |

---

## 四、验证（本会话实跑门禁结果原文数字）

以下命令均为**本报告写作者本会话实际运行**（仓库根目录，`python -X utf8` / npm）：

| 命令 | 结果（原文数字） | 退出码 |
|------|------------------|--------|
| `python -X utf8 scripts/run_tests.py`（全量） | `===== SUMMARY 167/167 通过，5 跳过（依赖缺失/平台不符） =====`（较 v5 的 164 增 3 = 三个新 python 守卫套件入列） | **0** |
| `npm test`（= `npm run build` + `node --import tsx --test "test/*.test.ts"`） | `ℹ tests 49 / ℹ pass 48 / ℹ fail 0 / ℹ skipped 1`（skipped 为既有平台跳过项；含两个新 TS 守卫全部用例 ✔，roleplay_edit_guard 六用例与 bridge 集成回归均绿；tsc build 随 npm test 先行通过） | **0** |
| `npm run gate`（check_unreachable + check_publish_artifact + cogmap_sync check + link_check + workspace_index --check + verify_discipline） | 复跑两遍，如实记录：**第一遍 FAIL**——R4 非追踪件面（非追踪=25，允许=24）点名本报告文件自身（新写入的未跟踪交付件进入 npm pack 清单；lib/ 外非追踪件须 git 登记，check_publish_artifact.py:45/ALLOW_NONTRACKED，历轮报告均追踪在案）；按守卫要求将本报告文件 git add 登记后**第二遍通过**：R1-R4 全 PASS、链接全可达、工作区索引 OK（顶层目录 20 · 追踪件随本报告 +1）、纪律注入面 8 条链 0 冲突 | 第二遍 **0**（第一遍 1，原因如左，非修复代码回归） |
| `python -X utf8 scripts/test_migrate_restricted.py`（新守卫单跑） | `migrate_restricted 守卫: 15 通过 / 0 失败`（含 frontmatter 九字段保全断言） | 0 |
| `python -X utf8 scripts/test_bootstrap_channel_b.py`（新守卫单跑） | `bootstrap 通道 B 守卫: 5 通过 / 0 失败`（首跑不崩/拒绝留痕/队列回写/死循环消除） | 0 |
| `python -X utf8 scripts/test_bootstrap_persist_gate.py`（新守卫单跑） | `bootstrap 固化闸守卫: 4 通过 / 0 失败`（固化入参只含验证通过者） | 0 |

**上游复核/修复会话已运行、本会话未重跑的验证**（如实区分，材料原文）：五项修复的「修复前红态」对照（test_migrate_restricted 9 FAIL、test_bootstrap_channel_b AttributeError、test_bootstrap_persist_gate 2 FAIL、roleplay_edit_guard 5 fail 1 pass、bridge_retry_leak 1 fail 7 存活）经 git stash push/pop 或时序对照完成；md_cg 回归五套（13/13、37/37、13/13、17/17、49/49）单跑通过（其覆盖面已含于本会话全量 167/167）；`scripts/run_tests.py scripts` 6/6；test/verify_web.ts 附加对照 2/5（HEAD 既有失败，非本次引入）。21 项新发现的复现实验（探针/实跑/对照）均在四区复核会话完成，证据要点已摘录进第三节各表。

**git 工作树核对**（本会话）：`git status`/`git diff --stat` —— 改动 4 文件 +105/-19（migrate_restricted +23、bootstrap_loop +12-3、bridge.ts +33、roleplay_web.ts +56-13）+ 未跟踪 5 守卫文件 + 本报告文件（已 git add 登记，见 gate 行说明），与五项修复一一对应，无多余改动面。

---

## 五、遗留清单（deferred 16 项逐项）

### medium（4 项）

| # | where | 问题 | 降级理由（材料原文要点） |
|---|------|------|--------------------------|
| N27 | scripts/judgment_manifest.py:129 | --verify 缺参 fail-open | 修复廉价但触发面窄（仅 CI/钩子参数拼空时发生），本轮名额让给影响面更大的 medium（N33/N44）；建议下轮优先收掉 |
| N28 | scripts/run_tests.py:216 | SUMMARY 与 _parse_counts 契约断裂，verdict.json 计数系统性失真 | verdict 判决本身仍正确，受损的是留痕计数；修复若动 run_tests 输出格式，其他消费方（verify_linux.sh 等按 SUMMARY 形态 grep）波及面需先盘清 |
| N38 | src/lib/token_store.ts:76-79 | auxRoot 双口径分裂，令牌重复签发永不吊销 | 触发依赖 MDCG_AUX_ROOT 非常规配置（波浪线家目录引用/相对路径）；改令牌/密钥环定位涉及既有部署兼容（现存令牌位置迁移），修复风险中等，宜单独一轮配齐验证 |
| N43 | src/lib/roleplay_web.ts:512（现行 :542） | roleplay 数据面相对路径随 cwd 漂移分裂 | 根因是 dbPath 本身相对（默认 'data/lingshu.db'），只锚 roleplayData 会令数据面与库文件分裂两处；连贯修法需连 dbPath 一起锚定——涉及既有部署 sqlite 库位置迁移语义，波及面与数据找回风险中等，宜单独一轮处理；本轮 roleplay 名额先给 high 的 N42 |

### low（12 项）

| # | where | 问题 | 降级理由/备注 |
|---|------|------|----------------|
| N29 | scripts/interop_gate.py:61 | pass 态缺 failed 字段 KeyError 崩溃（应 exit 3） | 触发面限旧版/手工产物（生产方总写该字段） |
| N30 | scripts/criteria_fingerprint.py:44 | --commit 每调泄漏一个仓库快照临时目录 | 资源泄漏、无正确性影响 |
| N34 | scripts/retr_time_coverage.py:179（同型 retr_baseline_diff.py:444） | --out 裸文件名即崩 | 用法边缘；同型两文件宜一并修 |
| N35 | scripts/check_unreachable.py:46 | 错目录运行 0 文件仍 PASS（假绿） | 手动守卫无自动调用入口；修法（ROOTS 锚脚本父目录 + 0 文件判失败）清晰但需连带跑通 gate 六链 |
| N36 | scripts/bootstrap_watchdog.py:82 | 盘符 glob 死代码 | 永不生效分支，无错误行为 |
| N37 | scripts/bootstrap_watchdog.py:289 | 进程已死且日志新鲜时不拉起，恢复延迟至日志陈旧 | 验证手段不足（需真实拉起/杀死常驻进程），按纪律不宜本轮修 |
| N39 | src/lib/token_store.ts:97-99 | writeKeyring 无 try：激活中断 + 每次重试泄漏一枚未吊销令牌 | 触发需 keyring 父路径不可写；与 N38 同文件同域，宜与 N38 一轮配齐验证（含已泄漏令牌清理语义） |
| N40 | src/lib/mutual.ts:33 | restartCooldownMs 契约未实现，可致 guardian 进程泄漏 | mutual.enabled 默认 false + 默认 10min 心跳事实节流 |
| N41 | src/lib/mutual.ts:305-310 | stale processing-* 无恢复机制 | 同上默认关闭；且 A 侧重发可覆盖 stale 文件（实测 renameSync 覆盖成功），自愈路径待 A 侧核清 |
| N45 | src/lib/roleplay_web.ts:715 | 三入口 role_id 未走 validRoleId 白名单 | 写路径在 403 闸后，需持密钥者手动构造请求；与 N43 同文件宜一并处置 |
| N46 | src/lib/whitebox_llm.ts:195 | 白箱输入混拼 assistant 文本 | 文件已下线、无运行时调用方 |
| N47 | src/hooks.ts:239 | waterfall 内同步 await，桥挂死时每请求 +60s | 修法（预取缓存/移出 waterfall/加短超时）属设计变更，需 mock Cordis waterfall 验证台架，本轮验证手段不足 |

**优先级建议**：N27（修复一个条件判断即除 fail-open，建议下轮首位）；N28 需先盘清 SUMMARY 消费方再动格式；N38/N39（令牌面）与 N43/N45（roleplay 面）各自成对、分别一轮配齐验证；N34-N37、N40/N41/N46/N47 可按批次缓收。

---

## 六、纪律合规声明

1. **定位先读索引**：本报告写作前已读 WORKSPACE_INDEX.md（按职责定位 docs/ 为文档归域、scripts/ 为工程管线、src/ 为 TS 源码），未做全盘浏览。
2. **结论引证**：所有修复与缺陷均引 path:line（缺陷时点快照行号，漂移处另注现行行号）；第三节证据要点摘自任务输入的四区复核材料（上游会话读码 + 实跑，环境细节按仓库口径转录），第四节六项门禁数字为本会话实跑原文输出；未实际运行的检查均标注来源或「未重跑」，无一冒充。
3. **修复落盘核验**：五项修复的现行代码本会话逐处读核（migrate_restricted passthrough 段、bootstrap_loop 两处、roleplay_web 🔑/apiHeaders/:356 注释锚点、bridge killStaleProc/:189），git 工作树改动面与五项一一对应。
4. **如实记录**：rejected 为 0 如实写明（零证伪）；两项证据不完整的缺陷如实标注「未动态复现」（N37 需真实 kill/拉起进程、N47 需 mock waterfall 台架）；N33 复现采用只读 monkeypatch 台架、未实跑真实 persist（避免改写仓库源文件），材料已声明；gate 第一遍 FAIL 的原因（本报告文件未登记追踪触发 R4）如实写入第四节，登记后复跑通过——非修复代码回归。
5. **隐私脱敏**：全文只写仓库相对路径；本机盘符绝对路径、用户家目录路径（含波浪线引用形态）、系统临时目录路径与探针中的令牌记录 ID 字面量均已去除（以「隔离临时目录」「系统临时区」「波浪线家目录引用」「新增 1 枚未吊销令牌记录」等表述替代）；环境变量名（ROLEPLAY_EDIT_KEY/MDCG_AUX_ROOT 等）为源码标识符非凭据值，为证据必需而保留；无任何 key/token 值出现；定稿前经机械扫描复查（盘符路径、家目录路径、系统临时目录、令牌 ID 形态零命中）。

---

## 七、跳过项及原因

- **修复跳过**：无「确认后主动跳过」——21 项确认中 5 项本轮修复（3 high 全修 + 2 medium），余 16 项全部列入第五节遗留清单并逐项给降级理由。
- **未运行项（如实说明）**：
  - 五项修复的「旧代码红态」对照（git stash push/pop、时序对照）未在本会话重做——引用上游修复会话结果（材料原文）；本会话以终态绿（守卫单跑 + npm test 全量）覆盖。
  - md_cg 回归五套（test_p19_migrate_roleplay 13/13、test_p9_forget_protect 37/37、test_security_audit 13/13、test_security_audit_b26 17/17、test_security_audit_v21 49/49）未单独重跑——其覆盖面已包含在本会话全量 `run_tests.py` 167/167 之内。
  - `node --import tsx test/verify_web.ts` 附加对照未重跑——该脚本不在 npm test 套内、bridge mock 缺 isReady，材料记载改动前后均 2/5（HEAD 既有失败），不构成门禁。
  - `scripts/verify_linux.sh`（Docker Linux 口径）本轮未跑——本轮 python 侧改动集中于 scripts/（迁移与自愈脚本），Linux 口径确认可后续补跑。
  - N37/N47 的运行时复现未做（需真实进程拉起 / mock Cordis waterfall 台架），以读码 + 机制链证据定级，见第三节各表声明。
  - bench_swarm_parallel / bench_swarm_scale 两套为全量入口既定跳过（负载敏感仅串行），非本轮新增。
  - 本报告文件未做 git 提交（commit 需任务方指令）；仅按 R4 守卫要求 git add 登记追踪面，提交由后续流程处置。

---

*报告依据：任务输入的第 6 轮材料（四区复核 21 项确认明细及证据、五项修复的 where/守卫/verified_by、留档 16 项及降级理由、门禁结果）；本会话的修复落盘读核（migrate_restricted.py/bootstrap_loop.py/roleplay_web.ts/bridge.ts + 5 守卫文件存在性）、关键缺陷行号抽读（judgment_manifest/run_tests/verify_runner/token_store/whitebox_llm/hooks）、git 工作树核对（4 文件 +105/-19 + 5 新守卫）、三门禁实跑（run_tests 167/167·exit 0、npm test 49/48/0/1·exit 0、npm run gate 登记 R4 后复跑·exit 0）、三新 python 守卫单跑（15+5+4=24 通过 0 失败）。*

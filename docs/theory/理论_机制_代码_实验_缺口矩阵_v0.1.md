# 理论—机制—代码—实验—缺口矩阵 v0.4（条件论 + 端口/锚定 + 信息差）

> 目的：回应外部评审（AGI 架构 8.3/10）风险 5——「理论-实现同构未证明，须建立 Theory → Implementation Traceability Matrix」。
> 方法：三路并行盘点（理论命题 P-T-* ／ 工程对象 P-E-* ／ 证据 P-V-*），逐条对齐。
> 范围：v0.1 条件论主链；v0.2 新增端口架构/锚定验证/五层记忆/自我连续性/维生系统（P-T-61~116、P-E-21~45、P-V-20~45）；v0.3 信息差公理（P-T-117~140、P-E-46~59、P-V-46~57）；**v0.4（2026-09-13）三轨收口：#23 通道贝叶斯 / #6 增益门槛+四保护 / #37 信息价值链 / #30 互维闭环 / #25 端口命题声明**。

## 0. 证据等级定义

| 等级 | 含义 |
|---|---|
| A | 跑过的测试产出数据，有数字/断言可复现 |
| B | 测试断言在库（可一条命令复现），未见独立运行记录 |
| C | 文档声明 / 仅归档（含 vb=null 节点） |
| D | 无证据（缺口） |

## 1. 核心映射矩阵（条件论主链）

| # | 理论命题（P-T） | 工程对象（P-E） | 测试 | 证据（强度） | 状态 |
|---|---|---|---|---|---|
| 1 | P-T-06 条件空间四维坐标 C_position/C_tool/C_time/C_existence | P-E-06 `CONDITION_SLOTS` 四槽合成（nodefile.py，`condition_space_text` 唯一入口，fail-closed） | test_p37 ① | P-V-05 全库 96.7% 已声明生效条件，单槽冒充残留 0（A） | ✅ 已证 |
| 2 | P-T-30 四态判定 ACCEPT/REJECT/DEFER/BLINDSPOT | P-E-01 `judge_qualification`（mdcg.py，判定序 BLINDSPOT→REJECT→DEFER→ACCEPT） | test_cond_match 全套 / test_p1 维度3 / test_p33 ⑥ | P-V-05 全库复算四态分布 29/4487/1（A） | ✅ 已证 |
| 3 | P-T-31 概率≠资格（相似度产生候选，条件授予资格） | P-E-05 search→judge 贯通（tier 与 state 正交） | test_p1「被 REJECT 的节点仍被召回（资格≠性能）」 | P-V-09 deja-vu 实测：显式否决对检索零影响，其 25%「负条件拒绝率」全为 BM25 词法阈值（A） | ✅ 已证（且有竞品反例对照） |
| 4 | P-T-32 负路由（能力级不适用条件，拒什么与命中什么同构） | P-E-14 `positive_body` 负条件不作召回键，反例去向 judge REJECT | test_p33 ⑤⑥ / test_p5 条件级负路由 | P-V-01 L3 对照：无路由 0.20 → 条件路由 1.00 且双向对称（A） | ✅ 已证 |
| 5 | P-T-42/43 条件扰动反事实（条件词互换→路由/计划/结果三变） | 条件扰动实验（理论仓 §6/§11，实验 3/3 切换+混合条件 BLINDSPOT） | **test_ccg_perturb.py 已入库（2026-09-13）**：36 断言确定性反事实 | ✅ 已证并归位（A）：①结果变 3/3（条件词互换→ACCEPT↔REJECT 双向对称翻转）②路由变（剥条件行正文逐字相同→桶不同）③计划变（T0/T1 桶路↔T2/T3 全局路；无路由对照同对得分相等 0.75——相似度无法区分、归属由条件决定）④混合条件 BLINDSPOT=要素不全触发（如实边界：混域词面不检测矛盾）。可证伪口径：任一②失败即推翻命题 | ✅ 已证 |
| 6 | P-T-40 信息增益门槛（gain≤0 → DEFER_EXHAUSTED，即「新增条件须有预测增益」防线） | **autonomy.explore 四保护投影（2026-09-13）**：①深度=单轮不递归展开 ②本轮 seen 循环去重 ③`gain_gate` 增益门槛（读上轮 outcomes 实现值，连续 2 次终态停滞→DEFER_EXHAUSTED 冷却）④`limit` 预算 + `bypass_gain` 显式豁免（2.9.3.1） | **test_gain_gate 14 断言（2026-09-13）**：首探放行/停滞冷却/冷却过期/证据不足/终态有变化/proposals 拦截 deferred 可审计/enforce_gain 放行/outcomes 留痕/预算豁免 | B（judge 当前单层无递归；四保护投影到 explore 探索循环=当前唯一显式循环面，诚实边界） | ✅ 已证 |
| 7 | P-T-44 分层判断 escalation（L1→L2→L3，盲区是所有层共同结论） | 无直接对象（judge 为单层判定） | 无 | D | 🔴 工程缺口 |
| 8 | P-T-45 递归缩小信息差至可验证执行路径（可证伪四条 §12.2） | P-E-18 autonomy/predict（盲区提案→五态终判） | test_autonomy / test_p17 / **test_transfer** | P-V-15 transfer_test 首批实测（test_transfer，2026-09-13）：固定题族 20 新实体×20 旧实体（同条件空间四槽+CCG 六要素），成功率 **0.0→1.0**（Δ=1.0，完全分离，2×SE 显著），迁移后 20/20 全 ACCEPT，旧任务不遗忘 20→20，样本守卫 n<20 拒判（D→A） | ✅ 已证 |
| 9 | P-T-46 搜索=导航（先定位条件空间再激活） | P-E-12 两阶段收敛路由 + P-E-13 T0/T1 阶梯 | test_p1 维度5 | P-V-06 T0 1.1ms vs 全量 13.9ms（12.4x），回退代价 0.91x（A） | ✅ 已证 |
| 10 | P-T-54 实体/关系附条件空间声明，未验证关系衰减 | condition_space 字段 + forgetting | test_p5 槽位正交 | P-V-05（A） | ✅ 已证 |
| 11 | P-T-38 五失配维度（四要素+条件边界） | P-E-06 四槽（**无「条件边界」第五槽**） | — | — | 🟡 部分：第五维未投影 |
| 12 | P-T-37 三方条件空间（任务/感知/声明）失败归因 | designer.py（skills 侧方法论，非引擎内置） | — | C | 🟡 方法论在 skills 层，引擎未内置 |
| 13 | P-T-33 四态判定不交给 LLM（导航税） | 判定全为确定性词面/规则（P-E-01/02） | test_cond_match ②「无语义猜测」 | 结构性成立（代码即证），导航税本身未度量（理论盲区91已登记） | ✅ 结构已证 / 🟡 税未量化 |
| 14 | P-T-47 执行资格声明（六要素+三闸门） | 部分：judge reason + 验证基底声明 | 部分 | C | 🟡 六要素未全投影 |
| 15 | P-T-51/52 状态空间 S=(C,K,B)、目标函数 π* | 无对应（DEV-003 自认有损投影） | — | — | 🔴 理论层缺口（暂不投影，符合投影声明） |

统计：✅ 已证 8 ｜ 🟡 部分/待归位 6 ｜ 🔴 缺口 4（#6 工程侧、#7 工程侧、#8 双侧、#15 理论侧）

## 1A. 端口/锚定/五层记忆映射矩阵（v0.2，编号续接）

| # | 理论命题（P-T） | 工程对象（P-E） | 测试 | 证据（强度） | 状态 |
|---|---|---|---|---|---|
| 16 | P-T-85 五层记忆职责（锚点/结构/知识/情境/自我） | P-E-21 `LAYERS` 八槽（+rejected/unresolved/goals）；**structural 业务写入口 = `md_cg/tasks.py`（2026-09-16）** | 逐层测试齐；**test_tasks 75 断言（2026-09-16）** | G4 对账锚点闭合 228+6102+4+5=6339（A）；~~structural 实库 0 条~~ → 任务实体为该层正式业务入口（结构性一等节点：layer=structural、importance=0.8、跨会话不遗忘） | ✅ 层已启用（#16 结案，裁决见 §3A.5） |
| 17 | P-T-86 SELF 薄自我（身份+价值观+图接口，不存内容） | P-E-31 `self_state` 状态卡：九项只存当前值+指针，样本不足 unknown 不编造 | test_p16 51/51 | P-V-21/25/26 版本链+审计实库在跑（A） | ✅ 结构已证 |
| 18 | P-T-87 会话连续性（启动自动加载 SELF，按 session 索引） | P-E-31 session 切片+recall | test_p45 30/30 全绿 | P-V-34/37 跨会话续接实测（A） | ✅ 机制已证；🔴 实库纵深仅 2 版本留痕（月级连续性未证） |
| 19 | P-T-99 协定式自我赋予/扮演论边界（角色≠自身锚点） | P-E-26 `set_anchor` kind 分流（self→self 层，role→anchor 层，越界拒绝） | test_p10 24 项全绿 | P-V-24/30 实库锚点+保护留痕（A） | ✅ 已证 |
| 20 | P-T-79 stable 是带 TTL 的租约，超时降级 | P-E-36 `verify` 非对称降级（confirmed +0.05 / weakened −0.15）+ DEMOTE 0.2 自动迁层 | test_p6 72 项 | B | 🟡 机制在；TTL 显式租约未投影 |
| 21 | P-T-81 降级作用于状态不作用于记录 | P-E-36 evidence_log 累加 + falsified→rejected 层保留 | test_p30 | C | ✅ 结构已证（负记忆保留纪律） |
| 22 | P-T-77 完全确认四条件（通道一致∧强验证命中∧跨时稳定∧无矛盾） | P-E-32 `verify_answer` 三层校验 + chat_engine 出口强制闸 | test_verify_answer 31/31（2026-09-13） | P-V-32 专项测试落地：三层拦截矩阵 13 拒绝案例留痕+通过面+边界值+强制闸接线断言+自指案例；anchor 层 4 节点 vb=null→test + confirmed 证据链（A） | ✅ 已证 |
| 23 | P-T-73/74 置信度≠可信度、通道贝叶斯后验更新 | **predict.py 通道贝叶斯（2026-09-13）**：`feedback(channel=...)` 通道标注 + `beta_posterior` Beta-Bernoulli 后验（α0=8/β0=12，先验均值=BASE_HIT_RATE 口径无缝）+ `channel_posterior` 通道分解；self_state 预测面增 posterior 字段 | **test_predict_beta 23 断言（2026-09-13）**：先验=基线/小样本收缩（n=5 全命中→0.52 不过度自信）/大样本收敛（n=200→0.9455）/通道分离/留痕 unlabeled 诚实/dynamic_hit_threshold 零回归 | A | ✅ 已证 |
| 24 | P-T-75 弱验证/强验证分级（感知互裁慢、行动裁决快） | 部分：`consistency.check` 写入预检 ≈ 弱验证；世界反馈侧无 | 无 | D | 🔴 缺口 |
| 25 | P-T-62/63 三端口架构（输入/内部/输出）与输出端口=验证前提 | **文档层声明已落（2026-09-13，v0.4）**：输入端口=AEIS 感知侧（ingest/add 写入门控）；内部端口=predict/reflect（预测-反馈-反思）；输出端口=宿主工具面（MCP/explore/mutual_watch 心跳拉起——互维守护面即输出端口对环境施加动作的最小具身形态）。测试面仍留具身场景 | **无端口专属测试**（机制测试已在位：test_preflight_failclosed 输入闸 / test_predict_beta 内部端口 / test_sustain_mutual 输出动作面） | C→B（映射边界声明 + 三端口各自的机制测试在位） | 🟡 声明已落，专属具身场景测试待真实多实例环境 |
| 26 | P-T-94 节点必带四维条件空间；importance≥0.7 不可遗忘 | P-E-22 `add` 写入门控 + P-E-24 `protect` 三层保护 | test_p37/p9 全绿 | P-V-05 96.7% 声明（A） | ✅ 已证 |
| 27 | P-T-92 睡眠整理/联想保留（无边=候选垃圾） | P-E-37 `consolidate.promote` 三门槛（CCG 齐/非密文/热度） | test_p30 ⑦ | 知识层治理 94.7% 孤岛→高联系密度（C，理论仓数据） | 🟡 机制在，本仓无治理前后数字 |
| 28 | P-T-105~108 维生三级仲裁（P2 观察/P1 响应/P0 保护） | sustain 心跳/自愈/水位续接 + **互维守护面（2026-09-13）** | test_p14 44 项全绿 + **test_sustain_mutual 12 断言** | P-V-36（A/B） | ✅ 已证（单机形态三级仲裁+互维机制；跨机 W4 实机部署留待，见 #30） |
| 29 | P-T-114 preflight 输出前价值观检查 | P-E-44 AEIS 门面 + md_cg 镜像同步 **fail-closed**（未装配 ok:False+装配错误透传） | test_preflight_failclosed 双侧 12+12（2026-09-13） | 未装配拦截+错误可见可修、装配路径零干扰委托、真身↔镜像逐行一致；AEIS test_body 50/50 回归（A） | ✅ 已证 |
| 30 | P-T-110 互维闭环（双实例心跳互检） | **sustain.py 互维守护面（2026-09-13）**：`mutual_watch`（pid 探活防误杀→冷却防风暴→幂等拉起→**验戳新鲜闭合**）+ `mutual_status` 双亡检测（both_dead→外部告警语义）；全链路审计进 _sustain.jsonl | **test_sustain_mutual 12 断言（2026-09-13）**：端到端真子进程拉起 recovered=True / **W4 场景显式暴露**（拉起已执行但戳未更新→recovered=False+unresponsive，不假装成功）/冷却/无手段诚实上报/alive_but_stale 防误杀/双亡 | A（同机双实例心跳互检实证；v1.1 §7「拉起后验证对端戳新鲜度」部署教训已机制化） | ✅ 已证（跨机实机部署留待——机制问题已解决，余部署问题） |

统计（端口/锚定）：✅ 已证 7 ｜ 🟡 部分 5 ｜ 🔴 缺口 3（2026-09-13 v0.4：#23/#25/#30 收口后）
**跨专题不对称**：条件论 ✅8/🔴4 vs 端口/锚定 ✅4/🔴6——与外部评审评分结构（Memory/CondRouting 9.0 vs Embodied 5.5）**独立收敛**，两条证据链互相印证。**v0.4 三轨收口后**：条件论 ✅9/🔴3 vs 端口/锚定 ✅7/🟡5/🔴3——不对称显著收窄（Embodied 缺口从 6 降到 3：#7 escalation/#24 强验证分级/#31-33 D 分离待真实场景）。

## 2. 六项实现—理论张力（盘点实查，如实列出）

1. **载体/位置主动合并**：理论四元中「智能载体」无独立槽，与观测位置合并为 `observation_position`（保留未来拆分扩展位）——主动合并非遗漏。
2. **时间维双重身份**：`time_window` 参与四槽声明完整性，但不参与路由键（实测仅 3 种取值，进哈希键是纯噪声）与条件词面匹配——「参与声明、不参与匹配」。
3. **无情境 ACCEPT 兼容窗口**：query/context 全空时正条件段跳过可 ACCEPT（reason 诚实标注「未做正条件确认」）——v1 明确保留的决策，与「条件授予资格」强主张存在张力。
4. **条件路由仅覆盖 knowledge 层**：`BUCKETED_LAYERS=("knowledge",)`，contextual 层不路由——收益验证范围有限。
5. ~~**条件扰动实验工件不在库**~~（2026-09-13 已解决）：`md_cg/test_ccg_perturb.py` 36 断言入库，命题①-④全部可复现（见 §1 表 #5）。同轮偿债：judge v2 断言过期 5 处（test_p1/test_p26_refindex/test_p27_docindex 旧断言仍期待「条件未命中→ACCEPT」）已对齐 DEFER 诚实语义。
6. **实现残留**：mdcg.search 负记忆覆盖段重复实现未清理（子类 `_neg_coverage` 已替代）。
7. ~~**test_p44 白箱镜像漂移（新发现，未修）**~~（2026-09-13 已解决）：真因修正——非「api.py 未同步」（其 modified 仅为 preflight fail-closed 行为层，与 schema/数据无关），而是 md 语料真源 `_md_cg_wisdom_graph/` **空壳化**（10 个分层目录在、0 个 md 文件，gitignored 可再生产物疑被清理误删，MdCGOS 对空语料静默返回 0 节点）。修复：`migrate_wisdom_graph.export()` 重新导出（4355/4355 逐节点、L1 层级/L2 关系/L3 字段三校验全绿）→ test_p44 四面对齐全绿（结构 4355/2948 一致、候选集 20/20、端到端严格一致 19/20 + 同分并列 1 非数据差异）。防复发：test_p44 的 force=True 重建即守卫（空语料 0/0 vs 源库必 FAIL）；再生命令=`python -c "from md_cg.migrate_wisdom_graph import export; export()"`。

## 3. 外部评审五风险对照（条件论部分）

| 风险 | 本矩阵结论 |
|---|---|
| 风险1 条件论过强（防万能解释变量） | **理论已设防**：P-T-40 信息增益门槛即「新增条件须产生预测增益」的功能等价物（候选集缩减/路由确定性提升，gain≤0→DEFER_EXHAUSTED）；**工程未投影**（缺口 #6） |
| 风险3 自我指涉（须理论外盲测） | bench6 即理论外盲测：六家竞品均不知 CTP，条件词面影响检索的弱证据干扰实证发生在盲测语境（P-V-04/11） |
| 风险5 同构未证明（须溯源矩阵） | 本文件即 v0.4 溯源矩阵；条件论 ✅9 / 🟡3 / 🔴3（#7/#11/#15 待真实场景或理论裁决，见 §1） |

## 4. 缺口裁决与补测建议（按优先级）

1. **#8 transfer_test 迁移成功率**（双侧缺口，最高优先）：工具定义已有（「条件空间内新实体预测成功率，2×SE 显著性，样本<20 不判定」），predict.py 部分实现。补测设计：固定题族（同条件空间、新实体）×新旧知识库对照，产出成功率+显著性；这直接回应外部评审「Level 3 能力学习证据」与「开放持续基准」诉求。
   **✅ 已完成（2026-09-13）**：`md_cg/test_transfer.py` 落地三阶段对照（Phase A 旧库→B 纯 add 写入→C 复测），实测成功率 **0.0→1.0**（Δ=1.0 完全分离，20/20 全 ACCEPT，旧任务不遗忘 20→20），DEVIATION-005 样本守卫实跑验证；语料构造踩坑留证：CCG 要素须 `# 要素：` 标题行（正文行不识别）、生效条件须四槽渲染不得正文手写（两形态均触发 BLINDSPOT）。
2. ~~**#6 递归四保护投影**~~（**已完成 2026-09-13**）：judge 当前单层无递归（如实边界）；四保护投影到 autonomy.explore 探索循环=当前唯一显式循环面——①单轮深度②seen 循环去重③`gain_gate` 增益门槛（outcomes 实现值→DEFER_EXHAUSTED 冷却）④`limit` 预算+bypass_gain 显式豁免；test_gain_gate 14 断言全绿。
3. **#5 条件扰动实验入库**：把理论仓的条件扰动/条件词互换实验落为 md_cg 内可复现测试（对应 §12.2 可证伪四条的第一条），消掉「理论仓声明已证、代码库无工件」的裂缝。
4. **#7 分层判断 escalation**：L1→L2→L3 升级判定暂缓——需先有真实多域场景，避免为投影而投影（理论投影声明允许）。
5. **#11 第五失配维「条件边界」**：评估是否新增独立槽，或声明为派生量；先出裁决再动数据。

## 2A. 端口/锚定实现—理论张力（v0.2 盘点实查）

1. ~~**structural 层空层**~~（**已完成 2026-09-16**）：`LAYERS` 八槽齐全，structural 层此前唯一业务写入口 `identity.add_trait` 在主库从未使用（G4 对账中 0 条）。现由 `md_cg/tasks.py`（`cg(op=task)`）承担该层正式业务写入口——任务实体为结构性一等节点，直接对位「忘记已实现的工程／计划与实际不符／缺核验／换表述即新任务」四症状的机制解。
2. **「锚点」双义未消解**：anchor 身份锚点层（不可遗忘）与 `reconstruct_scene`/`anchor_from_description` 的检索锚点（动态词面选出、跨层）同名异物；重构的 blindspot 判定不读 anchor 层语义。
3. ~~**preflight fail-open**~~（**已完成 2026-09-13**）：门面（AEIS 真身+md_cg 镜像同步）改为 fail-closed——未装配返回 `ok:False + status=fail_closed + 装配错误透传`，「检查未执行」不再冒充「检查通过」；实现体实际在库内镜像可审计（v0.2 盘点时误判为仓外）。
4. **自我连续性纵深极薄**：机制测试全绿（P16 51/51、P45 30/30），但实库 `_self_state.jsonl` 仅 2 个版本、self 层 5 节点中 3 个 vb=null——「一个月后还是同一个我」目前仅小时级留痕支撑，v2.0 评分报告已自declared 此边界。
5. ~~**锚定验证三层校验零运行记录**~~（**已完成 2026-09-13**）：`test_verify_answer.py` 31/31 落地——13 条拒绝案例留痕（L1 内部格式泄漏/超长、L2 未知类别/无hits/缺导航、L3 截断/占位）+ 强制闸接线静态断言 + 自指案例；anchor 层 4 节点经 add(override)+verify(confirmed) 回填 vb=test 并落保护。

## 3A. 端口/锚定缺口裁决（按优先级）

1. ~~**#22 verify_answer 专项测试**~~（**已完成 2026-09-13，B→A**）：`md_cg/test_verify_answer.py` 31/31——三层拦截确定性用例矩阵+拒绝案例留痕 13 条+chat_engine 强制闸接线断言；anchor 层 4 节点 vb 回填（add override 快照通道 + verify confirmed 证据链，importance≥0.7 自动落保护）。
2. ~~**#29 preflight 改 fail-closed**~~（**已完成 2026-09-13**）：未装配返回拦截（ok:False+fail_closed+错误透传）而非放行，与写入侧纪律对齐；双侧专项测试 12+12、装配路径回归 50/50。
3. ~~**#25 端口命题最小投影**~~（**已完成 2026-09-13**）：文档层声明已落（§1A #25 行）——输入=AEIS 感知侧（ingest/add 闸）、内部=predict/reflect、输出=宿主工具面（mutual_watch 心跳拉起=输出端口最小具身形态）；三端口各自机制测试已在位（failclosed/predict_beta/sustain_mutual），专属具身场景测试待真实多实例环境。
4. **#18 自我连续性长周期实证**：机制已证，缺的是时间——由日常运行自然积累（self_state refresh 留痕），数月后回看，无需专项开发。
5. ~~**#16 structural 层启用裁决**~~（**已裁决并落地 2026-09-16**）：两条候选中选「接入实际流程」而非「文档声明暂缓」——structural 层新增正式业务写入口 `md_cg/tasks.py`（任务实体）。生效条件：存在需跨会话/跨上下文压缩续接的工程任务；边界：该层只承载任务骨架（目标／计划／计划变更／结果／状态／验收／边界），执行中间过程仍由情境层自持、任务产出知识归知识层，三者不互相替代；不适用：闲聊与无延续性的单步操作。

## 1B. 信息差公理映射矩阵（v0.3，编号续接；理论 24 条 P-T-117~140 / 工程 P-E-46~59 / 证据 P-V-46~57）

| # | 理论命题（P-T） | 工程对象（P-E） | 测试 | 证据（强度） | 状态 |
|---|---|---|---|---|---|
| 31 | P-T-117 信息差增定律 dD/dt≥0（自我定级「可操作化的经验命题」） | D_task/D_meta 分离未投影 | — | — | 🟡 **结构性投影已落地（2026-09-20）**：`md_cg/d_meta.py`（边界压力向量三代理）+ `mdcg.reflect` 并列落 `d_meta`/`d_meta_delta` + MCP 观测面 `cg(op=metacognition, action="d_meta")`；**D_meta 不入 `_compute_d`**（D_task 口径零变更，数值不互换）。剩余=真实多域场景下对增定律的轨迹观测（方案 v0.2 阶段 6 交使用者） |
| 32 | P-T-124 D_norm 四分量（w=0.30/0.25/0.30/0.15，**权重无来源推导**） | P-E-59 `wisdom_book.dex_auto_verify` 唯一数值实现 | 无 | B | ✅ **桥接裁决完成（2026-09-13）**：智能论3.4 §2.7.0 新增 DEV-002a——主工程检索口径 D 与 wisdom 四分量 D_norm 同为 D_task 工程化身（检索链路/验证链路），对象同一、数值不互换；两侧实现 docstring/注释已落口径声明。权重来源推导仍开放（诚实保留） |
| 33 | P-T-126/127 D_task/D_meta 分离（学习悖论消解，有实测方案） | D_task≈`reflect` 的 D（检索命中口径）；D_meta 无对象 | 无 | P-V-51 审计自认「world 侧无处可写」 | 🟡 **对象已建立（2026-09-20）**：D_meta = 边界压力向量（`events_pressure`/`unmodeled_growth`/`boundary_violation_rate`），三代理各自 [0,1] 归一、**不合成单值**（DEV-002a）；落 `_reflection.jsonl.d_meta`、`_explore.jsonl.meta_outcomes`、`self_state.prediction.d_meta_trend`。剩余=代理与「世界侧真实事件」的距离（诚实口径：只计**进入系统但未被消化**的事件，非世界真实未发生事件） |
| 34 | P-T-133 情绪=d²D/dt²（自我定级「形式化假设」） | P-E-48 `_emotion`（D2_EMOTION=0.05 阈值三态） | test_p12 状态转移断言 | P-V-47（B）；实库已激活（2026-09-13）：emotion=stable（D 序列 [1,1,1] → d2=0.0；D 恒 1.0 因复合 query 检索全 DEFER/BLINDSPOT 无 ACCEPT，D 口径对 judge 结果敏感） | 🟡 机制已证、实库有值（语义弱：序列无变化） |
| 35 | P-T-134 情感=d²T/dt² | P-E-49 trust 二阶差分 | test_p12 D1-D3 | B | 🟡 同上 |
| 36 | P-T-140 P_trust≠信息差补集、P_gap 判定 | P-E-49 `metacognition.trust` + P-E-58 `links.py` 链路级 P_trust（±步进/衰减/降级） | test_p12 | 实库 p_trust=0.75（12 事件 9 confirmed，2026-09-13：补测③ anchor 回填 4 + 本轮 preflight gate confirmed + 既有 mem 验证；p_gap=0.0 与 D=1.0 一致自洽） | 🟡 信任面已激活 |
| 37 | P-T-129/130 Value=ΔD·σ(Gain) 与猎奇出局条款 | **autonomy 价值链（2026-09-13）**：score（ΔD 代理）定价排序 × `gain_gate` σ(Gain) 资格筛选；**期望/实现分离**：proposals 端 σ 由上轮 explore 的 outcomes 实现值推导；被拦提案落 deferred 可审计（不静默丢弃）；`bypass_gain` 预算化豁免（2.9.3.1 非任务探索显式预算） | **test_gain_gate 14 断言**（含「被拦提案的 ΔD 定价仍在——被拦原因=σ=0 非 score 低」）；连续 σ 留待 D_task 分离后（当前 0/1 粗粒度，诚实边界） | A | ✅ 已证 |
| 38 | P-T-137 管理三工具（索引+条件路由/BLINDSPOT/误差更新） | 三工具全部在库（routing/judge/predict.feedback） | 全绿 | 条件论部分 A 级 | ✅ 管理宣言的工程形态已证 |
| 39 | P-T-136（本次抽取发现的理论缺口）统一变量**无边界条款**——审美/创造零条款，好奇心被降格为策略而非豁免 | — | — | — | ✅ **条款已写入（2026-09-13）**：智能论3.4 §2.9.3.1「统一变量边界条款」——独立动机类/预算化豁免（非无限豁免）/与猎奇出局分条件空间/白箱纪律自限（规范性声明非定理）。对 GPT 三核心问题①的正面回答基础就位 |

统计（信息差）：✅ 4 ｜ 🟡 5 ｜ 🔴 0（2026-09-20：#31/#33 由结构性投影收口——`md_cg/d_meta.py` + 双 D 落盘 + 观测面，方案 `docs/mdcg/D_meta_工程化方案_v0.2.md`；剩余为真实多域场景轨迹观测，不再计 🔴）

## 2B. 信息差实现—理论张力（v0.3 盘点实查）

1. **实库空转**（v0.3 盘点发现；**2026-09-13 已基本解除**）：激活后四面有值——D 面 `_reflection.jsonl` 3 条真实反思留痕（D 序列 [1,1,1]：复合 query 检索全 DEFER/BLINDSPOT 无 ACCEPT，D 口径对 judge 结果敏感——序列无变化能力的张力如实保留）、信任面 p_trust=0.75（12 事件 9 confirmed）、校准面 calibrated（ece=0.22）、预测反馈面 `_prediction.jsonl` 3 条 feedback（样本 1→3，hit 0/3 如实；9 条真实 routes 路线证实机制走通）。取证修正：盘点时「0 次 feedback」过时——盘点会话已留 1 条（文件名为 `_prediction.jsonl` 而非 `_predict.jsonl`）。根因发现：预测面数据形态先决条件——仅 causal/sequential 边可作为预测依据（hierarchical 被 D-002 门设计性拒绝），md 知识节点无边、须选真实因果链节点（如存在论）为起点。
2. ~~**两套 D_norm 口径未打通**~~（2026-09-13 已桥接）：智能论3.4 §2.7.0 DEV-002a 声明两口径同属 D_task 工程化身（检索链路/验证链路），对象同一、数值不互换；mdcg._compute_d 与 wisdom VERIFY_WEIGHTS 两侧已落口径声明。剩余开放项：四分量权重（0.30/0.25/0.30/0.15）仍无来源推导。
3. **U_behavior 零对应**：行为偏差只存文本字段无量化结构；U_prediction_error 不回流 D（误差只进 self 的校准面统计）。
4. ~~**理论版本不同步**~~（2026-09-13 已回改）：智能论3.4 §2.7 信息差增定律处已回改为「结构对应，非数学同构——结构映射是有损投影，不构成数学等价」，与论证篇口径一致；附录15 映射表自带「映射性质声明」自限，维持原文。

## 3B. 三专题合并补测优先级（总收尾）

1. ~~**实库空转激活**~~（v0.3，成本最低）：**已完成（2026-09-13）**——真实 reflect ×3 + predict.feedback ×2（存在论因果链起点，口径先于预测声明，hit=False 如实登记）+ verify(confirmed) + self_state.refresh：D/情绪/p_trust/校准/预测反馈五面非空，audit 九条约束 PASS，状态卡 v5→v7；剩余张力如实入 §2B-1（D 恒 1.0、预测 hit 0/3）。
2. ~~**transfer_test 迁移成功率**~~（v0.1 #8，双侧最高价值）：**已完成（2026-09-13，test_transfer 7/7）**——成功率 0.0→1.0、Δ=1.0 完全分离、全 ACCEPT、无遗忘、样本守卫验证；直接回应外部评审 Level 3 诉求。
3. ~~**verify_answer 专项测试 + anchor 层 vb 回填**~~（v0.2 #22，B→A）：**已完成（2026-09-13，test_verify_answer 31/31）**——拒绝案例留痕 13 条，anchor 层 4 节点 vb=test+confirmed 证据链。
4. ~~**preflight 改 fail-closed**~~（v0.2 #29）：**已完成（2026-09-13，双侧 12+12，真身+镜像同步）**。
5. **条件扰动实验入库**（v0.1 #5，消理论仓声明与代码库工件裂缝）。
6. ~~**D_norm 口径桥接裁决**（v0.3 #32）~~：**已完成（2026-09-13）**——选「声明」路径：智能论3.4 §2.7.0 DEV-002a 声明主工程 D=检索链路工程化身、wisdom 四分量=验证链路工程化身，对象同一数值不互换；未做工程打通（两化身各自条件空间内自洽，强打通无必要）。
7. ~~**统一变量边界条款**（v0.3 #39，理论仓侧）~~：**已完成（2026-09-13）**——智能论3.4 §2.9.3.1 落「不归约」四条边界（独立动机类/预算化豁免/分条件空间/白箱自限）。

## 5. 下一步

- 三专题审计完成（条件论 ✅8/🔴4、端口锚定 ✅4/🔴6、信息差 ✅1/🔴4+实库空转）——矩阵 v0.3 定稿
- 补测按 §3B 优先级执行；理论仓侧事项（版本不同步、边界条款）单列移交
- 本文件含本地工作区路径与内部节点引用，**公开仓推送前需脱敏审查**（路径、内部报告名）

---
*盘点方法：三路并行多智能体侦察（理论仓 CommonTrustProtocol ／ md_cg 工程面 ／ 测试证据面），主仓对齐裁决。*
